<?php
// Start of extension: standard
// - parsed on POSIX with PHP 5.6.34 (extension version 5.6.34)
// - parsed on Windows with PHP 5.6.34 (extension version 5.6.34)

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ABDAY_1 = 131072;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ABDAY_2 = 131073;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ABDAY_3 = 131074;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ABDAY_4 = 131075;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ABDAY_5 = 131076;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ABDAY_6 = 131077;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ABDAY_7 = 131078;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ABMON_1 = 131086;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ABMON_10 = 131095;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ABMON_11 = 131096;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ABMON_12 = 131097;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ABMON_2 = 131087;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ABMON_3 = 131088;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ABMON_4 = 131089;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ABMON_5 = 131090;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ABMON_6 = 131091;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ABMON_7 = 131092;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ABMON_8 = 131093;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ABMON_9 = 131094;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ALT_DIGITS = 131119;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const AM_STR = 131110;

/**
 * <code>ARRAY_FILTER_USE_BOTH</code> is used with
 * <code>array_filter</code> to pass both value and key to the given callback function.
 * Added in PHP 5.6.0.
 *
 * @link http://www.php.net/manual/en/array.constants.php#constant.array-filter-use-both
 *
 * @var int
 */
const ARRAY_FILTER_USE_BOTH = 1;

/**
 * <code>ARRAY_FILTER_USE_KEY</code> is used with
 * <code>array_filter</code> to pass each key as the first argument to the given callback function.
 * Added in PHP 5.6.0.
 *
 * @link http://www.php.net/manual/en/array.constants.php#constant.array-filter-use-key
 *
 * @var int
 */
const ARRAY_FILTER_USE_KEY = 2;

/**
 * assert.active
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.assert-active
 *
 * @var int
 */
const ASSERT_ACTIVE = 1;

/**
 * assert.bail
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.assert-bail
 *
 * @var int
 */
const ASSERT_BAIL = 3;

/**
 * assert.callback
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.assert-callback
 *
 * @var int
 */
const ASSERT_CALLBACK = 2;

/**
 * assert.quiet_eval
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.assert-quiet-eval
 *
 * @var int
 */
const ASSERT_QUIET_EVAL = 5;

/**
 * assert.warning
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.assert-warning
 *
 * @var int
 */
const ASSERT_WARNING = 4;

/**
 * <code>CASE_LOWER</code> is used with
 * <code>array_change_key_case</code> and is used to convert array
 * keys to lower case. This is also the default case for
 * <code>array_change_key_case</code>.
 *
 * @link http://www.php.net/manual/en/array.constants.php#constant.case-lower
 *
 * @var int
 */
const CASE_LOWER = 0;

/**
 * <code>CASE_UPPER</code> is used with
 * <code>array_change_key_case</code> and is used to convert array
 * keys to upper case.
 *
 * @link http://www.php.net/manual/en/array.constants.php#constant.case-upper
 *
 * @var int
 */
const CASE_UPPER = 1;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.char-max
 *
 * @var int
 */
const CHAR_MAX = 127;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const CODESET = 14;

/**
 * @link http://www.php.net/manual/en/misc.constants.php#constant.connection-aborted
 *
 * @var int
 */
const CONNECTION_ABORTED = 1;

/**
 * @link http://www.php.net/manual/en/misc.constants.php#constant.connection-normal
 *
 * @var int
 */
const CONNECTION_NORMAL = 0;

/**
 * @link http://www.php.net/manual/en/misc.constants.php#constant.connection-timeout
 *
 * @var int
 */
const CONNECTION_TIMEOUT = 2;

/**
 * @link http://www.php.net/manual/en/array.constants.php#constant.count-normal
 *
 * @var int
 */
const COUNT_NORMAL = 0;

/**
 * @link http://www.php.net/manual/en/array.constants.php#constant.count-recursive
 *
 * @var int
 */
const COUNT_RECURSIVE = 1;

/**
 * -1
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.credits-all
 *
 * @var float
 */
const CREDITS_ALL = 4294967295;

/**
 * 16
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.credits-docs
 *
 * @var int
 */
const CREDITS_DOCS = 16;

/**
 * 32
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.credits-fullpage
 *
 * @var int
 */
const CREDITS_FULLPAGE = 32;

/**
 * 2
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.credits-general
 *
 * @var int
 */
const CREDITS_GENERAL = 2;

/**
 * 1
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.credits-group
 *
 * @var int
 */
const CREDITS_GROUP = 1;

/**
 * 8
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.credits-modules
 *
 * @var int
 */
const CREDITS_MODULES = 8;

/**
 * 64
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.credits-qa
 *
 * @var int
 */
const CREDITS_QA = 64;

/**
 * 4
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.credits-sapi
 *
 * @var int
 */
const CREDITS_SAPI = 4;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const CRNCYSTR = 262159;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.crypt-blowfish
 *
 * @var int
 */
const CRYPT_BLOWFISH = 1;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.crypt-ext-des
 *
 * @var int
 */
const CRYPT_EXT_DES = 1;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.crypt-md5
 *
 * @var int
 */
const CRYPT_MD5 = 1;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.crypt-salt-length
 *
 * @var int
 */
const CRYPT_SALT_LENGTH = 123;

/**
 * @var int
 */
const CRYPT_SHA256 = 1;

/**
 * @var int
 */
const CRYPT_SHA512 = 1;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.crypt-std-des
 *
 * @var int
 */
const CRYPT_STD_DES = 1;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const D_FMT = 131113;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const D_T_FMT = 131112;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const DAY_1 = 131079;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const DAY_2 = 131080;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const DAY_3 = 131081;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const DAY_4 = 131082;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const DAY_5 = 131083;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const DAY_6 = 131084;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const DAY_7 = 131085;

/**
 * @link http://www.php.net/manual/en/dir.constants.php#constant.directory-separator
 *
 * @var string
 */
const DIRECTORY_SEPARATOR = '/';

/**
 * IPv4 Address Resource
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.dns-a
 *
 * @var int
 */
const DNS_A = 1;

/**
 * @var int
 */
const DNS_A6 = 16777216;

/**
 * IPv6 Address Resource
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.dns-aaaa
 *
 * @var int
 */
const DNS_AAAA = 134217728;

/**
 * Iteratively query the name server for
 * each available record type.
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.dns-any
 *
 * @var int
 */
const DNS_ALL = 251713587;

/**
 * Any Resource Record. On most systems
 * this returns all resource records, however
 * it should not be counted upon for critical
 * uses. Try <code>DNS_ALL</code> instead.
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.dns-any
 *
 * @var int
 */
const DNS_ANY = 268435456;

/**
 * Alias (Canonical Name) Resource
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.dns-cname
 *
 * @var int
 */
const DNS_CNAME = 16;

/**
 * Host Info Resource (See IANA's
 * <code>Operating System Names</code>
 * for the meaning of these values)
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.dns-hinfo
 *
 * @var int
 */
const DNS_HINFO = 4096;

/**
 * Mail Exchanger Resource
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.dns-mx
 *
 * @var int
 */
const DNS_MX = 16384;

/**
 * @var int
 */
const DNS_NAPTR = 67108864;

/**
 * Authoritative Name Server Resource
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.dns-ns
 *
 * @var int
 */
const DNS_NS = 2;

/**
 * Pointer Resource
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.dns-ptr
 *
 * @var int
 */
const DNS_PTR = 2048;

/**
 * Start of Authority Resource
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.dns-soa
 *
 * @var int
 */
const DNS_SOA = 32;

/**
 * @var int
 */
const DNS_SRV = 33554432;

/**
 * Text Resource
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.dns-txt
 *
 * @var int
 */
const DNS_TXT = 32768;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.ent-compat
 *
 * @var int
 */
const ENT_COMPAT = 2;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.ent-disallowed
 *
 * @var int
 */
const ENT_DISALLOWED = 128;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.ent-html401
 *
 * @var int
 */
const ENT_HTML401 = 0;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.ent-html5
 *
 * @var int
 */
const ENT_HTML5 = 48;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.ent-ignore
 *
 * @var int
 */
const ENT_IGNORE = 4;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.ent-noquotes
 *
 * @var int
 */
const ENT_NOQUOTES = 0;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.ent-quotes
 *
 * @var int
 */
const ENT_QUOTES = 3;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.ent-substitute
 *
 * @var int
 */
const ENT_SUBSTITUTE = 8;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.ent-xhtml
 *
 * @var int
 */
const ENT_XHTML = 32;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.ent-xml1
 *
 * @var int
 */
const ENT_XML1 = 16;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ERA = 131116;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ERA_D_FMT = 131118;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ERA_D_T_FMT = 131120;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const ERA_T_FMT = 131121;

/**
 * @link http://www.php.net/manual/en/array.constants.php#constant.extr-if-exists
 *
 * @var int
 */
const EXTR_IF_EXISTS = 6;

/**
 * @link http://www.php.net/manual/en/array.constants.php#constant.extr-overwrite
 *
 * @var int
 */
const EXTR_OVERWRITE = 0;

/**
 * @link http://www.php.net/manual/en/array.constants.php#constant.extr-prefix-all
 *
 * @var int
 */
const EXTR_PREFIX_ALL = 3;

/**
 * @link http://www.php.net/manual/en/array.constants.php#constant.extr-prefix-if-exists
 *
 * @var int
 */
const EXTR_PREFIX_IF_EXISTS = 5;

/**
 * @link http://www.php.net/manual/en/array.constants.php#constant.extr-prefix-invalid
 *
 * @var int
 */
const EXTR_PREFIX_INVALID = 4;

/**
 * @link http://www.php.net/manual/en/array.constants.php#constant.extr-prefix-same
 *
 * @var int
 */
const EXTR_PREFIX_SAME = 2;

/**
 * @link http://www.php.net/manual/en/array.constants.php#constant.extr-refs
 *
 * @var int
 */
const EXTR_REFS = 256;

/**
 * @link http://www.php.net/manual/en/array.constants.php#constant.extr-skip
 *
 * @var int
 */
const EXTR_SKIP = 1;

/**
 * Append content to existing file.
 *
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.file-append
 *
 * @var int
 */
const FILE_APPEND = 8;

/**
 * Binary mode (since PHP 5.2.7).
 * <blockquote>
 * This constant has no effect, and is only available for
 * <code>forward compatibility</code>.
 * </blockquote>
 *
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.file-binary
 *
 * @var int
 */
const FILE_BINARY = 0;

/**
 * Strip EOL characters (since PHP 5).
 *
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.file-ignore-new-lines
 *
 * @var int
 */
const FILE_IGNORE_NEW_LINES = 2;

/**
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.file-no-default-context
 *
 * @var int
 */
const FILE_NO_DEFAULT_CONTEXT = 16;

/**
 * Skip empty lines (since PHP 5).
 *
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.file-skip-empty-lines
 *
 * @var int
 */
const FILE_SKIP_EMPTY_LINES = 4;

/**
 * Text mode (since PHP 5.2.7).
 * <blockquote>
 * This constant has no effect, and is only available for
 * <code>forward compatibility</code>.
 * </blockquote>
 *
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.file-text
 *
 * @var int
 */
const FILE_TEXT = 0;

/**
 * Search for <code>filename</code> in
 * include_path (since PHP 5).
 *
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.file-use-include-path
 *
 * @var int
 */
const FILE_USE_INCLUDE_PATH = 1;

/**
 * Caseless match. Part of the GNU extension.
 *
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.fnm-casefold
 *
 * @var int
 */
const FNM_CASEFOLD = 16;

/**
 * Disable backslash escaping.
 *
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.fnm-noescape
 *
 * @var int
 */
const FNM_NOESCAPE = 2;

/**
 * Slash in string only matches slash in the given pattern.
 *
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.fnm-pathname
 *
 * @var int
 */
const FNM_PATHNAME = 1;

/**
 * Leading period in string must be exactly matched by period in the given pattern.
 *
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.fnm-period
 *
 * @var int
 */
const FNM_PERIOD = 4;

/**
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.glob-available-flags
 *
 * @var int
 */
const GLOB_AVAILABLE_FLAGS = 9303;

/**
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.glob-brace
 *
 * @var int
 */
const GLOB_BRACE = 1024;

/**
 * @var int
 */
const GLOB_ERR = 1;

/**
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.glob-mark
 *
 * @var int
 */
const GLOB_MARK = 2;

/**
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.glob-nocheck
 *
 * @var int
 */
const GLOB_NOCHECK = 16;

/**
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.glob-noescape
 *
 * @var int
 */
const GLOB_NOESCAPE = 64;

/**
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.glob-nosort
 *
 * @var int
 */
const GLOB_NOSORT = 4;

/**
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.glob-onlydir
 *
 * @var int
 */
const GLOB_ONLYDIR = 8192;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.html-entities
 *
 * @var int
 */
const HTML_ENTITIES = 1;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.html-specialchars
 *
 * @var int
 */
const HTML_SPECIALCHARS = 0;

/**
 * Image type constant used by the <code>image_type_to_mime_type</code>
 * and <code>image_type_to_extension</code> functions.
 *
 * @link http://www.php.net/manual/en/image.constants.php#constant.imagetype-bmp
 *
 * @var int
 */
const IMAGETYPE_BMP = 6;

/**
 * @var int
 */
const IMAGETYPE_COUNT = 18;

/**
 * Image type constant used by the <code>image_type_to_mime_type</code>
 * and <code>image_type_to_extension</code> functions.
 *
 * @link http://www.php.net/manual/en/image.constants.php#constant.imagetype-gif
 *
 * @var int
 */
const IMAGETYPE_GIF = 1;

/**
 * Image type constant used by the <code>image_type_to_mime_type</code>
 * and <code>image_type_to_extension</code> functions.
 * (Available as of PHP 5.3.0)
 *
 * @link http://www.php.net/manual/en/image.constants.php#constant.imagetype-ico
 *
 * @var int
 */
const IMAGETYPE_ICO = 17;

/**
 * Image type constant used by the <code>image_type_to_mime_type</code>
 * and <code>image_type_to_extension</code> functions.
 *
 * @link http://www.php.net/manual/en/image.constants.php#constant.imagetype-iff
 *
 * @var int
 */
const IMAGETYPE_IFF = 14;

/**
 * Image type constant used by the <code>image_type_to_mime_type</code>
 * and <code>image_type_to_extension</code> functions.
 *
 * @link http://www.php.net/manual/en/image.constants.php#constant.imagetype-jb2
 *
 * @var int
 */
const IMAGETYPE_JB2 = 12;

/**
 * Image type constant used by the <code>image_type_to_mime_type</code>
 * and <code>image_type_to_extension</code> functions.
 *
 * @link http://www.php.net/manual/en/image.constants.php#constant.imagetype-jp2
 *
 * @var int
 */
const IMAGETYPE_JP2 = 10;

/**
 * Image type constant used by the <code>image_type_to_mime_type</code>
 * and <code>image_type_to_extension</code> functions.
 *
 * @link http://www.php.net/manual/en/image.constants.php#constant.imagetype-jpc
 *
 * @var int
 */
const IMAGETYPE_JPC = 9;

/**
 * Image type constant used by the <code>image_type_to_mime_type</code>
 * and <code>image_type_to_extension</code> functions.
 *
 * @link http://www.php.net/manual/en/image.constants.php#constant.imagetype-jpeg
 *
 * @var int
 */
const IMAGETYPE_JPEG = 2;

/**
 * Image type constant used by the <code>image_type_to_mime_type</code>
 * and <code>image_type_to_extension</code> functions.
 *
 * @link http://www.php.net/manual/en/image.constants.php#constant.imagetype-jpeg2000
 *
 * @var int
 */
const IMAGETYPE_JPEG2000 = 9;

/**
 * Image type constant used by the <code>image_type_to_mime_type</code>
 * and <code>image_type_to_extension</code> functions.
 *
 * @link http://www.php.net/manual/en/image.constants.php#constant.imagetype-jpx
 *
 * @var int
 */
const IMAGETYPE_JPX = 11;

/**
 * Image type constant used by the <code>image_type_to_mime_type</code>
 * and <code>image_type_to_extension</code> functions.
 *
 * @link http://www.php.net/manual/en/image.constants.php#constant.imagetype-png
 *
 * @var int
 */
const IMAGETYPE_PNG = 3;

/**
 * Image type constant used by the <code>image_type_to_mime_type</code>
 * and <code>image_type_to_extension</code> functions.
 *
 * @link http://www.php.net/manual/en/image.constants.php#constant.imagetype-psd
 *
 * @var int
 */
const IMAGETYPE_PSD = 5;

/**
 * Image type constant used by the <code>image_type_to_mime_type</code>
 * and <code>image_type_to_extension</code> functions.
 *
 * @link http://www.php.net/manual/en/image.constants.php#constant.imagetype-swc
 *
 * @var int
 */
const IMAGETYPE_SWC = 13;

/**
 * Image type constant used by the <code>image_type_to_mime_type</code>
 * and <code>image_type_to_extension</code> functions.
 *
 * @link http://www.php.net/manual/en/image.constants.php#constant.imagetype-swf
 *
 * @var int
 */
const IMAGETYPE_SWF = 4;

/**
 * Image type constant used by the <code>image_type_to_mime_type</code>
 * and <code>image_type_to_extension</code> functions.
 *
 * @link http://www.php.net/manual/en/image.constants.php#constant.imagetype-tiff-ii
 *
 * @var int
 */
const IMAGETYPE_TIFF_II = 7;

/**
 * Image type constant used by the <code>image_type_to_mime_type</code>
 * and <code>image_type_to_extension</code> functions.
 *
 * @link http://www.php.net/manual/en/image.constants.php#constant.imagetype-tiff-mm
 *
 * @var int
 */
const IMAGETYPE_TIFF_MM = 8;

/**
 * @var int
 */
const IMAGETYPE_UNKNOWN = 0;

/**
 * Image type constant used by the <code>image_type_to_mime_type</code>
 * and <code>image_type_to_extension</code> functions.
 *
 * @link http://www.php.net/manual/en/image.constants.php#constant.imagetype-wbmp
 *
 * @var int
 */
const IMAGETYPE_WBMP = 15;

/**
 * Image type constant used by the <code>image_type_to_mime_type</code>
 * and <code>image_type_to_extension</code> functions.
 *
 * @link http://www.php.net/manual/en/image.constants.php#constant.imagetype-xbm
 *
 * @var int
 */
const IMAGETYPE_XBM = 16;

/**
 * INF (as a float)
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.inf
 *
 * @var float
 */
const INF = INF;

/**
 * -1
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.info-all
 *
 * @var float
 */
const INFO_ALL = 4294967295;

/**
 * 4
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.info-configuration
 *
 * @var int
 */
const INFO_CONFIGURATION = 4;

/**
 * 2
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.info-credits
 *
 * @var int
 */
const INFO_CREDITS = 2;

/**
 * 16
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.info-environment
 *
 * @var int
 */
const INFO_ENVIRONMENT = 16;

/**
 * 1
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.info-general
 *
 * @var int
 */
const INFO_GENERAL = 1;

/**
 * 64
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.info-license
 *
 * @var int
 */
const INFO_LICENSE = 64;

/**
 * 8
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.info-modules
 *
 * @var int
 */
const INFO_MODULES = 8;

/**
 * 32
 *
 * @link http://www.php.net/manual/en/info.constants.php#constant.info-variables
 *
 * @var int
 */
const INFO_VARIABLES = 32;

/**
 * @var int
 */
const INI_ALL = 7;

/**
 * @var int
 */
const INI_PERDIR = 2;

/**
 * Normal INI scanner mode (since PHP 5.3).
 *
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.ini-scanner-normal
 *
 * @var int
 */
const INI_SCANNER_NORMAL = 0;

/**
 * Raw INI scanner mode (since PHP 5.3).
 *
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.ini-scanner-raw
 *
 * @var int
 */
const INI_SCANNER_RAW = 1;

/**
 * Typed INI scanner mode (since PHP 5.6.1).
 *
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.ini-scanner-typed
 *
 * @var int
 */
const INI_SCANNER_TYPED = 2;

/**
 * @var int
 */
const INI_SYSTEM = 4;

/**
 * @var int
 */
const INI_USER = 1;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.lc-all
 *
 * @var int
 */
const LC_ALL = 6;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.lc-collate
 *
 * @var int
 */
const LC_COLLATE = 3;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.lc-ctype
 *
 * @var int
 */
const LC_CTYPE = 0;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @link http://www.php.net/manual/en/string.constants.php#constant.lc-messages
 *
 * @var int
 */
const LC_MESSAGES = 5;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.lc-monetary
 *
 * @var int
 */
const LC_MONETARY = 4;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.lc-numeric
 *
 * @var int
 */
const LC_NUMERIC = 1;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.lc-time
 *
 * @var int
 */
const LC_TIME = 2;

/**
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.lock-ex
 *
 * @var int
 */
const LOCK_EX = 2;

/**
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.lock-nb
 *
 * @var int
 */
const LOCK_NB = 4;

/**
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.lock-sh
 *
 * @var int
 */
const LOCK_SH = 1;

/**
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.lock-un
 *
 * @var int
 */
const LOCK_UN = 3;

/**
 * action must be taken immediately
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-alert
 *
 * @var int
 */
const LOG_ALERT = 1;

/**
 * security/authorization messages (use <code>LOG_AUTHPRIV</code> instead
 * in systems where that constant is defined)
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-auth
 *
 * @var int
 */
const LOG_AUTH = 32;

/**
 * security/authorization messages (private)
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-auth
 *
 * @var int
 */
const LOG_AUTHPRIV = 80;

/**
 * if there is an error while sending data to the system logger,
 * write directly to the system console
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-cons
 *
 * @var int
 */
const LOG_CONS = 2;

/**
 * critical conditions
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-crit
 *
 * @var int
 */
const LOG_CRIT = 2;

/**
 * clock daemon (cron and at)
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-cron
 *
 * @var int
 */
const LOG_CRON = 72;

/**
 * other system daemons
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-daemon
 *
 * @var int
 */
const LOG_DAEMON = 24;

/**
 * debug-level message
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-debug
 *
 * @var int
 */
const LOG_DEBUG = 7;

/**
 * system is unusable
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-emerg
 *
 * @var int
 */
const LOG_EMERG = 0;

/**
 * error conditions
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-err
 *
 * @var int
 */
const LOG_ERR = 3;

/**
 * informational message
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-info
 *
 * @var int
 */
const LOG_INFO = 6;

/**
 * kernel messages
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-kern
 *
 * @var int
 */
const LOG_KERN = 0;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const LOG_LOCAL0 = 128;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const LOG_LOCAL1 = 136;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const LOG_LOCAL2 = 144;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const LOG_LOCAL3 = 152;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const LOG_LOCAL4 = 160;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const LOG_LOCAL5 = 168;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const LOG_LOCAL6 = 176;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const LOG_LOCAL7 = 184;

/**
 * line printer subsystem
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-lpr
 *
 * @var int
 */
const LOG_LPR = 48;

/**
 * mail subsystem
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-mail
 *
 * @var int
 */
const LOG_MAIL = 16;

/**
 * open the connection to the logger immediately
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-ndelay
 *
 * @var int
 */
const LOG_NDELAY = 8;

/**
 * USENET news subsystem
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-news
 *
 * @var int
 */
const LOG_NEWS = 56;

/**
 * normal, but significant, condition
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-notice
 *
 * @var int
 */
const LOG_NOTICE = 5;

/**
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-nowait
 *
 * @var int
 */
const LOG_NOWAIT = 16;

/**
 * (default) delay opening the connection until the first
 * message is logged
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-odelay
 *
 * @var int
 */
const LOG_ODELAY = 4;

/**
 * print log message also to standard error
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-perror
 *
 * @var int
 */
const LOG_PERROR = 32;

/**
 * include PID with each message
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-pid
 *
 * @var int
 */
const LOG_PID = 1;

/**
 * messages generated internally by syslogd
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-syslog
 *
 * @var int
 */
const LOG_SYSLOG = 40;

/**
 * generic user-level messages
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-user
 *
 * @var int
 */
const LOG_USER = 8;

/**
 * UUCP subsystem
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-uucp
 *
 * @var int
 */
const LOG_UUCP = 64;

/**
 * warning conditions
 *
 * @link http://www.php.net/manual/en/network.constants.php#constant.log-warning
 *
 * @var int
 */
const LOG_WARNING = 4;

/**
 * 0.31830988618379067154
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.m-1-pi
 *
 * @var float
 */
const M_1_PI = 0.31830988618379;

/**
 * 0.63661977236758134308
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.m-2-pi
 *
 * @var float
 */
const M_2_PI = 0.63661977236758;

/**
 * 1.12837916709551257390
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.m-2-sqrtpi
 *
 * @var float
 */
const M_2_SQRTPI = 1.1283791670955;

/**
 * 2.7182818284590452354
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.m-e
 *
 * @var float
 */
const M_E = 2.718281828459;

/**
 * 0.57721566490153286061
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.m-euler
 *
 * @var float
 */
const M_EULER = 0.57721566490153;

/**
 * 2.30258509299404568402
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.m-ln10
 *
 * @var float
 */
const M_LN10 = 2.302585092994;

/**
 * 0.69314718055994530942
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.m-ln2
 *
 * @var float
 */
const M_LN2 = 0.69314718055995;

/**
 * 1.14472988584940017414
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.m-lnpi
 *
 * @var float
 */
const M_LNPI = 1.1447298858494;

/**
 * 0.43429448190325182765
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.m-log10e
 *
 * @var float
 */
const M_LOG10E = 0.43429448190325;

/**
 * 1.4426950408889634074
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.m-log2e
 *
 * @var float
 */
const M_LOG2E = 1.442695040889;

/**
 * 3.14159265358979323846
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.m-pi
 *
 * @var float
 */
const M_PI = 3.1415926535898;

/**
 * 1.57079632679489661923
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.m-pi-2
 *
 * @var float
 */
const M_PI_2 = 1.5707963267949;

/**
 * 0.78539816339744830962
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.m-pi-4
 *
 * @var float
 */
const M_PI_4 = 0.78539816339745;

/**
 * 0.70710678118654752440
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.m-sqrt1-2
 *
 * @var float
 */
const M_SQRT1_2 = 0.70710678118655;

/**
 * 1.41421356237309504880
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.m-sqrt2
 *
 * @var float
 */
const M_SQRT2 = 1.4142135623731;

/**
 * 1.73205080756887729352
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.m-sqrt3
 *
 * @var float
 */
const M_SQRT3 = 1.7320508075689;

/**
 * 1.77245385090551602729
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.m-sqrtpi
 *
 * @var float
 */
const M_SQRTPI = 1.7724538509055;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const MON_1 = 131098;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const MON_10 = 131107;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const MON_11 = 131108;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const MON_12 = 131109;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const MON_2 = 131099;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const MON_3 = 131100;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const MON_4 = 131101;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const MON_5 = 131102;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const MON_6 = 131103;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const MON_7 = 131104;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const MON_8 = 131105;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const MON_9 = 131106;

/**
 * NAN (as a float)
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.nan
 *
 * @var float
 */
const NAN = NAN;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const NOEXPR = 327681;

/**
 * <code>PASSWORD_BCRYPT</code> is used to create new password
 * hashes using the <code>CRYPT_BLOWFISH</code> algorithm.
 * This will always result in a hash using the "$2y$" crypt format,
 * which is always 60 characters wide.
 * Supported Options:
 * <ul>
 * <code>salt</code> (<code>string</code>) - to manually provide a salt to use when hashing the password.
 * Note that this will override and prevent a salt from being automatically generated.
 * If omitted, a random salt will be generated by <code>password_hash</code> for
 * each password hashed. This is the intended mode of operation
 * and as of PHP 7.0.0 the salt option has been deprecated.
 * <code>cost</code> (<code>integer</code>) - which denotes the algorithmic cost that
 * should be used. Examples of these values can be found on the <code>crypt</code>
 * page.
 * If omitted, a default value of <code>10</code> will be used. This is a good
 * baseline cost, but you may want to consider increasing it depending on your hardware.
 * </ul>
 *
 * @link http://www.php.net/manual/en/password.constants.php#constant.password-bcrypt
 *
 * @var int
 */
const PASSWORD_BCRYPT = 1;

/**
 * @var int
 */
const PASSWORD_BCRYPT_DEFAULT_COST = 10;

/**
 * The default algorithm to use for hashing if no algorithm is provided.
 * This may change in newer PHP releases when newer, stronger hashing
 * algorithms are supported.
 * It is worth noting that over time this constant can (and likely will)
 * change. Therefore you should be aware that the length of the resulting
 * hash can change. Therefore, if you use <code>PASSWORD_DEFAULT</code>
 * you should store the resulting hash in a way that can store more than 60
 * characters (255 is the recomended width).
 * Values for this constant:
 * <ul>
 * PHP 5.5.0 - <code>PASSWORD_BCRYPT</code>
 * </ul>
 *
 * @link http://www.php.net/manual/en/password.constants.php#constant.password-default
 *
 * @var int
 */
const PASSWORD_DEFAULT = 1;

/**
 * Semicolon on Windows, colon otherwise.
 *
 * @link http://www.php.net/manual/en/dir.constants.php#constant.path-separator
 *
 * @var string
 */
const PATH_SEPARATOR = ':';

/**
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.pathinfo-basename
 *
 * @var int
 */
const PATHINFO_BASENAME = 2;

/**
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.pathinfo-dirname
 *
 * @var int
 */
const PATHINFO_DIRNAME = 1;

/**
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.pathinfo-extension
 *
 * @var int
 */
const PATHINFO_EXTENSION = 4;

/**
 * Since PHP 5.2.0.
 *
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.pathinfo-filename
 *
 * @var int
 */
const PATHINFO_FILENAME = 8;

/**
 * Encoding is performed per
 * RFC 1738 and the
 * <code>application/x-www-form-urlencoded</code> media type, which
 * implies that spaces are encoded as plus (<code>+</code>) signs.
 *
 * @link http://www.php.net/manual/en/url.constants.php#constant.php-query-rfc1738
 *
 * @var int
 */
const PHP_QUERY_RFC1738 = 1;

/**
 * Encoding is performed according to RFC 3986,
 * and spaces will be percent encoded (<code>%20</code>).
 *
 * @link http://www.php.net/manual/en/url.constants.php#constant.php-query-rfc3986
 *
 * @var int
 */
const PHP_QUERY_RFC3986 = 2;

/**
 * 2
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.php-round-half-down
 *
 * @var int
 */
const PHP_ROUND_HALF_DOWN = 2;

/**
 * 3
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.php-round-half-even
 *
 * @var int
 */
const PHP_ROUND_HALF_EVEN = 3;

/**
 * 4
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.php-round-half-odd
 *
 * @var int
 */
const PHP_ROUND_HALF_ODD = 4;

/**
 * 1
 *
 * @link http://www.php.net/manual/en/math.constants.php#constant.php-round-half-up
 *
 * @var int
 */
const PHP_ROUND_HALF_UP = 1;

/**
 * Outputs the fragment (string after the hashmark #) of the URL parsed.
 *
 * @link http://www.php.net/manual/en/url.constants.php#constant.php-url-fragment
 *
 * @var int
 */
const PHP_URL_FRAGMENT = 7;

/**
 * Outputs the hostname of the URL parsed.
 *
 * @link http://www.php.net/manual/en/url.constants.php#constant.php-url-host
 *
 * @var int
 */
const PHP_URL_HOST = 1;

/**
 * Outputs the password of the URL parsed.
 *
 * @link http://www.php.net/manual/en/url.constants.php#constant.php-url-pass
 *
 * @var int
 */
const PHP_URL_PASS = 4;

/**
 * Outputs the path of the URL parsed.
 *
 * @link http://www.php.net/manual/en/url.constants.php#constant.php-url-path
 *
 * @var int
 */
const PHP_URL_PATH = 5;

/**
 * Outputs the port of the URL parsed.
 *
 * @link http://www.php.net/manual/en/url.constants.php#constant.php-url-port
 *
 * @var int
 */
const PHP_URL_PORT = 2;

/**
 * Outputs the query string of the URL parsed.
 *
 * @link http://www.php.net/manual/en/url.constants.php#constant.php-url-query
 *
 * @var int
 */
const PHP_URL_QUERY = 6;

/**
 * @link http://www.php.net/manual/en/url.constants.php#constant.php-url-scheme
 *
 * @var int
 */
const PHP_URL_SCHEME = 0;

/**
 * Outputs the user of the URL parsed.
 *
 * @link http://www.php.net/manual/en/url.constants.php#constant.php-url-user
 *
 * @var int
 */
const PHP_URL_USER = 3;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const PM_STR = 131111;

/**
 * <code>Return Code</code> indicating that the
 * userspace filter encountered an unrecoverable error
 * (i.e. Invalid data received).
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.psfs-err-fatal
 *
 * @var int
 */
const PSFS_ERR_FATAL = 0;

/**
 * <code>Return Code</code> indicating that the
 * userspace filter did not return buckets in <code>$out</code>
 * (i.e. No data available).
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.psfs-feed-me
 *
 * @var int
 */
const PSFS_FEED_ME = 1;

/**
 * Final flush prior to closing.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.psfs-flag-flush-close
 *
 * @var int
 */
const PSFS_FLAG_FLUSH_CLOSE = 2;

/**
 * An incremental flush.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.psfs-flag-flush-inc
 *
 * @var int
 */
const PSFS_FLAG_FLUSH_INC = 1;

/**
 * Regular read/write.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.psfs-flag-normal
 *
 * @var int
 */
const PSFS_FLAG_NORMAL = 0;

/**
 * <code>Return Code</code> indicating that the
 * userspace filter returned buckets in <code>$out</code>.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.psfs-pass-on
 *
 * @var int
 */
const PSFS_PASS_ON = 2;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const RADIXCHAR = 65536;

/**
 * Available since PHP 5.4.0.
 *
 * @link http://www.php.net/manual/en/dir.constants.php#constant.scandir-sort-ascending
 *
 * @var int
 */
const SCANDIR_SORT_ASCENDING = 0;

/**
 * Available since PHP 5.4.0.
 *
 * @link http://www.php.net/manual/en/dir.constants.php#constant.scandir-sort-descending
 *
 * @var int
 */
const SCANDIR_SORT_DESCENDING = 1;

/**
 * Available since PHP 5.4.0.
 *
 * @link http://www.php.net/manual/en/dir.constants.php#constant.scandir-sort-none
 *
 * @var int
 */
const SCANDIR_SORT_NONE = 2;

/**
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.seek-cur
 *
 * @var int
 */
const SEEK_CUR = 1;

/**
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.seek-end
 *
 * @var int
 */
const SEEK_END = 2;

/**
 * @link http://www.php.net/manual/en/filesystem.constants.php#constant.seek-set
 *
 * @var int
 */
const SEEK_SET = 0;

/**
 * <code>SORT_ASC</code> is used with
 * <code>array_multisort</code> to sort in ascending order.
 *
 * @link http://www.php.net/manual/en/array.constants.php#constant.sort-asc
 *
 * @var int
 */
const SORT_ASC = 4;

/**
 * <code>SORT_DESC</code> is used with
 * <code>array_multisort</code> to sort in descending order.
 *
 * @link http://www.php.net/manual/en/array.constants.php#constant.sort-desc
 *
 * @var int
 */
const SORT_DESC = 3;

/**
 * <code>SORT_FLAG_CASE</code> can be combined
 * (bitwise OR) with
 * <code>SORT_STRING</code> or
 * <code>SORT_NATURAL</code> to sort strings case-insensitively. Added in PHP 5.4.0.
 *
 * @link http://www.php.net/manual/en/array.constants.php#constant.sort-flag-case
 *
 * @var int
 */
const SORT_FLAG_CASE = 8;

/**
 * <code>SORT_LOCALE_STRING</code> is used to compare items as
 * strings, based on the current locale. Added in PHP 5.0.2.
 *
 * @link http://www.php.net/manual/en/array.constants.php#constant.sort-locale-string
 *
 * @var int
 */
const SORT_LOCALE_STRING = 5;

/**
 * <code>SORT_NATURAL</code> is used to compare items as
 * strings using "natural ordering" like <code>natsort</code>. Added in PHP 5.4.0.
 *
 * @link http://www.php.net/manual/en/array.constants.php#constant.sort-natural
 *
 * @var int
 */
const SORT_NATURAL = 6;

/**
 * <code>SORT_NUMERIC</code> is used to compare items numerically.
 *
 * @link http://www.php.net/manual/en/array.constants.php#constant.sort-numeric
 *
 * @var int
 */
const SORT_NUMERIC = 1;

/**
 * <code>SORT_REGULAR</code> is used to compare items normally.
 *
 * @link http://www.php.net/manual/en/array.constants.php#constant.sort-regular
 *
 * @var int
 */
const SORT_REGULAR = 0;

/**
 * <code>SORT_STRING</code> is used to compare items as strings.
 *
 * @link http://www.php.net/manual/en/array.constants.php#constant.sort-string
 *
 * @var int
 */
const SORT_STRING = 2;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.str-pad-both
 *
 * @var int
 */
const STR_PAD_BOTH = 2;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.str-pad-left
 *
 * @var int
 */
const STR_PAD_LEFT = 0;

/**
 * @link http://www.php.net/manual/en/string.constants.php#constant.str-pad-right
 *
 * @var int
 */
const STR_PAD_RIGHT = 1;

/**
 * @var int
 */
const STREAM_BUFFER_FULL = 2;

/**
 * @var int
 */
const STREAM_BUFFER_LINE = 1;

/**
 * @var int
 */
const STREAM_BUFFER_NONE = 0;

/**
 * Stream casting, when <code>stream_cast</code> is called
 * otherwise (see above).
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-cast-as-stream
 *
 * @var int
 */
const STREAM_CAST_AS_STREAM = 0;

/**
 * Stream casting, for when <code>stream_select</code> is
 * calling <code>stream_cast</code>.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-cast-for-select
 *
 * @var int
 */
const STREAM_CAST_FOR_SELECT = 3;

/**
 * Open client socket asynchronously. This option must be used
 * together with the <code>STREAM_CLIENT_CONNECT</code> flag.
 * Used with <code>stream_socket_client</code>.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-client-async-connect
 *
 * @var int
 */
const STREAM_CLIENT_ASYNC_CONNECT = 2;

/**
 * Open client socket connection. Client sockets should always
 * include this flag. Used with <code>stream_socket_client</code>.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-client-async-connect
 *
 * @var int
 */
const STREAM_CLIENT_CONNECT = 4;

/**
 * Client socket opened with <code>stream_socket_client</code>
 * should remain persistent between page loads.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-client-persistent
 *
 * @var int
 */
const STREAM_CLIENT_PERSISTENT = 1;

/**
 * @var int
 */
const STREAM_CRYPTO_METHOD_ANY_CLIENT = 63;

/**
 * @var int
 */
const STREAM_CRYPTO_METHOD_ANY_SERVER = 62;

/**
 * @var int
 */
const STREAM_CRYPTO_METHOD_SSLv23_CLIENT = 57;

/**
 * @var int
 */
const STREAM_CRYPTO_METHOD_SSLv23_SERVER = 56;

/**
 * @var int
 */
const STREAM_CRYPTO_METHOD_SSLv2_CLIENT = 3;

/**
 * @var int
 */
const STREAM_CRYPTO_METHOD_SSLv2_SERVER = 2;

/**
 * @var int
 */
const STREAM_CRYPTO_METHOD_SSLv3_CLIENT = 5;

/**
 * @var int
 */
const STREAM_CRYPTO_METHOD_SSLv3_SERVER = 4;

/**
 * @var int
 */
const STREAM_CRYPTO_METHOD_TLS_CLIENT = 9;

/**
 * @var int
 */
const STREAM_CRYPTO_METHOD_TLS_SERVER = 8;

/**
 * @var int
 */
const STREAM_CRYPTO_METHOD_TLSv1_0_CLIENT = 9;

/**
 * @var int
 */
const STREAM_CRYPTO_METHOD_TLSv1_0_SERVER = 8;

/**
 * @var int
 */
const STREAM_CRYPTO_METHOD_TLSv1_1_CLIENT = 17;

/**
 * @var int
 */
const STREAM_CRYPTO_METHOD_TLSv1_1_SERVER = 16;

/**
 * @var int
 */
const STREAM_CRYPTO_METHOD_TLSv1_2_CLIENT = 33;

/**
 * @var int
 */
const STREAM_CRYPTO_METHOD_TLSv1_2_SERVER = 32;

/**
 * This constant is equivalent to
 * <code>STREAM_FILTER_READ | STREAM_FILTER_WRITE</code>
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-filter-all
 *
 * @var int
 */
const STREAM_FILTER_ALL = 3;

/**
 * Used with <code>stream_filter_append</code> and
 * <code>stream_filter_prepend</code> to indicate
 * that the specified filter should only be applied when
 * <code>reading</code>
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-filter-read
 *
 * @var int
 */
const STREAM_FILTER_READ = 1;

/**
 * Used with <code>stream_filter_append</code> and
 * <code>stream_filter_prepend</code> to indicate
 * that the specified filter should only be applied when
 * <code>writing</code>
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-filter-write
 *
 * @var int
 */
const STREAM_FILTER_WRITE = 2;

/**
 * @var int
 */
const STREAM_IGNORE_URL = 2;

/**
 * Provides a ICMP socket.
 *
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-ipproto-icmp
 *
 * @var int
 */
const STREAM_IPPROTO_ICMP = 1;

/**
 * Provides a IP socket.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-ipproto-ip
 *
 * @var int
 */
const STREAM_IPPROTO_IP = 0;

/**
 * Provides a RAW socket.
 *
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-ipproto-raw
 *
 * @var int
 */
const STREAM_IPPROTO_RAW = 255;

/**
 * Provides a TCP socket.
 *
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-ipproto-tcp
 *
 * @var int
 */
const STREAM_IPPROTO_TCP = 6;

/**
 * Provides a UDP socket.
 *
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-ipproto-udp
 *
 * @var int
 */
const STREAM_IPPROTO_UDP = 17;

/**
 * @var int
 */
const STREAM_IS_URL = 1;

/**
 * Used with <code>stream_metadata</code>, to specify <code>chmod</code> call.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-meta-access
 *
 * @var int
 */
const STREAM_META_ACCESS = 6;

/**
 * Used with <code>stream_metadata</code>, to specify <code>chgrp</code> call.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-meta-group
 *
 * @var int
 */
const STREAM_META_GROUP = 5;

/**
 * Used with <code>stream_metadata</code>, to specify <code>chgrp</code> call.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-meta-group-name
 *
 * @var int
 */
const STREAM_META_GROUP_NAME = 4;

/**
 * Used with <code>stream_metadata</code>, to specify <code>chown</code> call.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-meta-owner
 *
 * @var int
 */
const STREAM_META_OWNER = 3;

/**
 * Used with <code>stream_metadata</code>, to specify <code>chown</code> call.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-meta-owner-name
 *
 * @var int
 */
const STREAM_META_OWNER_NAME = 2;

/**
 * Used with <code>stream_metadata</code>, to specify <code>touch</code> call.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-meta-touch
 *
 * @var int
 */
const STREAM_META_TOUCH = 1;

/**
 * @var int
 */
const STREAM_MKDIR_RECURSIVE = 1;

/**
 * @var int
 */
const STREAM_MUST_SEEK = 16;

/**
 * Additional authorization is required to access the specified resource.
 * Typical issued with <code>severity</code> level of
 * <code>STREAM_NOTIFY_SEVERITY_ERR</code>.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-notify-auth-required
 *
 * @var int
 */
const STREAM_NOTIFY_AUTH_REQUIRED = 3;

/**
 * Authorization has been completed (with or without success).
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-notify-auth-result
 *
 * @var int
 */
const STREAM_NOTIFY_AUTH_RESULT = 10;

/**
 * There is no more data available on the stream.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-notify-completed
 *
 * @var int
 */
const STREAM_NOTIFY_COMPLETED = 8;

/**
 * A connection with an external resource has been established.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-notify-connect
 *
 * @var int
 */
const STREAM_NOTIFY_CONNECT = 2;

/**
 * A generic error occurred on the stream, consult
 * <code>message</code> and <code>message_code</code>
 * for details.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-notify-failure
 *
 * @var int
 */
const STREAM_NOTIFY_FAILURE = 9;

/**
 * The <code>size</code> of the resource has been discovered.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-notify-file-size-is
 *
 * @var int
 */
const STREAM_NOTIFY_FILE_SIZE_IS = 5;

/**
 * The <code>mime-type</code> of resource has been identified,
 * refer to <code>message</code> for a description of the
 * discovered type.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-notify-mime-type-is
 *
 * @var int
 */
const STREAM_NOTIFY_MIME_TYPE_IS = 4;

/**
 * Indicates current progress of the stream transfer in
 * <code>bytes_transferred</code> and possibly
 * <code>bytes_max</code> as well.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-notify-progress
 *
 * @var int
 */
const STREAM_NOTIFY_PROGRESS = 7;

/**
 * The external resource has redirected the stream to an alternate
 * location. Refer to <code>message</code>.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-notify-redirected
 *
 * @var int
 */
const STREAM_NOTIFY_REDIRECTED = 6;

/**
 * A remote address required for this stream has been resolved, or the resolution
 * failed. See <code>severity</code> for an indication of which happened.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-notify-resolve
 *
 * @var int
 */
const STREAM_NOTIFY_RESOLVE = 1;

/**
 * A critical error occurred. Processing cannot continue.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-notify-auth-required
 *
 * @var int
 */
const STREAM_NOTIFY_SEVERITY_ERR = 2;

/**
 * Normal, non-error related, notification.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-notify-severity-info
 *
 * @var int
 */
const STREAM_NOTIFY_SEVERITY_INFO = 0;

/**
 * Non critical error condition. Processing may continue.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-notify-severity-warn
 *
 * @var int
 */
const STREAM_NOTIFY_SEVERITY_WARN = 1;

/**
 * @var int
 */
const STREAM_OOB = 1;

/**
 * @var int
 */
const STREAM_OPTION_BLOCKING = 1;

/**
 * @var int
 */
const STREAM_OPTION_READ_BUFFER = 2;

/**
 * @var int
 */
const STREAM_OPTION_READ_TIMEOUT = 4;

/**
 * @var int
 */
const STREAM_OPTION_WRITE_BUFFER = 3;

/**
 * @var int
 */
const STREAM_PEEK = 2;

/**
 * Internet Protocol Version 4 (IPv4).
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-pf-inet
 *
 * @var int
 */
const STREAM_PF_INET = 2;

/**
 * Internet Protocol Version 6 (IPv6).
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-pf-inet6
 *
 * @var int
 */
const STREAM_PF_INET6 = 10;

/**
 * Unix system internal protocols.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-pf-unix
 *
 * @var int
 */
const STREAM_PF_UNIX = 1;

/**
 * <code>Flag</code> indicating if the <code>wrapper</code>
 * is responsible for raising errors using <code>trigger_error</code>
 * during opening of the stream. If this flag is not set, you
 * should not raise any errors.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-report-errors
 *
 * @var int
 */
const STREAM_REPORT_ERRORS = 8;

/**
 * Tells a stream created with <code>stream_socket_server</code>
 * to bind to the specified target. Server sockets should always include this flag.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-server-bind
 *
 * @var int
 */
const STREAM_SERVER_BIND = 4;

/**
 * Tells a stream created with <code>stream_socket_server</code>
 * and bound using the <code>STREAM_SERVER_BIND</code> flag to start
 * listening on the socket. Connection-orientated transports (such as TCP)
 * must use this flag, otherwise the server socket will not be enabled.
 * Using this flag for connect-less transports (such as UDP) is an error.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-server-listen
 *
 * @var int
 */
const STREAM_SERVER_LISTEN = 8;

/**
 * Used with <code>stream_socket_shutdown</code> to disable
 * further receptions. Added in PHP 5.2.1.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-shut-rd
 *
 * @var int
 */
const STREAM_SHUT_RD = 0;

/**
 * Used with <code>stream_socket_shutdown</code> to disable
 * further receptions and transmissions. Added in PHP 5.2.1.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-shut-rdwr
 *
 * @var int
 */
const STREAM_SHUT_RDWR = 2;

/**
 * Used with <code>stream_socket_shutdown</code> to disable
 * further transmissions. Added in PHP 5.2.1.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-shut-wr
 *
 * @var int
 */
const STREAM_SHUT_WR = 1;

/**
 * Provides datagrams, which are connectionless messages (UDP, for
 * example).
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-sock-dgram
 *
 * @var int
 */
const STREAM_SOCK_DGRAM = 2;

/**
 * Provides a raw socket, which provides access to internal network
 * protocols and interfaces. Usually this type of socket is just available
 * to the root user.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-sock-raw
 *
 * @var int
 */
const STREAM_SOCK_RAW = 3;

/**
 * Provides a RDM (Reliably-delivered messages) socket.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-sock-rdm
 *
 * @var int
 */
const STREAM_SOCK_RDM = 4;

/**
 * Provides a sequenced packet stream socket.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-sock-seqpacket
 *
 * @var int
 */
const STREAM_SOCK_SEQPACKET = 5;

/**
 * Provides sequenced, two-way byte streams with a transmission mechanism
 * for out-of-band data (TCP, for example).
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-sock-stream
 *
 * @var int
 */
const STREAM_SOCK_STREAM = 1;

/**
 * @var int
 */
const STREAM_URL_STAT_LINK = 1;

/**
 * @var int
 */
const STREAM_URL_STAT_QUIET = 2;

/**
 * <code>Flag</code> indicating if the <code>stream</code>
 * used the include path.
 *
 * @link http://www.php.net/manual/en/stream.constants.php#constant.stream-use-path
 *
 * @var int
 */
const STREAM_USE_PATH = 1;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const T_FMT = 131114;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const T_FMT_AMPM = 131115;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const THOUSEP = 65537;

/**
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @var int
 */
const YESEXPR = 327680;

class __PHP_Incomplete_Class
{
}

/**
 * Instances of <code>Directory</code> are created by calling the
 * <code>dir</code> function, not by the new operator.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/class.directory.php
 */
class Directory
{
    /**
     * Close directory handle
     *
     * @param resource $dir_handle
     *
     * @return void
     *
     * @since PHP 4, PHP 5, PHP 7
     *
     * @link http://www.php.net/manual/en/directory.close.php
     */
    public function close($dir_handle = null)
    {
    }

    /**
     * Read entry from directory handle
     *
     * @param resource $dir_handle
     *
     * @return string
     *
     * @since PHP 4, PHP 5, PHP 7
     *
     * @link http://www.php.net/manual/en/directory.read.php
     */
    public function read($dir_handle = null)
    {
    }

    /**
     * Rewind directory handle
     *
     * @param resource $dir_handle
     *
     * @return void
     *
     * @since PHP 4, PHP 5, PHP 7
     *
     * @link http://www.php.net/manual/en/directory.rewind.php
     */
    public function rewind($dir_handle = null)
    {
    }
}

/**
 * Children of this class are passed to
 * <code>stream_filter_register</code>.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/class.php-user-filter.php
 */
class php_user_filter
{
    /**
     * @var string
     */
    public $filtername = '';

    /**
     * @var string
     */
    public $params = '';

    /**
     * Called when applying the filter
     *
     * @param resource $in <code>in</code> is a resource pointing to a <code>bucket brigade</code>
     * which contains one or more <code>bucket</code> objects containing data to be filtered.
     * @param resource $out <code>out</code> is a resource pointing to a second <code>bucket brigade</code>
     * into which your modified buckets should be placed.
     * @param int $consumed <code>consumed</code>, which must <code>always</code>
     * be declared by reference, should be incremented by the length of the data
     * which your filter reads in and alters. In most cases this means you will
     * increment <code>consumed</code> by <code>$bucket-&gt;datalen</code>
     * for each <code>$bucket</code>.
     * @param bool $closing If the stream is in the process of closing
     * (and therefore this is the last pass through the filterchain),
     * the <code>closing</code> parameter will be set to <code>TRUE</code>.
     *
     * @return int The <code>filter</code> method must return one of
     * three values upon completion.
     * <table>
     * <thead>
     * <tr>
     * <td>Return Value</td><td>Meaning</td>
     * </tr>
     * </thead>
     * <tbody>
     * <tr>
     * <td><code>PSFS_PASS_ON</code></td><td>
     * Filter processed successfully with data available in the
     * <code>out</code> <code>bucket brigade</code>.
     * </td>
     * </tr>
     * <tr>
     * <td><code>PSFS_FEED_ME</code></td><td>
     * Filter processed successfully, however no data was available to
     * return. More data is required from the stream or prior filter.
     * </td>
     * </tr>
     * <tr>
     * <td><code>PSFS_ERR_FATAL</code> (default)</td><td>
     * The filter experienced an unrecoverable error and cannot continue.
     * </td>
     * </tr>
     * </tbody>
     * </table>
     *
     * @since PHP 5, PHP 7
     *
     * @link http://www.php.net/manual/en/php-user-filter.filter.php
     */
    public function filter($in, $out, &$consumed, $closing)
    {
    }

    /**
     * Called when closing the filter
     *
     * @return void Return value is ignored.
     *
     * @since PHP 5, PHP 7
     *
     * @link http://www.php.net/manual/en/php-user-filter.onclose.php
     */
    public function onClose()
    {
    }

    /**
     * Called when creating the filter
     *
     * @return bool Your implementation of
     * this method should return <code>FALSE</code> on failure, or <code>TRUE</code> on success.
     *
     * @since PHP 5, PHP 7
     *
     * @link http://www.php.net/manual/en/php-user-filter.oncreate.php
     */
    public function onCreate()
    {
    }
}

/**
 * Absolute value
 *
 * @param mixed $number The numeric value to process
 *
 * @return float The absolute value of <code>number</code>. If the
 * argument <code>number</code> is
 * of type <code>float</code>, the return type is also <code>float</code>,
 * otherwise it is <code>integer</code> (as <code>float</code> usually has a
 * bigger value range than <code>integer</code>).
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.abs.php
 */
function abs($number)
{
}

/**
 * Arc cosine
 *
 * @param float $arg The argument to process
 *
 * @return float The arc cosine of <code>arg</code> in radians.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.acos.php
 */
function acos($arg)
{
}

/**
 * Inverse hyperbolic cosine
 *
 * @param float $arg The value to process
 *
 * @return float The inverse hyperbolic cosine of <code>arg</code>
 *
 * @since PHP 4 >= 4.1.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.acosh.php
 */
function acosh($arg)
{
}

/**
 * Quote string with slashes in a C style
 *
 * @param string $str The string to be escaped.
 * @param string $charlist A list of characters to be escaped. If
 * <code>charlist</code> contains characters
 * <code>\n</code>, <code>\r</code> etc., they are
 * converted in C-like style, while other non-alphanumeric characters
 * with ASCII codes lower than 32 and higher than 126 converted to
 * octal representation.
 *
 * @return string Returns the escaped string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.addcslashes.php
 */
function addcslashes($str, $charlist)
{
}

/**
 * Quote string with slashes
 *
 * @param string $str The string to be escaped.
 *
 * @return string Returns the escaped string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.addslashes.php
 */
function addslashes($str)
{
}

/**
 * Changes the case of all keys in an array
 *
 * @param array $array The array to work on
 * @param int|null $case Either <code>CASE_UPPER</code> or
 * <code>CASE_LOWER</code> (default)
 *
 * @return array Returns an array with its keys lower or uppercased, or <code>FALSE</code> if
 * <code>array</code> is not an array.
 *
 * @since PHP 4 >= 4.2.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-change-key-case.php
 */
function array_change_key_case(array $array, $case = CASE_LOWER)
{
}

/**
 * Split an array into chunks
 *
 * @param array $array The array to work on
 * @param int $size The size of each chunk
 * @param bool|null $preserve_keys When set to <code>TRUE</code> keys will be preserved.
 * Default is <code>FALSE</code> which will reindex the chunk numerically
 *
 * @return array Returns a multidimensional numerically indexed array, starting with zero,
 * with each dimension containing <code>size</code> elements.
 *
 * @since PHP 4 >= 4.2.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-chunk.php
 */
function array_chunk(array $array, $size, $preserve_keys = false)
{
}

/**
 * Return the values from a single column in the input array
 *
 * @param array $input A multi-dimensional array or an array of objects from which to pull a
 * column of values from. If an array of objects is provided, then public
 * properties can be directly pulled. In order for protected or private
 * properties to be pulled, the class must implement both the
 * <code>__get</code> and <code>__isset</code> magic
 * methods.
 * @param mixed $column_key The column of values to return. This value may be an integer key of the
 * column you wish to retrieve, or it may be a string key name for an
 * associative array or property name. It may also be <code>NULL</code> to return
 * complete arrays or objects (this is useful together with
 * <code>index_key</code> to reindex the array).
 * @param mixed|null $index_key The column to use as the index/keys for the returned array. This value
 * may be the integer key of the column, or it may be the string key name.
 *
 * @return array Returns an array of values representing a single column from the input array.
 *
 * @since PHP 5 >= 5.5.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-column.php
 */
function array_column(array $input, $column_key, $index_key = null)
{
}

/**
 * Creates an array by using one array for keys and another for its values
 *
 * @param array $keys Array of keys to be used. Illegal values for key will be
 * converted to <code>string</code>.
 * @param array $values <code>Array</code> of values to be used
 *
 * @return array Returns the combined <code>array</code>, <code>FALSE</code> if the number of elements
 * for each array isn't equal.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-combine.php
 */
function array_combine(array $keys, array $values)
{
}

/**
 * Counts all the values of an array
 *
 * @param array $array The array of values to count
 *
 * @return array Returns an associative array of values from <code>array</code> as
 * keys and their count as value.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-count-values.php
 */
function array_count_values(array $array)
{
}

/**
 * Computes the difference of arrays
 *
 * @param array $array1 The array to compare from
 * @param array $array2 An array to compare against
 *
 * @return array Returns an <code>array</code> containing all the entries from
 * <code>array1</code> that are not present in any of the other arrays.
 *
 * @since PHP 4 >= 4.0.1, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-diff.php
 */
function array_diff(array $array1, array ...$array2)
{
}

/**
 * Computes the difference of arrays with additional index check
 *
 * @param array $array1 The array to compare from
 * @param array $array2 An array to compare against
 *
 * @return array Returns an <code>array</code> containing all the values from
 * <code>array1</code> that are not present in any of the other arrays.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-diff-assoc.php
 */
function array_diff_assoc(array $array1, array ...$array2)
{
}

/**
 * Computes the difference of arrays using keys for comparison
 *
 * @param array $array1 The array to compare from
 * @param array $array2 An array to compare against
 *
 * @return array Returns an <code>array</code> containing all the entries from
 * <code>array1</code> whose keys are absent from all of the
 * other arrays.
 *
 * @since PHP 5 >= 5.1.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-diff-key.php
 */
function array_diff_key(array $array1, array ...$array2)
{
}

/**
 * Computes the difference of arrays with additional index check which is performed by a user supplied callback function
 *
 * @param array $array1 The array to compare from
 * @param array $array2 An array to compare against
 * @param array $callback_data_comp_func More arrays to compare against
 *
 * @return array Returns an <code>array</code> containing all the entries from
 * <code>array1</code> that are not present in any of the other arrays.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-diff-uassoc.php
 */
function array_diff_uassoc(array $array1, array $array2, array $callback_data_comp_func)
{
}

/**
 * Computes the difference of arrays using a callback function on the keys for comparison
 *
 * @param array $array1 The array to compare from
 * @param array $array2 An array to compare against
 * @param array $callback_key_comp_func More arrays to compare against
 *
 * @return array Returns an <code>array</code> containing all the entries from
 * <code>array1</code> that are not present in any of the other arrays.
 *
 * @since PHP 5 >= 5.1.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-diff-ukey.php
 */
function array_diff_ukey(array $array1, array $array2, array $callback_key_comp_func)
{
}

/**
 * Fill an array with values
 *
 * @param int $start_index The first index of the returned array.
 * @param int $num Number of elements to insert.
 * Must be greater than or equal to zero.
 * @param mixed $value Value to use for filling
 *
 * @return array Returns the filled array
 *
 * @since PHP 4 >= 4.2.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-fill.php
 */
function array_fill($start_index, $num, $value)
{
}

/**
 * Fill an array with values, specifying keys
 *
 * @param array $keys Array of values that will be used as keys. Illegal values
 * for key will be converted to <code>string</code>.
 * @param mixed $value Value to use for filling
 *
 * @return array Returns the filled array
 *
 * @since PHP 5 >= 5.2.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-fill-keys.php
 */
function array_fill_keys(array $keys, $value)
{
}

/**
 * Filters elements of an array using a callback function
 *
 * @param array $array The array to iterate over
 * @param callable $callback The callback function to use
 * @param int|null $flag Flag determining what arguments are sent to <code>callback</code>:
 * <ul>
 * <code>ARRAY_FILTER_USE_KEY</code> - pass key as the only argument
 * to <code>callback</code> instead of the value
 * <code>ARRAY_FILTER_USE_BOTH</code> - pass both value and key as
 * arguments to <code>callback</code> instead of the value
 * </ul>
 *
 * @return array Returns the filtered array.
 *
 * @since PHP 4 >= 4.0.6, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-filter.php
 */
function array_filter(array $array, callable $callback = null, $flag = 0)
{
}

/**
 * Exchanges all keys with their associated values in an array
 *
 * @param array $array An array of key/value pairs to be flipped.
 *
 * @return array Returns the flipped array on success and <code>NULL</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-flip.php
 */
function array_flip(array $array)
{
}

/**
 * Computes the intersection of arrays
 *
 * @param array $array1 The array with master values to check.
 * @param array $array2 An array to compare values against.
 *
 * @return array Returns an array containing all of the values in
 * <code>array1</code> whose values exist in all of the parameters.
 *
 * @since PHP 4 >= 4.0.1, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-intersect.php
 */
function array_intersect(array $array1, array ...$array2)
{
}

/**
 * Computes the intersection of arrays with additional index check
 *
 * @param array $array1 The array with master values to check.
 * @param array $array2 An array to compare values against.
 *
 * @return array Returns an associative array containing all the values in
 * <code>array1</code> that are present in all of the arguments.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-intersect-assoc.php
 */
function array_intersect_assoc(array $array1, array ...$array2)
{
}

/**
 * Computes the intersection of arrays using keys for comparison
 *
 * @param array $array1 The array with master keys to check.
 * @param array $array2 An array to compare keys against.
 *
 * @return array Returns an associative array containing all the entries of
 * <code>array1</code> which have keys that are present in all
 * arguments.
 *
 * @since PHP 5 >= 5.1.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-intersect-key.php
 */
function array_intersect_key(array $array1, array ...$array2)
{
}

/**
 * Computes the intersection of arrays with additional index check, compares indexes by a callback function
 *
 * @param array $array1 Initial array for comparison of the arrays.
 * @param array $array2 First array to compare keys against.
 * @param array $callback_key_compare_func Variable list of array arguments to compare values against.
 *
 * @return array Returns the values of <code>array1</code> whose values exist
 * in all of the arguments.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-intersect-uassoc.php
 */
function array_intersect_uassoc(array $array1, array $array2, array $callback_key_compare_func)
{
}

/**
 * Computes the intersection of arrays using a callback function on the keys for comparison
 *
 * @param array $array1 Initial array for comparison of the arrays.
 * @param array $array2 First array to compare keys against.
 * @param array $callback_key_compare_func Variable list of array arguments to compare keys against.
 *
 * @return array Returns the values of <code>array1</code> whose keys exist
 * in all the arguments.
 *
 * @since PHP 5 >= 5.1.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-intersect-ukey.php
 */
function array_intersect_ukey(array $array1, array $array2, array $callback_key_compare_func)
{
}

/**
 * Checks if the given key or index exists in the array
 *
 * @param mixed $key Value to check.
 * @param array $array An array with keys to check.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4 >= 4.0.7, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-key-exists.php
 */
function array_key_exists($key, array $array)
{
}

/**
 * Return all the keys or a subset of the keys of an array
 *
 * @param array $array An array containing keys to return.
 * @param mixed|null $search_value If specified, then only keys containing these values are returned.
 * @param bool|null $strict Determines if strict comparison (===) should be used during the search.
 *
 * @return array Returns an array of all the keys in <code>array</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-keys.php
 */
function array_keys(array $array, $search_value = null, $strict = false)
{
}

/**
 * Applies the callback to the elements of the given arrays
 *
 * @param callable $callback Callback function to run for each element in each array.
 * @param array $array1 An array to run through the <code>callback</code> function.
 *
 * @return array Returns an array containing all the elements of <code>array1</code>
 * after applying the <code>callback</code> function to each one.
 *
 * @since PHP 4 >= 4.0.6, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-map.php
 */
function array_map(callable $callback, array ...$array1)
{
}

/**
 * Merge one or more arrays
 *
 * @param array $array1 Initial array to merge.
 * @param array $arrays Variable list of arrays to merge.
 *
 * @return array Returns the resulting array.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-merge.php
 */
function array_merge(array $array1, array ...$arrays)
{
}

/**
 * Merge two or more arrays recursively
 *
 * @param array $array1 Initial array to merge.
 * @param array $arrays Variable list of arrays to recursively merge.
 *
 * @return array An array of values resulted from merging the arguments together.
 *
 * @since PHP 4 >= 4.0.1, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-merge-recursive.php
 */
function array_merge_recursive(array $array1, array ...$arrays)
{
}

/**
 * Sort multiple or multi-dimensional arrays
 *
 * @param array $array1 An <code>array</code> being sorted.
 * @param mixed|null $array1_sort_order The order used to sort the previous <code>array</code> argument. Either
 * <code>SORT_ASC</code> to sort ascendingly or <code>SORT_DESC</code>
 * to sort descendingly.
 * @param mixed|null $array1_sort_flags Sort options for the previous <code>array</code> argument:
 * @param mixed $arr2 More arrays, optionally followed by sort order and flags. Only elements
 * corresponding to equivalent elements in previous arrays are compared.
 * In other words, the sort is lexicographical.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-multisort.php
 */
function array_multisort(array &$array1, &$array1_sort_order = SORT_ASC, &$array1_sort_flags = SORT_REGULAR, &...$arr2)
{
}

/**
 * Pad array to the specified length with a value
 *
 * @param array $array Initial array of values to pad.
 * @param int $size New size of the array.
 * @param mixed $value Value to pad if <code>array</code> is less than
 * <code>size</code>.
 *
 * @return array Returns a copy of the <code>array</code> padded to size specified
 * by <code>size</code> with value
 * <code>value</code>. If <code>size</code> is
 * positive then the array is padded on the right, if it's negative then
 * on the left. If the absolute value of <code>size</code> is less
 * than or equal to the length of the <code>array</code> then no
 * padding takes place.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-pad.php
 */
function array_pad(array $array, $size, $value)
{
}

/**
 * Pop the element off the end of array
 *
 * @param array $array The array to get the value from.
 *
 * @return mixed Returns the last value of <code>array</code>.
 * If <code>array</code> is empty (or is not an array),
 * <code>NULL</code> will be returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-pop.php
 */
function array_pop(array &$array)
{
}

/**
 * Calculate the product of values in an array
 *
 * @param array $array The array.
 *
 * @return float Returns the product as an integer or float.
 *
 * @since PHP 5 >= 5.1.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-product.php
 */
function array_product(array $array)
{
}

/**
 * Push one or more elements onto the end of array
 *
 * @param array $array The input array.
 * @param mixed $vars The values to push onto the end of the <code>array</code>.
 *
 * @return int Returns the new number of elements in the array.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-push.php
 */
function array_push(array &$array, ...$vars)
{
}

/**
 * Pick one or more random entries out of an array
 *
 * @param array $array The input array.
 * @param int|null $num Specifies how many entries should be picked.
 *
 * @return mixed When picking only one entry, <code>array_rand</code> returns
 * the key for a random entry. Otherwise, an array of keys for the random
 * entries is returned. This is done so that random keys can be picked
 * from the array as well as random values. Trying to pick more elements
 * than there are in the array will result in an
 * <code>E_WARNING</code> level error, and NULL will be returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-rand.php
 */
function array_rand(array $array, $num = 1)
{
}

/**
 * Iteratively reduce the array to a single value using a callback function
 *
 * @param array $array The input array.
 * @param callable $callback Holds the return value of the previous iteration; in the case of the
 * first iteration it instead holds the value of
 * <code>initial</code>.
 * @param mixed|null $initial If the optional <code>initial</code> is available, it will
 * be used at the beginning of the process, or as a final result in case
 * the array is empty.
 *
 * @return mixed Returns the resulting value.
 * If the array is empty and <code>initial</code> is not passed,
 * <code>array_reduce</code> returns <code>NULL</code>.
 *
 * @since PHP 4 >= 4.0.5, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-reduce.php
 */
function array_reduce(array $array, callable $callback, $initial = null)
{
}

/**
 * Replaces elements from passed arrays into the first array
 *
 * @param array $array1 The array in which elements are replaced.
 * @param array $array2 The array from which elements will be extracted.
 *
 * @return array Returns an <code>array</code>, or <code>NULL</code> if an error occurs.
 *
 * @since PHP 5 >= 5.3.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-replace.php
 */
function array_replace(array $array1, array ...$array2)
{
}

/**
 * Replaces elements from passed arrays into the first array recursively
 *
 * @param array $array1 The array in which elements are replaced.
 * @param array $array2 The array from which elements will be extracted.
 *
 * @return array Returns an <code>array</code>, or <code>NULL</code> if an error occurs.
 *
 * @since PHP 5 >= 5.3.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-replace-recursive.php
 */
function array_replace_recursive(array $array1, array ...$array2)
{
}

/**
 * Return an array with elements in reverse order
 *
 * @param array $array The input array.
 * @param bool|null $preserve_keys If set to <code>TRUE</code> numeric keys are preserved.
 * Non-numeric keys are not affected by this setting and will always be preserved.
 *
 * @return array Returns the reversed array.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-reverse.php
 */
function array_reverse(array $array, $preserve_keys = false)
{
}

/**
 * Searches the array for a given value and returns the first corresponding key if successful
 *
 * @param mixed $needle The searched value.
 * @param array $haystack The array.
 * @param bool|null $strict If the third parameter <code>strict</code> is set to <code>TRUE</code>
 * then the <code>array_search</code> function will search for
 * <code>identical</code> elements in the
 * <code>haystack</code>. This means it will also perform a
 * strict type comparison of the
 * <code>needle</code> in the <code>haystack</code>,
 * and objects must be the same instance.
 *
 * @return mixed Returns the key for <code>needle</code> if it is found in the
 * array, <code>FALSE</code> otherwise.
 * If <code>needle</code> is found in <code>haystack</code>
 * more than once, the first matching key is returned. To return the keys for
 * all matching values, use <code>array_keys</code> with the optional
 * <code>search_value</code> parameter instead.
 *
 * @since PHP 4 >= 4.0.5, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-search.php
 */
function array_search($needle, array $haystack, $strict = false)
{
}

/**
 * Shift an element off the beginning of array
 *
 * @param array $array The input array.
 *
 * @return mixed Returns the shifted value, or <code>NULL</code> if <code>array</code> is
 * empty or is not an array.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-shift.php
 */
function array_shift(array &$array)
{
}

/**
 * Extract a slice of the array
 *
 * @param array $array The input array.
 * @param int $offset If <code>offset</code> is non-negative, the sequence will
 * start at that offset in the <code>array</code>. If
 * <code>offset</code> is negative, the sequence will
 * start that far from the end of the <code>array</code>.
 * @param int|null $length If <code>length</code> is given and is positive, then
 * the sequence will have up to that many elements in it. If the array
 * is shorter than the <code>length</code>, then only the
 * available array elements will be present. If
 * <code>length</code> is given and is negative then the
 * sequence will stop that many elements from the end of the
 * array. If it is omitted, then the sequence will have everything
 * from <code>offset</code> up until the end of the
 * <code>array</code>.
 * @param bool|null $preserve_keys Note that <code>array_slice</code> will reorder and reset the
 * numeric array indices by default. You can change this behaviour by setting
 * <code>preserve_keys</code> to <code>TRUE</code>.
 *
 * @return array Returns the slice. If the offset is larger than the size of the array then returns an empty array.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-slice.php
 */
function array_slice(array $array, $offset, $length = null, $preserve_keys = false)
{
}

/**
 * Remove a portion of the array and replace it with something else
 *
 * @param array $input The input array.
 * @param int $offset If <code>offset</code> is positive then the start of removed
 * portion is at that offset from the beginning of the
 * <code>input</code> array. If <code>offset</code>
 * is negative then it starts that far from the end of the
 * <code>input</code> array.
 * @param int|null $length If <code>length</code> is omitted, removes everything
 * from <code>offset</code> to the end of the array. If
 * <code>length</code> is specified and is positive, then
 * that many elements will be removed. If
 * <code>length</code> is specified and is negative then
 * the end of the removed portion will be that many elements from
 * the end of the array. If <code>length</code> is
 * specified and is zero, no elements will be removed.
 * Tip: to remove everything from
 * <code>offset</code> to the end of the array when
 * <code>replacement</code> is also specified, use
 * <code>count($input)</code> for
 * <code>length</code>.
 * @param mixed|null $replacement If <code>replacement</code> array is specified, then the
 * removed elements are replaced with elements from this array.
 *
 * @return array Returns an array consisting of the extracted elements.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-splice.php
 */
function array_splice(array &$input, $offset, $length = /* count($input) */ null, $replacement = /* array() */ null)
{
}

/**
 * Calculate the sum of values in an array
 *
 * @param array $array The input array.
 *
 * @return float Returns the sum of values as an integer or float; <code>0</code> if the
 * <code>array</code> is empty.
 *
 * @since PHP 4 >= 4.0.4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-sum.php
 */
function array_sum(array $array)
{
}

/**
 * Computes the difference of arrays by using a callback function for data comparison
 *
 * @param array $array1 The first array.
 * @param array $array2 The second array.
 * @param array $callback_data_comp_func
 *
 * @return array Returns an array containing all the values of <code>array1</code>
 * that are not present in any of the other arguments.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-udiff.php
 */
function array_udiff(array $array1, array $array2, array $callback_data_comp_func)
{
}

/**
 * Computes the difference of arrays with additional index check, compares data by a callback function
 *
 * @param array $array1 The first array.
 * @param array $array2 The second array.
 * @param array $callback_key_comp_func
 *
 * @return array <code>array_udiff_assoc</code> returns an <code>array</code>
 * containing all the values from <code>array1</code>
 * that are not present in any of the other arguments.
 * Note that the keys are used in the comparison unlike
 * <code>array_diff</code> and <code>array_udiff</code>.
 * The comparison of arrays' data is performed by using an user-supplied
 * callback. In this aspect the behaviour is opposite to the behaviour of
 * <code>array_diff_assoc</code> which uses internal function for
 * comparison.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-udiff-assoc.php
 */
function array_udiff_assoc(array $array1, array $array2, array $callback_key_comp_func)
{
}

/**
 * Computes the difference of arrays with additional index check, compares data and indexes by a callback function
 *
 * @param array $array1 The first array.
 * @param array $array2 The second array.
 * @param array $callback_data_comp_func
 * @param callable $value_compare_func The comparison function must return an integer less than, equal to, or greater than zero if the first argument is considered to be respectively less than, equal to, or greater than the second.
 * Note that before PHP 7.0.0 this integer had to be in the range from -2147483648 to 2147483647.
 *
 * @return array Returns an <code>array</code> containing all the values from
 * <code>array1</code> that are not present in any of the other
 * arguments.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-udiff-uassoc.php
 */
function array_udiff_uassoc(array $array1, array $array2, array $callback_data_comp_func, callable $value_compare_func)
{
}

/**
 * Computes the intersection of arrays, compares data by a callback function
 *
 * @param array $array1 The first array.
 * @param array $array2 The second array.
 * @param array $callback_data_compare_func
 *
 * @return array Returns an array containing all the values of <code>array1</code>
 * that are present in all the arguments.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-uintersect.php
 */
function array_uintersect(array $array1, array $array2, array $callback_data_compare_func)
{
}

/**
 * Computes the intersection of arrays with additional index check, compares data by a callback function
 *
 * @param array $array1 The first array.
 * @param array $array2 The second array.
 * @param array $callback_data_compare_func
 *
 * @return array Returns an array containing all the values of
 * <code>array1</code> that are present in all the arguments.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-uintersect-assoc.php
 */
function array_uintersect_assoc(array $array1, array $array2, array $callback_data_compare_func)
{
}

/**
 * Computes the intersection of arrays with additional index check, compares data and indexes by separate callback functions
 *
 * @param array $array1 The first array.
 * @param array $array2 The second array.
 * @param array $callback_data_compare_func
 * @param callable $value_compare_func The comparison function must return an integer less than, equal to, or greater than zero if the first argument is considered to be respectively less than, equal to, or greater than the second.
 * Note that before PHP 7.0.0 this integer had to be in the range from -2147483648 to 2147483647.
 *
 * @return array Returns an array containing all the values of
 * <code>array1</code> that are present in all the arguments.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-uintersect-uassoc.php
 */
function array_uintersect_uassoc(array $array1, array $array2, array $callback_data_compare_func, callable $value_compare_func)
{
}

/**
 * Removes duplicate values from an array
 *
 * @param array $array The input array.
 * @param int|null $sort_flags The optional second parameter <code>sort_flags</code>
 * may be used to modify the sorting behavior using these values:
 *
 * @return array Returns the filtered array.
 *
 * @since PHP 4 >= 4.0.1, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-unique.php
 */
function array_unique(array $array, $sort_flags = SORT_STRING)
{
}

/**
 * Prepend one or more elements to the beginning of an array
 *
 * @param array $array The input array.
 * @param mixed $vars The values to prepend.
 *
 * @return int Returns the new number of elements in the <code>array</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-unshift.php
 */
function array_unshift(array &$array, ...$vars)
{
}

/**
 * Return all the values of an array
 *
 * @param array $array The array.
 *
 * @return array Returns an indexed array of values.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-values.php
 */
function array_values(array $array)
{
}

/**
 * Apply a user supplied function to every member of an array
 *
 * @param array $array The input array.
 * @param callable $callback Typically, <code>callback</code> takes on two parameters.
 * The <code>array</code> parameter's value being the first, and
 * the key/index second.
 * @param mixed|null $userdata If the optional <code>userdata</code> parameter is supplied,
 * it will be passed as the third parameter to the
 * <code>callback</code>.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-walk.php
 */
function array_walk(array &$array, callable $callback, $userdata = null)
{
}

/**
 * Apply a user function recursively to every member of an array
 *
 * @param array $array The input array.
 * @param callable $callback Typically, <code>callback</code> takes on two parameters.
 * The <code>array</code> parameter's value being the first, and
 * the key/index second.
 * @param mixed|null $userdata If the optional <code>userdata</code> parameter is supplied,
 * it will be passed as the third parameter to the
 * <code>callback</code>.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.array-walk-recursive.php
 */
function array_walk_recursive(array &$array, callable $callback, $userdata = null)
{
}

/**
 * Sort an array in reverse order and maintain index association
 *
 * @param array $array The input array.
 * @param int|null $sort_flags You may modify the behavior of the sort using the optional parameter
 * <code>sort_flags</code>, for details see
 * <code>sort</code>.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.arsort.php
 */
function arsort(array &$array, $sort_flags = SORT_REGULAR)
{
}

/**
 * Arc sine
 *
 * @param float $arg The argument to process
 *
 * @return float The arc sine of <code>arg</code> in radians
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.asin.php
 */
function asin($arg)
{
}

/**
 * Inverse hyperbolic sine
 *
 * @param float $arg The argument to process
 *
 * @return float The inverse hyperbolic sine of <code>arg</code>
 *
 * @since PHP 4 >= 4.1.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.asinh.php
 */
function asinh($arg)
{
}

/**
 * Sort an array and maintain index association
 *
 * @param array $array The input array.
 * @param int|null $sort_flags You may modify the behavior of the sort using the optional
 * parameter <code>sort_flags</code>, for details
 * see <code>sort</code>.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.asort.php
 */
function asort(array &$array, $sort_flags = SORT_REGULAR)
{
}

/**
 * Checks if assertion is <code>FALSE</code>
 *
 * @param mixed $assertion The assertion. In PHP 5, this must be either a <code>string</code> to
 * be evaluated or a <code>boolean</code> to be tested. In PHP 7, this may
 * also be any expression that returns a value, which will be executed and
 * the result used to indicate whether the assertion succeeded or failed.
 *
 * @return bool <code>FALSE</code> if the assertion is false, <code>TRUE</code> otherwise.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.assert.php
 */
function assert($assertion)
{
}

/**
 * Set/get the various assert flags
 *
 * @param int $what <table>
 * <title>Assert Options</title>
 * <thead>
 * <tr>
 * <td>Option</td><td>INI Setting</td><td>Default value</td><td>Description</td>
 * </tr>
 * </thead>
 * <tbody>
 * <tr>
 * <td>ASSERT_ACTIVE</td><td>assert.active</td><td>1</td><td>enable <code>assert</code> evaluation</td>
 * </tr>
 * <tr>
 * <td>ASSERT_WARNING</td><td>assert.warning</td><td>1</td><td>issue a PHP warning for each failed assertion</td>
 * </tr>
 * <tr>
 * <td>ASSERT_BAIL</td><td>assert.bail</td><td>0</td><td>terminate execution on failed assertions</td>
 * </tr>
 * <tr>
 * <td>ASSERT_QUIET_EVAL</td><td>assert.quiet_eval</td><td>0</td><td>
 * disable error_reporting during assertion expression
 * evaluation
 * </td>
 * </tr>
 * <tr>
 * <td>ASSERT_CALLBACK</td><td>assert.callback</td><td>(<code>NULL</code>)</td><td>Callback to call on failed assertions</td>
 * </tr>
 * </tbody>
 * </table>
 * @param mixed $value An optional new value for the option.
 *
 * @return mixed Returns the original setting of any option or <code>FALSE</code> on errors.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.assert-options.php
 */
function assert_options($what, $value = null)
{
}

/**
 * Arc tangent
 *
 * @param float $arg The argument to process
 *
 * @return float The arc tangent of <code>arg</code> in radians.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.atan.php
 */
function atan($arg)
{
}

/**
 * Arc tangent of two variables
 *
 * @param float $y Dividend parameter
 * @param float $x Divisor parameter
 *
 * @return float The arc tangent of <code>y</code>/<code>x</code>
 * in radians.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.atan2.php
 */
function atan2($y, $x)
{
}

/**
 * Inverse hyperbolic tangent
 *
 * @param float $arg The argument to process
 *
 * @return float Inverse hyperbolic tangent of <code>arg</code>
 *
 * @since PHP 4 >= 4.1.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.atanh.php
 */
function atanh($arg)
{
}

/**
 * Decodes data encoded with MIME base64
 *
 * @param string $data The encoded data.
 * @param bool|null $strict If the <code>strict</code> parameter is set to <code>TRUE</code>
 * then the <code>base64_decode</code> function will return
 * <code>FALSE</code> if the input contains character from outside the base64
 * alphabet. Otherwise invalid characters will be silently discarded.
 *
 * @return string Returns the original data or <code>FALSE</code> on failure. The returned data may be
 * binary.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.base64-decode.php
 */
function base64_decode($data, $strict = false)
{
}

/**
 * Encodes data with MIME base64
 *
 * @param string $data The data to encode.
 *
 * @return string The encoded data, as a string or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.base64-encode.php
 */
function base64_encode($data)
{
}

/**
 * Convert a number between arbitrary bases
 *
 * @param string $number The number to convert. Any invalid characters in
 * <code>number</code> are silently ignored.
 * @param int $frombase The base <code>number</code> is in
 * @param int $tobase The base to convert <code>number</code> to
 *
 * @return string <code>number</code> converted to base <code>tobase</code>
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.base-convert.php
 */
function base_convert($number, $frombase, $tobase)
{
}

/**
 * Returns trailing name component of path
 *
 * @param string $path A path.
 * @param string $suffix If the name component ends in <code>suffix</code> this will also
 * be cut off.
 *
 * @return string Returns the base name of the given <code>path</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.basename.php
 */
function basename($path, $suffix = null)
{
}

/**
 * Convert binary data into hexadecimal representation
 *
 * @param string $str A string.
 *
 * @return string Returns the hexadecimal representation of the given string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.bin2hex.php
 */
function bin2hex($str)
{
}

/**
 * Binary to decimal
 *
 * @param string $binary_string The binary string to convert
 *
 * @return float The decimal value of <code>binary_string</code>
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.bindec.php
 */
function bindec($binary_string)
{
}

/**
 * Get the boolean value of a variable
 *
 * @param mixed $var The scalar value being converted to a <code>boolean</code>.
 *
 * @return bool The <code>boolean</code> value of <code>var</code>.
 *
 * @since PHP 5 >= 5.5.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.boolval.php
 */
function boolval($var)
{
}

/**
 * Call the callback given by the first parameter
 *
 * @param callable $callback The <code>callable</code> to be called.
 * @param mixed $parameter Zero or more parameters to be passed to the callback.
 *
 * @return mixed Returns the return value of the callback.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.call-user-func.php
 */
function call_user_func(callable $callback, ...$parameter)
{
}

/**
 * Call a callback with an array of parameters
 *
 * @param callable $callback The <code>callable</code> to be called.
 * @param array $param_arr The parameters to be passed to the callback, as an indexed array.
 *
 * @return mixed Returns the return value of the callback, or <code>FALSE</code> on error.
 *
 * @since PHP 4 >= 4.0.4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.call-user-func-array.php
 */
function call_user_func_array(callable $callback, array $param_arr)
{
}

/**
 * Call a user method on an specific object
 *
 * @param string $method_name The method name being called.
 * @param mixed $obj The <code>object</code> that <code>method_name</code>
 * is being called on.
 * @param mixed $parameter
 *
 * @return mixed
 *
 * @since PHP 4, PHP 5
 *
 * @deprecated as of PHP 4.1.0
 *
 * @link http://www.php.net/manual/en/function.call-user-method.php
 */
function call_user_method($method_name, &$obj, ...$parameter)
{
}

/**
 * Call a user method given with an array of parameters
 *
 * @param string $method_name The method name being called.
 * @param mixed $obj The <code>object</code> that <code>method_name</code>
 * is being called on.
 * @param array $params An array of parameters.
 *
 * @return mixed
 *
 * @since PHP 4 >= 4.0.5, PHP 5
 *
 * @deprecated as of PHP 4.1.0
 *
 * @link http://www.php.net/manual/en/function.call-user-method-array.php
 */
function call_user_method_array($method_name, &$obj, array $params)
{
}

/**
 * Round fractions up
 *
 * @param float $value The value to round
 *
 * @return float <code>value</code> rounded up to the next highest
 * integer.
 * The return value of <code>ceil</code> is still of type
 * <code>float</code> as the value range of <code>float</code> is
 * usually bigger than that of <code>integer</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ceil.php
 */
function ceil($value)
{
}

/**
 * Change directory
 *
 * @param string $directory The new current directory
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.chdir.php
 */
function chdir($directory)
{
}

/**
 * Check DNS records corresponding to a given Internet host name or IP address
 *
 * @param string $host <code>host</code> may either be the IP address in
 * dotted-quad notation or the host name.
 * @param string|null $type <code>type</code> may be any one of: A, MX, NS, SOA,
 * PTR, CNAME, AAAA, A6, SRV, NAPTR, TXT or ANY.
 *
 * @return bool Returns <code>TRUE</code> if any records are found; returns <code>FALSE</code> if no records
 * were found or if an error occurred.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.checkdnsrr.php
 */
function checkdnsrr($host, $type = "MX")
{
}

/**
 * Changes file group
 *
 * @param string $filename Path to the file.
 * @param mixed $group A group name or number.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.chgrp.php
 */
function chgrp($filename, $group)
{
}

/**
 * Changes file mode
 *
 * @param string $filename Path to the file.
 * @param int $mode Note that <code>mode</code> is not automatically
 * assumed to be an octal value, so to ensure the expected operation,
 * you need to prefix <code>mode</code> with a zero (0).
 * Strings such as "g+w" will not work properly.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.chmod.php
 */
function chmod($filename, $mode)
{
}

/**
 * Alias of <code>rtrim</code>
 *
 * @param mixed $str
 * @param mixed $character_mask
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.chop.php
 */
function chop($str, $character_mask = null)
{
}

/**
 * Changes file owner
 *
 * @param string $filename Path to the file.
 * @param mixed $user A user name or number.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.chown.php
 */
function chown($filename, $user)
{
}

/**
 * Return a specific character
 *
 * @param int $ascii The extended ASCII code.
 *
 * @return string Returns the specified character.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.chr.php
 */
function chr($ascii)
{
}

/**
 * Change the root directory
 *
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @param string $directory The path to change the root directory to.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4 >= 4.0.5, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.chroot.php
 */
function chroot($directory)
{
}

/**
 * Split a string into smaller chunks
 *
 * @param string $body The string to be chunked.
 * @param int|null $chunklen The chunk length.
 * @param string|null $end The line ending sequence.
 *
 * @return string Returns the chunked string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.chunk-split.php
 */
function chunk_split($body, $chunklen = 76, $end = "\r\n")
{
}

/**
 * Clears file status cache
 *
 * @param bool|null $clear_realpath_cache Whether to clear the realpath cache or not.
 * @param string $filename Clear the realpath and the stat cache for a specific filename only; only
 * used if <code>clear_realpath_cache</code> is <code>TRUE</code>.
 *
 * @return void No value is returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.clearstatcache.php
 */
function clearstatcache($clear_realpath_cache = false, $filename = null)
{
}

/**
 * Returns the current process title
 *
 * @return string Return a string with the current process title or <code>NULL</code> on error.
 *
 * @since PHP 5 >= 5.5.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.cli-get-process-title.php
 */
function cli_get_process_title()
{
}

/**
 * Sets the process title
 *
 * @param string $title The new title.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 5 >= 5.5.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.cli-set-process-title.php
 */
function cli_set_process_title($title)
{
}

/**
 * Close directory handle
 *
 * @param resource $dir_handle The directory handle <code>resource</code> previously opened
 * with <code>opendir</code>. If the directory handle is
 * not specified, the last link opened by <code>opendir</code>
 * is assumed.
 *
 * @return void
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.closedir.php
 */
function closedir($dir_handle = null)
{
}

/**
 * Close connection to system logger
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.closelog.php
 */
function closelog()
{
}

/**
 * Create array containing variables and their values
 *
 * @param mixed $varname1 <code>compact</code> takes a variable number of parameters.
 * Each parameter can be either a string containing the name of the
 * variable, or an array of variable names. The array can contain other
 * arrays of variable names inside it; <code>compact</code>
 * handles it recursively.
 *
 * @return array Returns the output array with all the variables added to it.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.compact.php
 */
function compact(...$varname1)
{
}

/**
 * Check whether client disconnected
 *
 * @return int Returns 1 if client disconnected, 0 otherwise.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.connection-aborted.php
 */
function connection_aborted()
{
}

/**
 * Returns connection status bitfield
 *
 * @return int Returns the connection status bitfield, which can be used against the
 * <code>CONNECTION_XXX</code> constants to determine the connection
 * status.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.connection-status.php
 */
function connection_status()
{
}

/**
 * Returns the value of a constant
 *
 * @param string $name The constant name.
 *
 * @return mixed Returns the value of the constant, or <code>NULL</code> if the constant is not
 * defined.
 *
 * @since PHP 4 >= 4.0.4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.constant.php
 */
function constant($name)
{
}

/**
 * Convert from one Cyrillic character set to another
 *
 * @param string $str The string to be converted.
 * @param string $from The source Cyrillic character set, as a single character.
 * @param string $to The target Cyrillic character set, as a single character.
 *
 * @return string Returns the converted string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.convert-cyr-string.php
 */
function convert_cyr_string($str, $from, $to)
{
}

/**
 * Decode a uuencoded string
 *
 * @param string $data The uuencoded data.
 *
 * @return string Returns the decoded data as a string or <code>FALSE</code> on failure.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.convert-uudecode.php
 */
function convert_uudecode($data)
{
}

/**
 * Uuencode a string
 *
 * @param string $data The data to be encoded.
 *
 * @return string Returns the uuencoded data or <code>FALSE</code> on failure.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.convert-uuencode.php
 */
function convert_uuencode($data)
{
}

/**
 * Copies file
 *
 * @param string $source Path to the source file.
 * @param string $dest The destination path. If <code>dest</code> is a URL, the
 * copy operation may fail if the wrapper does not support overwriting of
 * existing files.
 * @param resource $context A valid context resource created with
 * <code>stream_context_create</code>.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.copy.php
 */
function copy($source, $dest, $context = null)
{
}

/**
 * Cosine
 *
 * @param float $arg An angle in radians
 *
 * @return float The cosine of <code>arg</code>
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.cos.php
 */
function cos($arg)
{
}

/**
 * Hyperbolic cosine
 *
 * @param float $arg The argument to process
 *
 * @return float The hyperbolic cosine of <code>arg</code>
 *
 * @since PHP 4 >= 4.1.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.cosh.php
 */
function cosh($arg)
{
}

/**
 * Count all elements in an array, or something in an object
 *
 * @param mixed $array_or_countable An array or <code>Countable</code> object.
 * @param int|null $mode If the optional <code>mode</code> parameter is set to
 * <code>COUNT_RECURSIVE</code> (or 1), <code>count</code>
 * will recursively count the array. This is particularly useful for
 * counting all the elements of a multidimensional array.
 *
 * @return int Returns the number of elements in <code>array_or_countable</code>.
 * When the parameter is neither an array nor an object with
 * implemented <code>Countable</code> interface,
 * <code>1</code> will be returned.
 * There is one exception, if <code>array_or_countable</code> is <code>NULL</code>,
 * <code>0</code> will be returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.count.php
 */
function count($array_or_countable, $mode = COUNT_NORMAL)
{
}

/**
 * Return information about characters used in a string
 *
 * @param string $string The examined string.
 * @param int|null $mode See return values.
 *
 * @return mixed Depending on <code>mode</code>
 * <code>count_chars</code> returns one of the following:
 * <ul>
 * 0 - an array with the byte-value as key and the frequency of
 * every byte as value.
 * 1 - same as 0 but only byte-values with a frequency greater
 * than zero are listed.
 * 2 - same as 0 but only byte-values with a frequency equal to
 * zero are listed.
 * 3 - a string containing all unique characters is returned.
 * 4 - a string containing all not used characters is returned.
 * </ul>
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.count-chars.php
 */
function count_chars($string, $mode = 0)
{
}

/**
 * Calculates the crc32 polynomial of a string
 *
 * @param string $str The data.
 *
 * @return int Returns the crc32 checksum of <code>str</code> as an integer.
 *
 * @since PHP 4 >= 4.0.1, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.crc32.php
 */
function crc32($str)
{
}

/**
 * One-way string hashing
 *
 * @param string $str The string to be hashed.
 * @param string $salt An optional salt string to base the hashing on. If not provided, the
 * behaviour is defined by the algorithm implementation and can lead to
 * unexpected results.
 *
 * @return string Returns the hashed string or a string that is shorter than 13 characters
 * and is guaranteed to differ from the salt on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.crypt.php
 */
function crypt($str, $salt = null)
{
}

/**
 * Return the current element in an array
 *
 * @param array $array The array.
 *
 * @return mixed The <code>current</code> function simply returns the
 * value of the array element that's currently being pointed to by the
 * internal pointer. It does not move the pointer in any way. If the
 * internal pointer points beyond the end of the elements list or the array is
 * empty, <code>current</code> returns <code>FALSE</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.current.php
 */
function current(array &$array)
{
}

/**
 * Dumps a string representation of an internal zend value to output
 *
 * @param mixed $variable The variable being evaluated.
 *
 * @return void No value is returned.
 *
 * @since PHP 4 >= 4.2.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.debug-zval-dump.php
 */
function debug_zval_dump(...$variable)
{
}

/**
 * Decimal to binary
 *
 * @param int $number Decimal value to convert
 *
 * @return string Binary string representation of <code>number</code>
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.decbin.php
 */
function decbin($number)
{
}

/**
 * Decimal to hexadecimal
 *
 * @param int $number The decimal value to convert.
 *
 * @return string Hexadecimal string representation of <code>number</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.dechex.php
 */
function dechex($number)
{
}

/**
 * Decimal to octal
 *
 * @param int $number Decimal value to convert
 *
 * @return string Octal string representation of <code>number</code>
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.decoct.php
 */
function decoct($number)
{
}

/**
 * Converts the number in degrees to the radian equivalent
 *
 * @param float $number Angular value in degrees
 *
 * @return float The radian equivalent of <code>number</code>
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.deg2rad.php
 */
function deg2rad($number)
{
}

/**
 * Return an instance of the Directory class
 *
 * @param string $directory Directory to open
 * @param resource $context <blockquote>Context support was added
 * with PHP 5.0.0. For a description of <code>contexts</code>, refer to
 * .</blockquote>
 *
 * @return Directory Returns an instance of <code>Directory</code>, or <code>NULL</code> with
 * wrong parameters, or <code>FALSE</code> in case of another error.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.dir.php
 */
function dir($directory, $context = null)
{
}

/**
 * Returns a parent directory's path
 *
 * @param string $path A path.
 *
 * @return string Returns the path of a parent directory. If there are no slashes in
 * <code>path</code>, a dot ('<code>.</code>') is returned,
 * indicating the current directory. Otherwise, the returned string is
 * <code>path</code> with any trailing
 * <code>/component</code> removed.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.dirname.php
 */
function dirname($path)
{
}

/**
 * Returns available space on filesystem or disk partition
 *
 * @param string $directory A directory of the filesystem or disk partition.
 *
 * @return float Returns the number of available bytes as a float
 * or <code>FALSE</code> on failure.
 *
 * @since PHP 4 >= 4.1.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.disk-free-space.php
 */
function disk_free_space($directory)
{
}

/**
 * Returns the total size of a filesystem or disk partition
 *
 * @param string $directory A directory of the filesystem or disk partition.
 *
 * @return float Returns the total number of bytes as a float
 * or <code>FALSE</code> on failure.
 *
 * @since PHP 4 >= 4.1.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.disk-total-space.php
 */
function disk_total_space($directory)
{
}

/**
 * Alias of <code>disk_free_space</code>
 *
 * @param mixed $path
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.diskfreespace.php
 */
function diskfreespace($path)
{
}

/**
 * Loads a PHP extension at runtime
 *
 * @param string $library This parameter is <code>only</code> the filename of the
 * extension to load which also depends on your platform. For example,
 * the sockets extension (if compiled
 * as a shared module, not the default!) would be called
 * <code>sockets.so</code> on Unix platforms whereas it is called
 * <code>php_sockets.dll</code> on the Windows platform.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure. If the functionality of loading modules is not available
 * or has been disabled (either by setting
 * enable_dl off or by enabling safe mode
 * in <code>php.ini</code>) an <code>E_ERROR</code> is emitted
 * and execution is stopped. If <code>dl</code> fails because the
 * specified library couldn't be loaded, in addition to <code>FALSE</code> an
 * <code>E_WARNING</code> message is emitted.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.dl.php
 */
function dl($library)
{
}

/**
 * Alias of <code>checkdnsrr</code>
 *
 * @param mixed $host
 * @param mixed $type
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.dns-check-record.php
 */
function dns_check_record($host, $type = null)
{
}

/**
 * Alias of <code>getmxrr</code>
 *
 * @param mixed $hostname
 * @param mixed $mxhosts
 * @param mixed $weight
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.dns-get-mx.php
 */
function dns_get_mx($hostname, &$mxhosts, &$weight = null)
{
}

/**
 * Fetch DNS Resource Records associated with a hostname
 *
 * @param string $hostname <code>hostname</code> should be a valid DNS hostname such
 * as "<code>www.example.com</code>". Reverse lookups can be generated
 * using <code>in-addr.arpa</code> notation, but
 * <code>gethostbyaddr</code> is more suitable for
 * the majority of reverse lookups.
 * @param int|null $type By default, <code>dns_get_record</code> will search for any
 * resource records associated with <code>hostname</code>.
 * To limit the query, specify the optional <code>type</code>
 * parameter. May be any one of the following:
 * <code>DNS_A</code>, <code>DNS_CNAME</code>,
 * <code>DNS_HINFO</code>, <code>DNS_CAA</code>,
 * <code>DNS_MX</code>, <code>DNS_NS</code>,
 * <code>DNS_PTR</code>, <code>DNS_SOA</code>,
 * <code>DNS_TXT</code>, <code>DNS_AAAA</code>,
 * <code>DNS_SRV</code>, <code>DNS_NAPTR</code>,
 * <code>DNS_A6</code>, <code>DNS_ALL</code>
 * or <code>DNS_ANY</code>.
 * @param array|null $authns Passed by reference and, if given, will be populated with Resource
 * Records for the <code>Authoritative Name Servers</code>.
 * @param array|null $addtl Passed by reference and, if given, will be populated with any
 * <code>Additional Records</code>.
 * @param bool|null $raw In case of raw mode, we query only the requested type instead of looping
 * type by type before going with the additional info stuff.
 *
 * @return array This function returns an array of associative arrays,
 * or <code>FALSE</code> on failure. Each associative array contains
 * <code>at minimum</code> the following keys:
 * <table>
 * <title>Basic DNS attributes</title>
 * <thead>
 * <tr>
 * <td>Attribute</td><td>Meaning</td>
 * </tr>
 * </thead>
 * <tbody>
 * <tr>
 * <td>host</td><td>
 * The record in the DNS namespace to which the rest of the associated data refers.
 * </td>
 * </tr>
 * <tr>
 * <td>class</td><td>
 * <code>dns_get_record</code> only returns Internet class records and as
 * such this parameter will always return <code>IN</code>.
 * </td>
 * </tr>
 * <tr>
 * <td>type</td><td>
 * String containing the record type. Additional attributes will also be contained
 * in the resulting array dependant on the value of type. See table below.
 * </td>
 * </tr>
 * <tr>
 * <td>ttl</td><td>
 * <code>"Time To Live"</code> remaining for this record. This will <code>not</code> equal
 * the record's original ttl, but will rather equal the original ttl minus whatever
 * length of time has passed since the authoritative name server was queried.
 * </td>
 * </tr>
 * </tbody>
 * </table>
 * <table>
 * <title>Other keys in associative arrays dependant on 'type'</title>
 * <thead>
 * <tr>
 * <td>Type</td><td>Extra Columns</td>
 * </tr>
 * </thead>
 * <tbody>
 * <tr>
 * <td><code>A</code></td><td>
 * <code>ip</code>: An IPv4 addresses in dotted decimal notation.
 * </td>
 * </tr>
 * <tr>
 * <td><code>MX</code></td><td>
 * <code>pri</code>: Priority of mail exchanger.
 * Lower numbers indicate greater priority.
 * <code>target</code>: FQDN of the mail exchanger.
 * See also <code>dns_get_mx</code>.
 * </td>
 * </tr>
 * <tr>
 * <td><code>CNAME</code></td><td>
 * <code>target</code>: FQDN of location in DNS namespace to which
 * the record is aliased.
 * </td>
 * </tr>
 * <tr>
 * <td><code>NS</code></td><td>
 * <code>target</code>: FQDN of the name server which is authoritative
 * for this hostname.
 * </td>
 * </tr>
 * <tr>
 * <td><code>PTR</code></td><td>
 * <code>target</code>: Location within the DNS namespace to which
 * this record points.
 * </td>
 * </tr>
 * <tr>
 * <td><code>TXT</code></td><td>
 * <code>txt</code>: Arbitrary string data associated with this record.
 * </td>
 * </tr>
 * <tr>
 * <td><code>HINFO</code></td><td>
 * <code>cpu</code>: IANA number designating the CPU of the machine
 * referenced by this record.
 * <code>os</code>: IANA number designating the Operating System on
 * the machine referenced by this record.
 * See IANA's <code>Operating System
 * Names</code> for the meaning of these values.
 * </td>
 * </tr>
 * <tr>
 * <td><code>CAA</code></td><td>
 * <code>flags</code>: A one-byte bitfield; currently only bit 0 is defined,
 * meaning 'critical'; other bits are reserved and should be ignored.
 * <code>tag</code>: The CAA tag name (alphanumeric ASCII string).
 * <code>value</code>: The CAA tag value (binary string, may use subformats).
 * For additional information see: RFC 6844
 * </td>
 * </tr>
 * <tr>
 * <td><code>SOA</code></td><td>
 * <code>mname</code>: FQDN of the machine from which the resource
 * records originated.
 * <code>rname</code>: Email address of the administrative contact
 * for this domain.
 * <code>serial</code>: Serial # of this revision of the requested
 * domain.
 * <code>refresh</code>: Refresh interval (seconds) secondary name
 * servers should use when updating remote copies of this domain.
 * <code>retry</code>: Length of time (seconds) to wait after a
 * failed refresh before making a second attempt.
 * <code>expire</code>: Maximum length of time (seconds) a secondary
 * DNS server should retain remote copies of the zone data without a
 * successful refresh before discarding.
 * <code>minimum-ttl</code>: Minimum length of time (seconds) a
 * client can continue to use a DNS resolution before it should request
 * a new resolution from the server. Can be overridden by individual
 * resource records.
 * </td>
 * </tr>
 * <tr>
 * <td><code>AAAA</code></td><td>
 * <code>ipv6</code>: IPv6 address
 * </td>
 * </tr>
 * <tr>
 * <td><code>A6</code>(PHP &gt;= 5.1.0)</td><td>
 * <code>masklen</code>: Length (in bits) to inherit from the target
 * specified by <code>chain</code>.
 * <code>ipv6</code>: Address for this specific record to merge with
 * <code>chain</code>.
 * <code>chain</code>: Parent record to merge with
 * <code>ipv6</code> data.
 * </td>
 * </tr>
 * <tr>
 * <td><code>SRV</code></td><td>
 * <code>pri</code>: (Priority) lowest priorities should be used first.
 * <code>weight</code>: Ranking to weight which of commonly prioritized
 * <code>targets</code> should be chosen at random.
 * <code>target</code> and <code>port</code>: hostname and port
 * where the requested service can be found.
 * For additional information see: RFC 2782
 * </td>
 * </tr>
 * <tr>
 * <td><code>NAPTR</code></td><td>
 * <code>order</code> and <code>pref</code>: Equivalent to
 * <code>pri</code> and <code>weight</code> above.
 * <code>flags</code>, <code>services</code>, <code>regex</code>,
 * and <code>replacement</code>: Parameters as defined by
 * RFC 2915.
 * </td>
 * </tr>
 * </tbody>
 * </table>
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.dns-get-record.php
 */
function dns_get_record($hostname, $type = DNS_ANY, array &$authns = null, array &$addtl = null, $raw = false)
{
}

/**
 * Alias of <code>floatval</code>
 *
 * @param mixed $var
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.doubleval.php
 */
function doubleval($var)
{
}

/**
 * Set the internal pointer of an array to its last element
 *
 * @param array $array The array. This array is passed by reference because it is modified by
 * the function. This means you must pass it a real variable and not
 * a function returning an array because only actual variables may be
 * passed by reference.
 *
 * @return mixed Returns the value of the last element or <code>FALSE</code> for empty array.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.end.php
 */
function end(array &$array)
{
}

/**
 * Get the last occurred error
 *
 * @return array Returns an associative array describing the last error with keys "type",
 * "message", "file" and "line". If the error has been caused by a PHP
 * internal function then the "message" begins with its name.
 * Returns <code>NULL</code> if there hasn't been an error yet.
 *
 * @since PHP 5 >= 5.2.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.error-get-last.php
 */
function error_get_last()
{
}

/**
 * Send an error message to the defined error handling routines
 *
 * @param string $message The error message that should be logged.
 * @param int|null $message_type Says where the error should go. The possible message types are as
 * follows:
 * @param string $destination The destination. Its meaning depends on the
 * <code>message_type</code> parameter as described above.
 * @param string $extra_headers The extra headers. It's used when the <code>message_type</code>
 * parameter is set to <code>1</code>.
 * This message type uses the same internal function as
 * <code>mail</code> does.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.error-log.php
 */
function error_log($message, $message_type = 0, $destination = null, $extra_headers = null)
{
}

/**
 * Escape a string to be used as a shell argument
 *
 * @param string $arg The argument that will be escaped.
 *
 * @return string The escaped string.
 *
 * @since PHP 4 >= 4.0.3, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.escapeshellarg.php
 */
function escapeshellarg($arg)
{
}

/**
 * Escape shell metacharacters
 *
 * @param string $command The command that will be escaped.
 *
 * @return string The escaped string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.escapeshellcmd.php
 */
function escapeshellcmd($command)
{
}

/**
 * Execute an external program
 *
 * @param string $command The command that will be executed.
 * @param array $output If the <code>output</code> argument is present, then the
 * specified array will be filled with every line of output from the
 * command. Trailing whitespace, such as <code>\n</code>, is not
 * included in this array. Note that if the array already contains some
 * elements, <code>exec</code> will append to the end of the array.
 * If you do not want the function to append elements, call
 * <code>unset</code> on the array before passing it to
 * <code>exec</code>.
 * @param int $return_var If the <code>return_var</code> argument is present
 * along with the <code>output</code> argument, then the
 * return status of the executed command will be written to this
 * variable.
 *
 * @return string The last line from the result of the command. If you need to execute a
 * command and have all the data from the command passed directly back without
 * any interference, use the <code>passthru</code> function.
 * To get the output of the executed command, be sure to set and use the
 * <code>output</code> parameter.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.exec.php
 */
function exec($command, array &$output = null, &$return_var = null)
{
}

/**
 * Calculates the exponent of <code>e</code>
 *
 * @param float $arg The argument to process
 *
 * @return float 'e' raised to the power of <code>arg</code>
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.exp.php
 */
function exp($arg)
{
}

/**
 * Split a string by a string
 *
 * @param string $delimiter The boundary string.
 * @param string $string The input string.
 * @param int|null $limit If <code>limit</code> is set and positive, the returned array will contain
 * a maximum of <code>limit</code> elements with the last
 * element containing the rest of <code>string</code>.
 *
 * @return array Returns an <code>array</code> of <code>string</code>s
 * created by splitting the <code>string</code> parameter on
 * boundaries formed by the <code>delimiter</code>.
 * If <code>delimiter</code> is an empty <code>string</code> (""),
 * <code>explode</code> will return <code>FALSE</code>.
 * If <code>delimiter</code> contains a value that is not
 * contained in <code>string</code> and a negative
 * <code>limit</code> is used, then an empty <code>array</code> will be
 * returned, otherwise an <code>array</code> containing
 * <code>string</code> will be returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.explode.php
 */
function explode($delimiter, $string, $limit = PHP_INT_MAX)
{
}

/**
 * Returns exp(number) - 1, computed in a way that is accurate even
 * when the value of number is close to zero
 *
 * @param float $arg The argument to process
 *
 * @return float 'e' to the power of <code>arg</code> minus one
 *
 * @since PHP 4 >= 4.1.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.expm1.php
 */
function expm1($arg)
{
}

/**
 * Import variables into the current symbol table from an array
 *
 * @param array $array An associative array. This function treats keys as variable names and
 * values as variable values. For each key/value pair it will create a
 * variable in the current symbol table, subject to
 * <code>flags</code> and <code>prefix</code> parameters.
 * @param int|null $flags The way invalid/numeric keys and collisions are treated is determined
 * by the extraction <code>flags</code>. It can be one of the
 * following values:
 * <ul>
 * <li>
 * <code><code>EXTR_OVERWRITE</code></code>
 * If there is a collision, overwrite the existing variable.
 * </li>
 * <li>
 * <code><code>EXTR_SKIP</code></code>
 * If there is a collision, don't overwrite the existing
 * variable.
 * </li>
 * <li>
 * <code><code>EXTR_PREFIX_SAME</code></code>
 * If there is a collision, prefix the variable name with
 * <code>prefix</code>.
 * </li>
 * <li>
 * <code><code>EXTR_PREFIX_ALL</code></code>
 * Prefix all variable names with
 * <code>prefix</code>.
 * </li>
 * <li>
 * <code><code>EXTR_PREFIX_INVALID</code></code>
 * Only prefix invalid/numeric variable names with
 * <code>prefix</code>.
 * </li>
 * <li>
 * <code><code>EXTR_IF_EXISTS</code></code>
 * Only overwrite the variable if it already exists in the
 * current symbol table, otherwise do nothing. This is useful
 * for defining a list of valid variables and then extracting
 * only those variables you have defined out of
 * <code>$_REQUEST</code>, for example.
 * </li>
 * <li>
 * <code><code>EXTR_PREFIX_IF_EXISTS</code></code>
 * Only create prefixed variable names if the non-prefixed version
 * of the same variable exists in the current symbol table.
 * </li>
 * <li>
 * <code><code>EXTR_REFS</code></code>
 * Extracts variables as references. This effectively means that the
 * values of the imported variables are still referencing the values of
 * the <code>array</code> parameter. You can use this flag
 * on its own or combine it with any other flag by OR'ing the
 * <code>flags</code>.
 * </li>
 * </ul>
 * @param string|null $prefix Note that <code>prefix</code> is only required if
 * <code>flags</code> is <code>EXTR_PREFIX_SAME</code>,
 * <code>EXTR_PREFIX_ALL</code>, <code>EXTR_PREFIX_INVALID</code>
 * or <code>EXTR_PREFIX_IF_EXISTS</code>. If
 * the prefixed result is not a valid variable name, it is not
 * imported into the symbol table. Prefixes are automatically separated from
 * the array key by an underscore character.
 *
 * @return int Returns the number of variables successfully imported into the symbol
 * table.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.extract.php
 */
function extract(array &$array, $flags = EXTR_OVERWRITE, $prefix = null)
{
}

/**
 * Calculate the hash value needed by EZMLM
 *
 * @param string $addr The email address that's being hashed.
 *
 * @return int The hash value of <code>addr</code>.
 *
 * @since PHP 4 >= 4.0.2, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ezmlm-hash.php
 */
function ezmlm_hash($addr)
{
}

/**
 * Closes an open file pointer
 *
 * @param resource $handle The file pointer must be valid, and must point to a file successfully
 * opened by <code>fopen</code> or <code>fsockopen</code>.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fclose.php
 */
function fclose($handle)
{
}

/**
 * Tests for end-of-file on a file pointer
 *
 * @param resource $handle The file pointer must be valid, and must point to
 * a file successfully opened by <code>fopen</code> or
 * <code>fsockopen</code> (and not yet closed by
 * <code>fclose</code>).
 *
 * @return bool Returns <code>TRUE</code> if the file pointer is at EOF or an error occurs
 * (including socket timeout); otherwise returns <code>FALSE</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.feof.php
 */
function feof($handle)
{
}

/**
 * Flushes the output to a file
 *
 * @param resource $handle The file pointer must be valid, and must point to
 * a file successfully opened by <code>fopen</code> or
 * <code>fsockopen</code> (and not yet closed by
 * <code>fclose</code>).
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4 >= 4.0.1, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fflush.php
 */
function fflush($handle)
{
}

/**
 * Gets character from file pointer
 *
 * @param resource $handle The file pointer must be valid, and must point to
 * a file successfully opened by <code>fopen</code> or
 * <code>fsockopen</code> (and not yet closed by
 * <code>fclose</code>).
 *
 * @return string Returns a string containing a single character read from the file pointed
 * to by <code>handle</code>. Returns <code>FALSE</code> on EOF.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fgetc.php
 */
function fgetc($handle)
{
}

/**
 * Gets line from file pointer and parse for CSV fields
 *
 * @param resource $handle A valid file pointer to a file successfully opened by
 * <code>fopen</code>, <code>popen</code>, or
 * <code>fsockopen</code>.
 * @param int|null $length Must be greater than the longest line (in characters) to be found in
 * the CSV file (allowing for trailing line-end characters). Otherwise the
 * line is split in chunks of <code>length</code> characters,
 * unless the split would occur inside an enclosure.
 * @param string|null $delimiter The optional <code>delimiter</code> parameter sets the field delimiter (one character only).
 * @param string|null $enclosure The optional <code>enclosure</code> parameter sets the field enclosure character (one character only).
 * @param string|null $escape The optional <code>escape</code> parameter sets the escape character (one character only).
 *
 * @return array Returns an indexed array containing the fields read.
 * <code>fgetcsv</code> returns <code>NULL</code> if an invalid
 * <code>handle</code> is supplied or <code>FALSE</code> on other errors,
 * including end of file.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fgetcsv.php
 */
function fgetcsv($handle, $length = 0, $delimiter = ",", $enclosure = '"', $escape = "\\")
{
}

/**
 * Gets line from file pointer
 *
 * @param resource $handle The file pointer must be valid, and must point to
 * a file successfully opened by <code>fopen</code> or
 * <code>fsockopen</code> (and not yet closed by
 * <code>fclose</code>).
 * @param int $length Reading ends when <code>length</code> - 1 bytes have been
 * read, or a newline (which is included in the return value), or an EOF
 * (whichever comes first). If no length is specified, it will keep
 * reading from the stream until it reaches the end of the line.
 *
 * @return string Returns a string of up to <code>length</code> - 1 bytes read from
 * the file pointed to by <code>handle</code>. If there is no more data
 * to read in the file pointer, then <code>FALSE</code> is returned.
 * If an error occurs, <code>FALSE</code> is returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fgets.php
 */
function fgets($handle, $length = null)
{
}

/**
 * Gets line from file pointer and strip HTML tags
 *
 * @param resource $handle The file pointer must be valid, and must point to
 * a file successfully opened by <code>fopen</code> or
 * <code>fsockopen</code> (and not yet closed by
 * <code>fclose</code>).
 * @param int $length Length of the data to be retrieved.
 * @param string $allowable_tags You can use the optional third parameter to specify tags which should
 * not be stripped.
 * See <code>strip_tags</code> for details regarding
 * <code>allowable_tags</code>.
 *
 * @return string Returns a string of up to <code>length</code> - 1 bytes read from
 * the file pointed to by <code>handle</code>, with all HTML and PHP
 * code stripped.
 * If an error occurs, returns <code>FALSE</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fgetss.php
 */
function fgetss($handle, $length = null, $allowable_tags = null)
{
}

/**
 * Reads entire file into an array
 *
 * @param string $filename Path to the file.
 * @param int|null $flags The optional parameter <code>flags</code> can be one, or
 * more, of the following constants:
 * <ul>
 * <li>
 * <code>
 * <code>FILE_USE_INCLUDE_PATH</code>
 * </code>
 * Search for the file in the include_path.
 * </li>
 * <li>
 * <code>
 * <code>FILE_IGNORE_NEW_LINES</code>
 * </code>
 * Do not add newline at the end of each array element
 * </li>
 * <li>
 * <code>
 * <code>FILE_SKIP_EMPTY_LINES</code>
 * </code>
 * Skip empty lines
 * </li>
 * </ul>
 * @param resource $context A context resource created with the
 * <code>stream_context_create</code> function.
 *
 * @return array Returns the file in an array. Each element of the array corresponds to a
 * line in the file, with the newline still attached. Upon failure,
 * <code>file</code> returns <code>FALSE</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.file.php
 */
function file($filename, $flags = 0, $context = null)
{
}

/**
 * Checks whether a file or directory exists
 *
 * @param string $filename Path to the file or directory.
 *
 * @return bool Returns <code>TRUE</code> if the file or directory specified by
 * <code>filename</code> exists; <code>FALSE</code> otherwise.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.file-exists.php
 */
function file_exists($filename)
{
}

/**
 * Reads entire file into a string
 *
 * @param string $filename Name of the file to read.
 * @param bool|null $use_include_path As of PHP 5 the <code>FILE_USE_INCLUDE_PATH</code> constant can be used
 * to trigger include path
 * search.
 * This is not possible if strict typing
 * is enabled, since <code>FILE_USE_INCLUDE_PATH</code> is an
 * <code>int</code>. Use <code>TRUE</code> instead.
 * @param resource $context A valid context resource created with
 * <code>stream_context_create</code>. If you don't need to use a
 * custom context, you can skip this parameter by <code>NULL</code>.
 * @param int|null $offset The offset where the reading starts on the original stream.
 * Negative offsets count from the end of the stream.
 * @param int $maxlen Maximum length of data read. The default is to read until end
 * of file is reached. Note that this parameter is applied to the
 * stream processed by the filters.
 *
 * @return string The function returns the read data or <code>FALSE</code> on failure.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.file-get-contents.php
 */
function file_get_contents($filename, $use_include_path = false, $context = null, $offset = 0, $maxlen = null)
{
}

/**
 * Write a string to a file
 *
 * @param string $filename Path to the file where to write the data.
 * @param mixed $data The data to write. Can be either a <code>string</code>, an
 * <code>array</code> or a <code>stream</code> resource.
 * @param int|null $flags The value of <code>flags</code> can be any combination of
 * the following flags, joined with the binary OR (<code>|</code>)
 * operator.
 * @param resource $context A valid context resource created with
 * <code>stream_context_create</code>.
 *
 * @return int This function returns the number of bytes that were written to the file, or
 * <code>FALSE</code> on failure.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.file-put-contents.php
 */
function file_put_contents($filename, $data, $flags = 0, $context = null)
{
}

/**
 * Gets last access time of file
 *
 * @param string $filename Path to the file.
 *
 * @return int Returns the time the file was last accessed, or <code>FALSE</code> on failure.
 * The time is returned as a Unix timestamp.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fileatime.php
 */
function fileatime($filename)
{
}

/**
 * Gets inode change time of file
 *
 * @param string $filename Path to the file.
 *
 * @return int Returns the time the file was last changed, or <code>FALSE</code> on failure.
 * The time is returned as a Unix timestamp.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.filectime.php
 */
function filectime($filename)
{
}

/**
 * Gets file group
 *
 * @param string $filename Path to the file.
 *
 * @return int Returns the group ID of the file, or <code>FALSE</code> if
 * an error occurs. The group ID is returned in numerical format, use
 * <code>posix_getgrgid</code> to resolve it to a group name.
 * Upon failure, <code>FALSE</code> is returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.filegroup.php
 */
function filegroup($filename)
{
}

/**
 * Gets file inode
 *
 * @param string $filename Path to the file.
 *
 * @return int Returns the inode number of the file, or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fileinode.php
 */
function fileinode($filename)
{
}

/**
 * Gets file modification time
 *
 * @param string $filename Path to the file.
 *
 * @return int Returns the time the file was last modified, or <code>FALSE</code> on failure.
 * The time is returned as a Unix timestamp, which is
 * suitable for the <code>date</code> function.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.filemtime.php
 */
function filemtime($filename)
{
}

/**
 * Gets file owner
 *
 * @param string $filename Path to the file.
 *
 * @return int Returns the user ID of the owner of the file, or <code>FALSE</code> on failure.
 * The user ID is returned in numerical format, use
 * <code>posix_getpwuid</code> to resolve it to a username.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fileowner.php
 */
function fileowner($filename)
{
}

/**
 * Gets file permissions
 *
 * @param string $filename Path to the file.
 *
 * @return int Returns the file's permissions as a numeric mode. Lower bits of this mode
 * are the same as the permissions expected by <code>chmod</code>,
 * however on most platforms the return value will also include information on
 * the type of file given as <code>filename</code>. The examples
 * below demonstrate how to test the return value for specific permissions and
 * file types on POSIX systems, including Linux and Mac OS X.
 * For local files, the specific return value is that of the
 * <code>st_mode</code> member of the structure returned by the C
 * library's <code>stat</code> function. Exactly which bits are set
 * can vary from platform to platform, and looking up your specific platform's
 * documentation is recommended if parsing the non-permission bits of the
 * return value is required.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fileperms.php
 */
function fileperms($filename)
{
}

/**
 * Gets file size
 *
 * @param string $filename Path to the file.
 *
 * @return int Returns the size of the file in bytes, or <code>FALSE</code> (and generates an error
 * of level <code>E_WARNING</code>) in case of an error.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.filesize.php
 */
function filesize($filename)
{
}

/**
 * Gets file type
 *
 * @param string $filename Path to the file.
 *
 * @return string Returns the type of the file. Possible values are fifo, char,
 * dir, block, link, file, socket and unknown.
 * Returns <code>FALSE</code> if an error occurs. <code>filetype</code> will also
 * produce an <code>E_NOTICE</code> message if the stat call fails
 * or if the file type is unknown.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.filetype.php
 */
function filetype($filename)
{
}

/**
 * Get float value of a variable
 *
 * @param mixed $var May be any scalar type. <code>floatval</code> should not be used
 * on objects, as doing so will emit an <code>E_NOTICE</code> level
 * error and return 1.
 *
 * @return float The float value of the given variable. Empty arrays return 0, non-empty
 * arrays return 1.
 * Strings will most likely return 0 although this depends on the
 * leftmost characters of the string. The common rules of
 * float casting
 * apply.
 *
 * @since PHP 4 >= 4.2.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.floatval.php
 */
function floatval($var)
{
}

/**
 * Portable advisory file locking
 *
 * @param resource $handle A file system pointer <code>resource</code>
 * that is typically created using <code>fopen</code>.
 * @param int $operation <code>operation</code> is one of the following:
 * <ul>
 * <code>LOCK_SH</code> to acquire a shared lock (reader).
 * <code>LOCK_EX</code> to acquire an exclusive lock (writer).
 * <code>LOCK_UN</code> to release a lock (shared or exclusive).
 * </ul>
 * @param int $wouldblock The optional third argument is set to 1 if the lock would block
 * (EWOULDBLOCK errno condition).
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.flock.php
 */
function flock($handle, $operation, &$wouldblock = null)
{
}

/**
 * Round fractions down
 *
 * @param float $value The numeric value to round
 *
 * @return float <code>value</code> rounded to the next lowest integer.
 * The return value of <code>floor</code> is still of type
 * <code>float</code> because the value range of <code>float</code> is
 * usually bigger than that of <code>integer</code>.
 * This function returns <code>FALSE</code> in case of an error (e.g. passing an array).
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.floor.php
 */
function floor($value)
{
}

/**
 * Flush system output buffer
 *
 * @return void No value is returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.flush.php
 */
function flush()
{
}

/**
 * Returns the floating point remainder (modulo) of the division
 * of the arguments
 *
 * @param float $x The dividend
 * @param float $y The divisor
 *
 * @return float The floating point remainder of
 * <code>x</code>/<code>y</code>
 *
 * @since PHP 4 >= 4.2.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fmod.php
 */
function fmod($x, $y)
{
}

/**
 * Match filename against a pattern
 *
 * @param string $pattern The shell wildcard pattern.
 * @param string $string The tested string. This function is especially useful for filenames,
 * but may also be used on regular strings.
 * @param int|null $flags The value of <code>flags</code> can be any combination of
 * the following flags, joined with the
 * binary OR (|) operator.
 * <table>
 * <title>
 * A list of possible flags for <code>fnmatch</code>
 * </title>
 * <thead>
 * <tr>
 * <td><code>Flag</code></td><td>Description</td>
 * </tr>
 * </thead>
 * <tbody>
 * <tr>
 * <td><code>FNM_NOESCAPE</code></td><td>
 * Disable backslash escaping.
 * </td>
 * </tr>
 * <tr>
 * <td><code>FNM_PATHNAME</code></td><td>
 * Slash in string only matches slash in the given pattern.
 * </td>
 * </tr>
 * <tr>
 * <td><code>FNM_PERIOD</code></td><td>
 * Leading period in string must be exactly matched by period in the given pattern.
 * </td>
 * </tr>
 * <tr>
 * <td><code>FNM_CASEFOLD</code></td><td>
 * Caseless match. Part of the GNU extension.
 * </td>
 * </tr>
 * </tbody>
 * </table>
 *
 * @return bool Returns <code>TRUE</code> if there is a match, <code>FALSE</code> otherwise.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fnmatch.php
 */
function fnmatch($pattern, $string, $flags = 0)
{
}

/**
 * Opens file or URL
 *
 * @param string $filename If <code>filename</code> is of the form "scheme://...", it
 * is assumed to be a URL and PHP will search for a protocol handler
 * (also known as a wrapper) for that scheme. If no wrappers for that
 * protocol are registered, PHP will emit a notice to help you track
 * potential problems in your script and then continue as though
 * <code>filename</code> specifies a regular file.
 * @param string $mode The <code>mode</code> parameter specifies the type of access
 * you require to the stream. It may be any of the following:
 * <table>
 * <title>
 * A list of possible modes for <code>fopen</code>
 * using <code>mode</code>
 * </title>
 * <thead>
 * <tr>
 * <td><code>mode</code></td><td>Description</td>
 * </tr>
 * </thead>
 * <tbody>
 * <tr>
 * <td><code>'r'</code></td><td>
 * Open for reading only; place the file pointer at the
 * beginning of the file.
 * </td>
 * </tr>
 * <tr>
 * <td><code>'r+'</code></td><td>
 * Open for reading and writing; place the file pointer at
 * the beginning of the file.
 * </td>
 * </tr>
 * <tr>
 * <td><code>'w'</code></td><td>
 * Open for writing only; place the file pointer at the
 * beginning of the file and truncate the file to zero length.
 * If the file does not exist, attempt to create it.
 * </td>
 * </tr>
 * <tr>
 * <td><code>'w+'</code></td><td>
 * Open for reading and writing; place the file pointer at
 * the beginning of the file and truncate the file to zero
 * length. If the file does not exist, attempt to create it.
 * </td>
 * </tr>
 * <tr>
 * <td><code>'a'</code></td><td>
 * Open for writing only; place the file pointer at the end of
 * the file. If the file does not exist, attempt to create it.
 * In this mode, <code>fseek</code> has no effect, writes are always appended.
 * </td>
 * </tr>
 * <tr>
 * <td><code>'a+'</code></td><td>
 * Open for reading and writing; place the file pointer at
 * the end of the file. If the file does not exist, attempt to
 * create it. In this mode, <code>fseek</code> only affects
 * the reading position, writes are always appended.
 * </td>
 * </tr>
 * <tr>
 * <td><code>'x'</code></td><td>
 * Create and open for writing only; place the file pointer at the
 * beginning of the file. If the file already exists, the
 * <code>fopen</code> call will fail by returning <code>FALSE</code> and
 * generating an error of level <code>E_WARNING</code>. If
 * the file does not exist, attempt to create it. This is equivalent
 * to specifying <code>O_EXCL|O_CREAT</code> flags for the
 * underlying <code>open(2)</code> system call.
 * </td>
 * </tr>
 * <tr>
 * <td><code>'x+'</code></td><td>
 * Create and open for reading and writing; otherwise it has the
 * same behavior as <code>'x'</code>.
 * </td>
 * </tr>
 * <tr>
 * <td><code>'c'</code></td><td>
 * Open the file for writing only. If the file does not exist, it is
 * created. If it exists, it is neither truncated (as opposed to
 * <code>'w'</code>), nor the call to this function fails (as is
 * the case with <code>'x'</code>). The file pointer is
 * positioned on the beginning of the file. This may be useful if it's
 * desired to get an advisory lock (see <code>flock</code>)
 * before attempting to modify the file, as using
 * <code>'w'</code> could truncate the file before the lock
 * was obtained (if truncation is desired,
 * <code>ftruncate</code> can be used after the lock is
 * requested).
 * </td>
 * </tr>
 * <tr>
 * <td><code>'c+'</code></td><td>
 * Open the file for reading and writing; otherwise it has the same
 * behavior as <code>'c'</code>.
 * </td>
 * </tr>
 * <tr>
 * <td><code>'e'</code></td><td>
 * Set close-on-exec flag on the opened file descriptor. Only
 * available in PHP compiled on POSIX.1-2008 conform systems.
 * </td>
 * </tr>
 * </tbody>
 * </table>
 * @param bool|null $use_include_path The optional third <code>use_include_path</code> parameter
 * can be set to '1' or <code>TRUE</code> if you want to search for the file in the
 * include_path, too.
 * @param resource $context
 *
 * @return resource Returns a file pointer resource on success, or <code>FALSE</code> on error.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fopen.php
 */
function fopen($filename, $mode, $use_include_path = false, $context = null)
{
}

/**
 * Call a static method
 *
 * @param callable $function The function or method to be called. This parameter may be an array,
 * with the name of the class, and the method, or a string, with a function
 * name.
 * @param mixed $parameter Zero or more parameters to be passed to the function.
 *
 * @return mixed Returns the function result, or <code>FALSE</code> on error.
 *
 * @since PHP 5 >= 5.3.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.forward-static-call.php
 */
function forward_static_call(callable $function, ...$parameter)
{
}

/**
 * Call a static method and pass the arguments as array
 *
 * @param callable $function The function or method to be called. This parameter may be an <code>array</code>,
 * with the name of the class, and the method, or a <code>string</code>, with a function
 * name.
 * @param array $parameters
 *
 * @return mixed Returns the function result, or <code>FALSE</code> on error.
 *
 * @since PHP 5 >= 5.3.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.forward-static-call-array.php
 */
function forward_static_call_array(callable $function, array $parameters)
{
}

/**
 * Output all remaining data on a file pointer
 *
 * @param resource $handle The file pointer must be valid, and must point to
 * a file successfully opened by <code>fopen</code> or
 * <code>fsockopen</code> (and not yet closed by
 * <code>fclose</code>).
 *
 * @return int If an error occurs, <code>fpassthru</code> returns
 * <code>FALSE</code>. Otherwise, <code>fpassthru</code> returns
 * the number of characters read from <code>handle</code>
 * and passed through to the output.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fpassthru.php
 */
function fpassthru($handle)
{
}

/**
 * Write a formatted string to a stream
 *
 * @param resource $handle A file system pointer <code>resource</code>
 * that is typically created using <code>fopen</code>.
 * @param string $format See <code>sprintf</code> for a description of
 * <code>format</code>.
 * @param mixed $args
 *
 * @return int Returns the length of the string written.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fprintf.php
 */
function fprintf($handle, $format, ...$args)
{
}

/**
 * Format line as CSV and write to file pointer
 *
 * @param resource $handle The file pointer must be valid, and must point to
 * a file successfully opened by <code>fopen</code> or
 * <code>fsockopen</code> (and not yet closed by
 * <code>fclose</code>).
 * @param array $fields An array of values.
 * @param string|null $delimiter The optional <code>delimiter</code> parameter sets the field
 * delimiter (one character only).
 * @param string|null $enclosure The optional <code>enclosure</code> parameter sets the field
 * enclosure (one character only).
 *
 * @return int Returns the length of the written string or <code>FALSE</code> on failure.
 *
 * @since PHP 5 >= 5.1.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fputcsv.php
 */
function fputcsv($handle, array $fields, $delimiter = ",", $enclosure = '"')
{
}

/**
 * Alias of <code>fwrite</code>
 *
 * @param mixed $fp
 * @param mixed $str
 * @param mixed $length
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fputs.php
 */
function fputs($fp, $str, $length = null)
{
}

/**
 * Binary-safe file read
 *
 * @param resource $handle A file system pointer <code>resource</code>
 * that is typically created using <code>fopen</code>.
 * @param int $length Up to <code>length</code> number of bytes read.
 *
 * @return string Returns the read string or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fread.php
 */
function fread($handle, $length)
{
}

/**
 * Parses input from a file according to a format
 *
 * @param resource $handle A file system pointer <code>resource</code>
 * that is typically created using <code>fopen</code>.
 * @param string $format The specified format as described in the
 * <code>sprintf</code> documentation.
 * @param mixed $vars The optional assigned values.
 *
 * @return mixed If only two parameters were passed to this function, the values parsed will be
 * returned as an array. Otherwise, if optional parameters are passed, the
 * function will return the number of assigned values. The optional
 * parameters must be passed by reference.
 *
 * @since PHP 4 >= 4.0.1, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fscanf.php
 */
function fscanf($handle, $format, &...$vars)
{
}

/**
 * Seeks on a file pointer
 *
 * @param resource $handle A file system pointer <code>resource</code>
 * that is typically created using <code>fopen</code>.
 * @param int $offset The offset.
 * @param int|null $whence <code>whence</code> values are:
 * <ul>
 * <li><code>SEEK_SET</code> - Set position equal to <code>offset</code> bytes.</li>
 * <li><code>SEEK_CUR</code> - Set position to current location plus <code>offset</code>.</li>
 * <li><code>SEEK_END</code> - Set position to end-of-file plus <code>offset</code>.</li>
 * </ul>
 *
 * @return int Upon success, returns 0; otherwise, returns -1.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fseek.php
 */
function fseek($handle, $offset, $whence = SEEK_SET)
{
}

/**
 * Open Internet or Unix domain socket connection
 *
 * @param string $hostname If OpenSSL support is
 * installed, you may prefix the <code>hostname</code>
 * with either <code>ssl://</code> or <code>tls://</code> to
 * use an SSL or TLS client connection over TCP/IP to connect to the
 * remote host.
 * @param int|null $port The port number. This can be omitted and skipped with
 * <code>-1</code> for transports that do not use ports, such as
 * <code>unix://</code>.
 * @param int $errno If provided, holds the system level error number that occurred in the
 * system-level <code>connect()</code> call.
 * @param string $errstr The error message as a string.
 * @param float|null $timeout The connection timeout, in seconds.
 *
 * @return resource <code>fsockopen</code> returns a file pointer which may be used
 * together with the other file functions (such as
 * <code>fgets</code>, <code>fgetss</code>,
 * <code>fwrite</code>, <code>fclose</code>, and
 * <code>feof</code>). If the call fails, it will return <code>FALSE</code>
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fsockopen.php
 */
function fsockopen($hostname, $port = -1, &$errno = null, &$errstr = null, $timeout = /* ini_get("default_socket_timeout") */ null)
{
}

/**
 * Gets information about a file using an open file pointer
 *
 * @param resource $handle A file system pointer <code>resource</code>
 * that is typically created using <code>fopen</code>.
 *
 * @return array Returns an array with the statistics of the file; the format of the array
 * is described in detail on the <code>stat</code> manual page.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fstat.php
 */
function fstat($handle)
{
}

/**
 * Returns the current position of the file read/write pointer
 *
 * @param resource $handle The file pointer must be valid, and must point to a file successfully
 * opened by <code>fopen</code> or <code>popen</code>.
 * <code>ftell</code> gives undefined results for append-only streams
 * (opened with "a" flag).
 *
 * @return int Returns the position of the file pointer referenced by
 * <code>handle</code> as an integer; i.e., its offset into the file stream.
 * If an error occurs, returns <code>FALSE</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ftell.php
 */
function ftell($handle)
{
}

/**
 * Convert a pathname and a project identifier to a System V IPC key
 *
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @param string $pathname Path to an accessible file.
 * @param string $proj Project identifier. This must be a one character string.
 *
 * @return int On success the return value will be the created key value, otherwise
 * <code>-1</code> is returned.
 *
 * @since PHP 4 >= 4.2.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ftok.php
 */
function ftok($pathname, $proj)
{
}

/**
 * Truncates a file to a given length
 *
 * @param resource $handle The file pointer.
 * @param int $size The size to truncate to.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ftruncate.php
 */
function ftruncate($handle, $size)
{
}

/**
 * Binary-safe file write
 *
 * @param resource $handle A file system pointer <code>resource</code>
 * that is typically created using <code>fopen</code>.
 * @param string $string The string that is to be written.
 * @param int $length If the <code>length</code> argument is given, writing will
 * stop after <code>length</code> bytes have been written or
 * the end of <code>string</code> is reached, whichever comes
 * first.
 *
 * @return int
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.fwrite.php
 */
function fwrite($handle, $string, $length = null)
{
}

/**
 * Tells what the user's browser is capable of
 *
 * @param string $user_agent The User Agent to be analyzed. By default, the value of HTTP
 * User-Agent header is used; however, you can alter this (i.e., look up
 * another browser's info) by passing this parameter.
 * @param bool|null $return_array If set to <code>TRUE</code>, this function will return an <code>array</code>
 * instead of an <code>object</code>.
 *
 * @return mixed The information is returned in an object or an array which will contain
 * various data elements representing, for instance, the browser's major and
 * minor version numbers and ID string; <code>TRUE</code>/<code>FALSE</code> values for features
 * such as frames, JavaScript, and cookies; and so forth.
 * The <code>cookies</code> value simply means that the browser
 * itself is capable of accepting cookies and does not mean the user has
 * enabled the browser to accept cookies or not. The only way to test if
 * cookies are accepted is to set one with <code>setcookie</code>,
 * reload, and check for the value.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.get-browser.php
 */
function get_browser($user_agent = null, $return_array = false)
{
}

/**
 * Gets the value of a PHP configuration option
 *
 * @param string $option The configuration option name.
 *
 * @return mixed Returns the current value of the PHP configuration variable specified by
 * <code>option</code>, or <code>FALSE</code> if an error occurs.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.get-cfg-var.php
 */
function get_cfg_var($option)
{
}

/**
 * Gets the name of the owner of the current PHP script
 *
 * @return string Returns the username as a string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.get-current-user.php
 */
function get_current_user()
{
}

/**
 * Fetches all the headers sent by the server in response to a HTTP request
 *
 * @param string $url The target URL.
 * @param int|null $format If the optional <code>format</code> parameter is set to non-zero,
 * <code>get_headers</code> parses the response and sets the
 * array's keys.
 *
 * @return array Returns an indexed or associative array with the headers, or <code>FALSE</code> on
 * failure.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.get-headers.php
 */
function get_headers($url, $format = 0)
{
}

/**
 * Returns the translation table used by <code>htmlspecialchars</code> and <code>htmlentities</code>
 *
 * @param int|null $table Which table to return. Either <code>HTML_ENTITIES</code> or
 * <code>HTML_SPECIALCHARS</code>.
 * @param int|null $flags A bitmask of one or more of the following flags, which specify which quotes the
 * table will contain as well as which document type the table is for. The default is
 * <code>ENT_COMPAT | ENT_HTML401</code>.
 * <table>
 * <title>Available <code>flags</code> constants</title>
 * <thead>
 * <tr>
 * <td>Constant Name</td><td>Description</td>
 * </tr>
 * </thead>
 * <tbody>
 * <tr>
 * <td><code>ENT_COMPAT</code></td><td>Table will contain entities for double-quotes, but not for single-quotes.</td>
 * </tr>
 * <tr>
 * <td><code>ENT_QUOTES</code></td><td>Table will contain entities for both double and single quotes.</td>
 * </tr>
 * <tr>
 * <td><code>ENT_NOQUOTES</code></td><td>Table will neither contain entities for single quotes nor for double quotes.</td>
 * </tr>
 * <tr>
 * <td><code>ENT_HTML401</code></td><td>Table for HTML 4.01.</td>
 * </tr>
 * <tr>
 * <td><code>ENT_XML1</code></td><td>Table for XML 1.</td>
 * </tr>
 * <tr>
 * <td><code>ENT_XHTML</code></td><td>Table for XHTML.</td>
 * </tr>
 * <tr>
 * <td><code>ENT_HTML5</code></td><td>Table for HTML 5.</td>
 * </tr>
 * </tbody>
 * </table>
 *
 * @return array Returns the translation table as an array, with the original characters
 * as keys and entities as values.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.get-html-translation-table.php
 */
function get_html_translation_table($table = HTML_SPECIALCHARS, $flags = ENT_COMPAT | ENT_HTML401)
{
}

/**
 * Gets the current include_path configuration option
 *
 * @return string Returns the path, as a string.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.get-include-path.php
 */
function get_include_path()
{
}

/**
 * Gets the current configuration setting of magic_quotes_gpc
 *
 * @return bool Returns 0 if magic_quotes_gpc is off, 1 otherwise.
 * Or always returns <code>FALSE</code> as of PHP 5.4.0.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.get-magic-quotes-gpc.php
 */
function get_magic_quotes_gpc()
{
}

/**
 * Gets the current active configuration setting of magic_quotes_runtime
 *
 * @return bool Returns 0 if magic_quotes_runtime is off, 1 otherwise.
 * Or always returns <code>FALSE</code> as of PHP 5.4.0.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.get-magic-quotes-runtime.php
 */
function get_magic_quotes_runtime()
{
}

/**
 * Extracts all meta tag content attributes from a file and returns an array
 *
 * @param string $filename The path to the HTML file, as a string. This can be a local file or an
 * <code>URL</code>.
 * @param bool|null $use_include_path Setting <code>use_include_path</code> to <code>TRUE</code> will result
 * in PHP trying to open the file along the standard include path as per
 * the include_path directive.
 * This is used for local files, not URLs.
 *
 * @return array Returns an array with all the parsed meta tags.
 * The value of the name property becomes the key, the value of the content
 * property becomes the value of the returned array, so you can easily use
 * standard array functions to traverse it or access single values.
 * Special characters in the value of the name property are substituted with
 * '_', the rest is converted to lower case. If two meta tags have the same
 * name, only the last one is returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.get-meta-tags.php
 */
function get_meta_tags($filename, $use_include_path = false)
{
}

/**
 * Gets the current working directory
 *
 * @return string Returns the current working directory on success, or <code>FALSE</code> on
 * failure.
 * On some Unix variants, <code>getcwd</code> will return
 * <code>FALSE</code> if any one of the parent directories does not have the
 * readable or search mode set, even if the current directory
 * does. See <code>chmod</code> for more information on
 * modes and permissions.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.getcwd.php
 */
function getcwd()
{
}

/**
 * Gets the value of an environment variable
 *
 * @param string $varname The variable name.
 * @param bool|null $local_only Set to true to only return local environment variables (set by the operating system or putenv).
 *
 * @return string Returns the value of the environment variable
 * <code>varname</code>, or <code>FALSE</code> if the environment
 * variable <code>varname</code> does not exist.
 * If <code>varname</code> is omitted, all environment variables are
 * returned as associative <code>array</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.getenv.php
 */
function getenv($varname, $local_only = false)
{
}

/**
 * Get the Internet host name corresponding to a given IP address
 *
 * @param string $ip_address The host IP address.
 *
 * @return string Returns the host name on success, the unmodified <code>ip_address</code>
 * on failure, or <code>FALSE</code> on malformed input.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.gethostbyaddr.php
 */
function gethostbyaddr($ip_address)
{
}

/**
 * Get the IPv4 address corresponding to a given Internet host name
 *
 * @param string $hostname The host name.
 *
 * @return string Returns the IPv4 address or a string containing the unmodified
 * <code>hostname</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.gethostbyname.php
 */
function gethostbyname($hostname)
{
}

/**
 * Get a list of IPv4 addresses corresponding to a given Internet host
 * name
 *
 * @param string $hostname The host name.
 *
 * @return array Returns an array of IPv4 addresses or <code>FALSE</code> if
 * <code>hostname</code> could not be resolved.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.gethostbynamel.php
 */
function gethostbynamel($hostname)
{
}

/**
 * Gets the host name
 *
 * @return string Returns a string with the hostname on success, otherwise <code>FALSE</code> is
 * returned.
 *
 * @since PHP 5 >= 5.3.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.gethostname.php
 */
function gethostname()
{
}

/**
 * Get the size of an image
 *
 * @param string $filename This parameter specifies the file you wish to retrieve information
 * about. It can reference a local file or (configuration permitting) a
 * remote file using one of the supported streams.
 * @param array $imageinfo This optional parameter allows you to extract some extended
 * information from the image file. Currently, this will return the
 * different <code>JPG</code> APP markers as an associative array.
 * Some programs use these APP markers to embed text information in
 * images. A very common one is to embed
 * IPTC information in the APP13 marker.
 * You can use the <code>iptcparse</code> function to parse the
 * binary APP13 marker into something readable.
 *
 * @return array Returns an array with up to 7 elements. Not all image types will include
 * the <code>channels</code> and <code>bits</code> elements.
 * Index 0 and 1 contains respectively the width and the height of the image.
 * Index 2 is one of the IMAGETYPE_XXX constants indicating
 * the type of the image.
 * Index 3 is a text string with the correct
 * <code>height="yyy" width="xxx"</code> string that can be used
 * directly in an <code>IMG</code> tag.
 * <code>mime</code> is the correspondant MIME type of the image.
 * This information can be used to deliver images with the correct HTTP
 * <code>Content-type</code> header:
 * <blockquote>
 * <title><code>getimagesize</code> and MIME types</title>
 * <blockquote><pre>
 * &lt;?php
 * $size = getimagesize($filename);
 * $fp = fopen($filename, "rb");
 * if ($size &amp;&amp; $fp) {
 * header("Content-type: {$size['mime']}");
 * fpassthru($fp);
 * exit;
 * } else {
 * // error
 * }
 * ?&gt;
 * </pre></blockquote>
 * </blockquote>
 * <code>channels</code> will be 3 for RGB pictures and 4 for CMYK
 * pictures.
 * <code>bits</code> is the number of bits for each color.
 * For some image types, the presence of <code>channels</code> and
 * <code>bits</code> values can be a bit
 * confusing. As an example, <code>GIF</code> always uses 3 channels
 * per pixel, but the number of bits per pixel cannot be calculated for an
 * animated <code>GIF</code> with a global color table.
 * On failure, <code>FALSE</code> is returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.getimagesize.php
 */
function getimagesize($filename, array &$imageinfo = null)
{
}

/**
 * Get the size of an image from a string
 *
 * @param string $imagedata The image data, as a string.
 * @param array $imageinfo See <code>getimagesize</code>.
 *
 * @return array See <code>getimagesize</code>.
 *
 * @since PHP 5 >= 5.4.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.getimagesizefromstring.php
 */
function getimagesizefromstring($imagedata, array &$imageinfo = null)
{
}

/**
 * Gets time of last page modification
 *
 * @return int Returns the time of the last modification of the current
 * page. The value returned is a Unix timestamp, suitable for
 * feeding to <code>date</code>. Returns <code>FALSE</code> on error.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.getlastmod.php
 */
function getlastmod()
{
}

/**
 * Get MX records corresponding to a given Internet host name
 *
 * @param string $hostname The Internet host name.
 * @param array $mxhosts A list of the MX records found is placed into the array
 * <code>mxhosts</code>.
 * @param array $weight If the <code>weight</code> array is given, it will be filled
 * with the weight information gathered.
 *
 * @return bool Returns <code>TRUE</code> if any records are found; returns <code>FALSE</code> if no records
 * were found or if an error occurred.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.getmxrr.php
 */
function getmxrr($hostname, array &$mxhosts, array &$weight = null)
{
}

/**
 * Get PHP script owner's GID
 *
 * @return int Returns the group ID of the current script, or <code>FALSE</code> on error.
 *
 * @since PHP 4 >= 4.1.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.getmygid.php
 */
function getmygid()
{
}

/**
 * Gets the inode of the current script
 *
 * @return int Returns the current script's inode as an integer, or <code>FALSE</code> on error.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.getmyinode.php
 */
function getmyinode()
{
}

/**
 * Gets PHP's process ID
 *
 * @return int Returns the current PHP process ID, or <code>FALSE</code> on error.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.getmypid.php
 */
function getmypid()
{
}

/**
 * Gets PHP script owner's UID
 *
 * @return int Returns the user ID of the current script, or <code>FALSE</code> on error.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.getmyuid.php
 */
function getmyuid()
{
}

/**
 * Gets options from the command line argument list
 *
 * @param string $options
 * @param array $longopts
 *
 * @return array This function will return an array of option / argument pairs, or <code>FALSE</code> on failure.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.getopt.php
 */
function getopt($options, array $longopts = null)
{
}

/**
 * Get protocol number associated with protocol name
 *
 * @param string $name The protocol name.
 *
 * @return int Returns the protocol number, or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.getprotobyname.php
 */
function getprotobyname($name)
{
}

/**
 * Get protocol name associated with protocol number
 *
 * @param int $number The protocol number.
 *
 * @return string Returns the protocol name as a string, or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.getprotobynumber.php
 */
function getprotobynumber($number)
{
}

/**
 * Show largest possible random value
 *
 * @return int The largest possible random value returned by <code>rand</code>
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.getrandmax.php
 */
function getrandmax()
{
}

/**
 * Gets the current resource usages
 *
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @param int|null $who If <code>who</code> is 1, getrusage will be called with
 * <code>RUSAGE_CHILDREN</code>.
 *
 * @return array Returns an associative array containing the data returned from the system
 * call. All entries are accessible by using their documented field names.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.getrusage.php
 */
function getrusage($who = 0)
{
}

/**
 * Get port number associated with an Internet service and protocol
 *
 * @param string $service The Internet service name, as a string.
 * @param string $protocol <code>protocol</code> is either <code>"tcp"</code>
 * or <code>"udp"</code> (in lowercase).
 *
 * @return int Returns the port number, or <code>FALSE</code> if <code>service</code> or
 * <code>protocol</code> is not found.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.getservbyname.php
 */
function getservbyname($service, $protocol)
{
}

/**
 * Get Internet service which corresponds to port and protocol
 *
 * @param int $port The port number.
 * @param string $protocol <code>protocol</code> is either <code>"tcp"</code>
 * or <code>"udp"</code> (in lowercase).
 *
 * @return string Returns the Internet service name as a string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.getservbyport.php
 */
function getservbyport($port, $protocol)
{
}

/**
 * Get current time
 *
 * @param bool|null $return_float When set to <code>TRUE</code>, a float instead of an array is returned.
 *
 * @return mixed By default an <code>array</code> is returned. If <code>return_float</code>
 * is set, then a <code>float</code> is returned.
 * Array keys:
 * <ul>
 * "sec" - seconds since the Unix Epoch
 * "usec" - microseconds
 * "minuteswest" - minutes west of Greenwich
 * "dsttime" - type of dst correction
 * </ul>
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.gettimeofday.php
 */
function gettimeofday($return_float = false)
{
}

/**
 * Get the type of a variable
 *
 * @param mixed $var The variable being type checked.
 *
 * @return string Possible values for the returned string are:
 * <ul>
 * "<code>boolean</code>"
 * "<code>integer</code>"
 * "<code>double</code>" (for historical reasons "double" is
 * returned in case of a <code>float</code>, and not simply
 * "float")
 * "<code>string</code>"
 * "<code>array</code>"
 * "<code>object</code>"
 * "<code>resource</code>"
 * "resource (closed)" as of PHP 7.2.0
 * "<code>NULL</code>"
 * "unknown type"
 * </ul>
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.gettype.php
 */
function gettype($var)
{
}

/**
 * Find pathnames matching a pattern
 *
 * @param string $pattern The pattern. No tilde expansion or parameter substitution is done.
 * @param int|null $flags Valid flags:
 * <ul>
 * <code>GLOB_MARK</code> - Adds a slash to each directory returned
 * <code>GLOB_NOSORT</code> - Return files as they appear in the
 * directory (no sorting). When this flag is not used, the pathnames are
 * sorted alphabetically
 * <code>GLOB_NOCHECK</code> - Return the search pattern if no
 * files matching it were found
 * <code>GLOB_NOESCAPE</code> - Backslashes do not quote
 * metacharacters
 * <code>GLOB_BRACE</code> - Expands {a,b,c} to match 'a', 'b',
 * or 'c'
 * <code>GLOB_ONLYDIR</code> - Return only directory entries
 * which match the pattern
 * <code>GLOB_ERR</code> - Stop on read errors (like unreadable
 * directories), by default errors are ignored.
 * </ul>
 *
 * @return array Returns an array containing the matched files/directories, an empty array
 * if no file matched or <code>FALSE</code> on error.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.glob.php
 */
function glob($pattern, $flags = 0)
{
}

/**
 * Send a raw HTTP header
 *
 * @param string $header The header string.
 * @param bool|null $replace The optional <code>replace</code> parameter indicates
 * whether the header should replace a previous similar header, or
 * add a second header of the same type. By default it will replace,
 * but if you pass in <code>FALSE</code> as the second argument you can force
 * multiple headers of the same type. For example:
 * @param int $http_response_code Forces the HTTP response code to the specified value. Note that this
 * parameter only has an effect if the <code>header</code> is
 * not empty.
 *
 * @return void No value is returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.header.php
 */
function header($header, $replace = true, $http_response_code = null)
{
}

/**
 * Call a header function
 *
 * @param callable $callback Function called just before the headers are sent. It gets no parameters
 * and the return value is ignored.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 5 >= 5.4.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.header-register-callback.php
 */
function header_register_callback(callable $callback)
{
}

/**
 * Remove previously set headers
 *
 * @param string $name The header name to be removed.
 *
 * @return void No value is returned.
 *
 * @since PHP 5 >= 5.3.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.header-remove.php
 */
function header_remove($name = null)
{
}

/**
 * Returns a list of response headers sent (or ready to send)
 *
 * @return array Returns a numerically indexed array of headers.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.headers-list.php
 */
function headers_list()
{
}

/**
 * Checks if or where headers have been sent
 *
 * @param string $file If the optional <code>file</code> and
 * <code>line</code> parameters are set,
 * <code>headers_sent</code> will put the PHP source file name
 * and line number where output started in the <code>file</code>
 * and <code>line</code> variables.
 * @param int $line The line number where the output started.
 *
 * @return bool <code>headers_sent</code> will return <code>FALSE</code> if no HTTP headers
 * have already been sent or <code>TRUE</code> otherwise.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.headers-sent.php
 */
function headers_sent(&$file = null, &$line = null)
{
}

/**
 * Convert logical Hebrew text to visual text
 *
 * @param string $hebrew_text A Hebrew input string.
 * @param int|null $max_chars_per_line This optional parameter indicates maximum number of characters per
 * line that will be returned.
 *
 * @return string Returns the visual string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.hebrev.php
 */
function hebrev($hebrew_text, $max_chars_per_line = 0)
{
}

/**
 * Convert logical Hebrew text to visual text with newline conversion
 *
 * @param string $hebrew_text A Hebrew input string.
 * @param int|null $max_chars_per_line This optional parameter indicates maximum number of characters per
 * line that will be returned.
 *
 * @return string Returns the visual string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.hebrevc.php
 */
function hebrevc($hebrew_text, $max_chars_per_line = 0)
{
}

/**
 * Decodes a hexadecimally encoded binary string
 *
 * @param string $data Hexadecimal representation of data.
 *
 * @return string Returns the binary representation of the given data or <code>FALSE</code> on failure.
 *
 * @since PHP 5 >= 5.4.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.hex2bin.php
 */
function hex2bin($data)
{
}

/**
 * Hexadecimal to decimal
 *
 * @param string $hex_string The hexadecimal string to convert
 *
 * @return float The decimal representation of <code>hex_string</code>
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.hexdec.php
 */
function hexdec($hex_string)
{
}

/**
 * Syntax highlighting of a file
 *
 * @param string $filename Path to the PHP file to be highlighted.
 * @param bool|null $return Set this parameter to <code>TRUE</code> to make this function return the
 * highlighted code.
 *
 * @return mixed If <code>return</code> is set to <code>TRUE</code>, returns the highlighted
 * code as a string instead of printing it out. Otherwise, it will return
 * <code>TRUE</code> on success, <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.highlight-file.php
 */
function highlight_file($filename, $return = false)
{
}

/**
 * Syntax highlighting of a string
 *
 * @param string $str The PHP code to be highlighted. This should include the opening tag.
 * @param bool|null $return Set this parameter to <code>TRUE</code> to make this function return the
 * highlighted code.
 *
 * @return mixed If <code>return</code> is set to <code>TRUE</code>, returns the highlighted
 * code as a string instead of printing it out. Otherwise, it will return
 * <code>TRUE</code> on success, <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.highlight-string.php
 */
function highlight_string($str, $return = false)
{
}

/**
 * Convert all HTML entities to their applicable characters
 *
 * @param string $string The input string.
 * @param int|null $flags A bitmask of one or more of the following flags, which specify how to handle quotes and
 * which document type to use. The default is <code>ENT_COMPAT | ENT_HTML401</code>.
 * <table>
 * <title>Available <code>flags</code> constants</title>
 * <thead>
 * <tr>
 * <td>Constant Name</td><td>Description</td>
 * </tr>
 * </thead>
 * <tbody>
 * <tr>
 * <td><code>ENT_COMPAT</code></td><td>Will convert double-quotes and leave single-quotes alone.</td>
 * </tr>
 * <tr>
 * <td><code>ENT_QUOTES</code></td><td>Will convert both double and single quotes.</td>
 * </tr>
 * <tr>
 * <td><code>ENT_NOQUOTES</code></td><td>Will leave both double and single quotes unconverted.</td>
 * </tr>
 * <tr>
 * <td><code>ENT_HTML401</code></td><td>
 * Handle code as HTML 4.01.
 * </td>
 * </tr>
 * <tr>
 * <td><code>ENT_XML1</code></td><td>
 * Handle code as XML 1.
 * </td>
 * </tr>
 * <tr>
 * <td><code>ENT_XHTML</code></td><td>
 * Handle code as XHTML.
 * </td>
 * </tr>
 * <tr>
 * <td><code>ENT_HTML5</code></td><td>
 * Handle code as HTML 5.
 * </td>
 * </tr>
 * </tbody>
 * </table>
 * @param string|null $encoding An optional argument defining the encoding used when converting characters.
 *
 * @return string Returns the decoded string.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.html-entity-decode.php
 */
function html_entity_decode($string, $flags = ENT_COMPAT | ENT_HTML401, $encoding = /* ini_get("default_charset") */ null)
{
}

/**
 * Convert all applicable characters to HTML entities
 *
 * @param string $string The input string.
 * @param int|null $flags A bitmask of one or more of the following flags, which specify how to handle quotes,
 * invalid code unit sequences and the used document type. The default is
 * <code>ENT_COMPAT | ENT_HTML401</code>.
 * <table>
 * <title>Available <code>flags</code> constants</title>
 * <thead>
 * <tr>
 * <td>Constant Name</td><td>Description</td>
 * </tr>
 * </thead>
 * <tbody>
 * <tr>
 * <td><code>ENT_COMPAT</code></td><td>Will convert double-quotes and leave single-quotes alone.</td>
 * </tr>
 * <tr>
 * <td><code>ENT_QUOTES</code></td><td>Will convert both double and single quotes.</td>
 * </tr>
 * <tr>
 * <td><code>ENT_NOQUOTES</code></td><td>Will leave both double and single quotes unconverted.</td>
 * </tr>
 * <tr>
 * <td><code>ENT_IGNORE</code></td><td>
 * Silently discard invalid code unit sequences instead of returning
 * an empty string. Using this flag is discouraged as it
 * may have security implications.
 * </td>
 * </tr>
 * <tr>
 * <td><code>ENT_SUBSTITUTE</code></td><td>
 * Replace invalid code unit sequences with a Unicode Replacement Character
 * U+FFFD (UTF-8) or &amp;#FFFD; (otherwise) instead of returning an empty string.
 * </td>
 * </tr>
 * <tr>
 * <td><code>ENT_DISALLOWED</code></td><td>
 * Replace invalid code points for the given document type with a
 * Unicode Replacement Character U+FFFD (UTF-8) or &amp;#FFFD;
 * (otherwise) instead of leaving them as is. This may be useful, for
 * instance, to ensure the well-formedness of XML documents with
 * embedded external content.
 * </td>
 * </tr>
 * <tr>
 * <td><code>ENT_HTML401</code></td><td>
 * Handle code as HTML 4.01.
 * </td>
 * </tr>
 * <tr>
 * <td><code>ENT_XML1</code></td><td>
 * Handle code as XML 1.
 * </td>
 * </tr>
 * <tr>
 * <td><code>ENT_XHTML</code></td><td>
 * Handle code as XHTML.
 * </td>
 * </tr>
 * <tr>
 * <td><code>ENT_HTML5</code></td><td>
 * Handle code as HTML 5.
 * </td>
 * </tr>
 * </tbody>
 * </table>
 * @param string|null $encoding An optional argument defining the encoding used when converting characters.
 * @param bool|null $double_encode When <code>double_encode</code> is turned off PHP will not
 * encode existing html entities. The default is to convert everything.
 *
 * @return string Returns the encoded string.
 * If the input <code>string</code> contains an invalid code unit
 * sequence within the given <code>encoding</code> an empty string
 * will be returned, unless either the <code>ENT_IGNORE</code> or
 * <code>ENT_SUBSTITUTE</code> flags are set.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.htmlentities.php
 */
function htmlentities($string, $flags = ENT_COMPAT | ENT_HTML401, $encoding = /* ini_get("default_charset") */ null, $double_encode = true)
{
}

/**
 * Convert special characters to HTML entities
 *
 * @param string $string The <code>string</code> being converted.
 * @param int|null $flags A bitmask of one or more of the following flags, which specify how to handle quotes,
 * invalid code unit sequences and the used document type. The default is
 * <code>ENT_COMPAT | ENT_HTML401</code>.
 * <table>
 * <title>Available <code>flags</code> constants</title>
 * <thead>
 * <tr>
 * <td>Constant Name</td><td>Description</td>
 * </tr>
 * </thead>
 * <tbody>
 * <tr>
 * <td><code>ENT_COMPAT</code></td><td>Will convert double-quotes and leave single-quotes alone.</td>
 * </tr>
 * <tr>
 * <td><code>ENT_QUOTES</code></td><td>Will convert both double and single quotes.</td>
 * </tr>
 * <tr>
 * <td><code>ENT_NOQUOTES</code></td><td>Will leave both double and single quotes unconverted.</td>
 * </tr>
 * <tr>
 * <td><code>ENT_IGNORE</code></td><td>
 * Silently discard invalid code unit sequences instead of returning
 * an empty string. Using this flag is discouraged as it
 * may have security implications.
 * </td>
 * </tr>
 * <tr>
 * <td><code>ENT_SUBSTITUTE</code></td><td>
 * Replace invalid code unit sequences with a Unicode Replacement Character
 * U+FFFD (UTF-8) or &amp;#xFFFD; (otherwise) instead of returning an empty string.
 * </td>
 * </tr>
 * <tr>
 * <td><code>ENT_DISALLOWED</code></td><td>
 * Replace invalid code points for the given document type with a
 * Unicode Replacement Character U+FFFD (UTF-8) or &amp;#xFFFD;
 * (otherwise) instead of leaving them as is. This may be useful, for
 * instance, to ensure the well-formedness of XML documents with
 * embedded external content.
 * </td>
 * </tr>
 * <tr>
 * <td><code>ENT_HTML401</code></td><td>
 * Handle code as HTML 4.01.
 * </td>
 * </tr>
 * <tr>
 * <td><code>ENT_XML1</code></td><td>
 * Handle code as XML 1.
 * </td>
 * </tr>
 * <tr>
 * <td><code>ENT_XHTML</code></td><td>
 * Handle code as XHTML.
 * </td>
 * </tr>
 * <tr>
 * <td><code>ENT_HTML5</code></td><td>
 * Handle code as HTML 5.
 * </td>
 * </tr>
 * </tbody>
 * </table>
 * @param string|null $encoding An optional argument defining the encoding used when converting characters.
 * @param bool|null $double_encode When <code>double_encode</code> is turned off PHP will not
 * encode existing html entities, the default is to convert everything.
 *
 * @return string The converted <code>string</code>.
 * If the input <code>string</code> contains an invalid code unit
 * sequence within the given <code>encoding</code> an empty string
 * will be returned, unless either the <code>ENT_IGNORE</code> or
 * <code>ENT_SUBSTITUTE</code> flags are set.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.htmlspecialchars.php
 */
function htmlspecialchars($string, $flags = ENT_COMPAT | ENT_HTML401, $encoding = /* ini_get("default_charset") */ null, $double_encode = true)
{
}

/**
 * Convert special HTML entities back to characters
 *
 * @param string $string The string to decode.
 * @param int|null $flags A bitmask of one or more of the following flags, which specify how to handle quotes and
 * which document type to use. The default is <code>ENT_COMPAT | ENT_HTML401</code>.
 * <table>
 * <title>Available <code>flags</code> constants</title>
 * <thead>
 * <tr>
 * <td>Constant Name</td><td>Description</td>
 * </tr>
 * </thead>
 * <tbody>
 * <tr>
 * <td><code>ENT_COMPAT</code></td><td>Will convert double-quotes and leave single-quotes alone.</td>
 * </tr>
 * <tr>
 * <td><code>ENT_QUOTES</code></td><td>Will convert both double and single quotes.</td>
 * </tr>
 * <tr>
 * <td><code>ENT_NOQUOTES</code></td><td>Will leave both double and single quotes unconverted.</td>
 * </tr>
 * <tr>
 * <td><code>ENT_HTML401</code></td><td>
 * Handle code as HTML 4.01.
 * </td>
 * </tr>
 * <tr>
 * <td><code>ENT_XML1</code></td><td>
 * Handle code as XML 1.
 * </td>
 * </tr>
 * <tr>
 * <td><code>ENT_XHTML</code></td><td>
 * Handle code as XHTML.
 * </td>
 * </tr>
 * <tr>
 * <td><code>ENT_HTML5</code></td><td>
 * Handle code as HTML 5.
 * </td>
 * </tr>
 * </tbody>
 * </table>
 *
 * @return string Returns the decoded string.
 *
 * @since PHP 5 >= 5.1.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.htmlspecialchars-decode.php
 */
function htmlspecialchars_decode($string, $flags = ENT_COMPAT | ENT_HTML401)
{
}

/**
 * Generate URL-encoded query string
 *
 * @param mixed $query_data May be an array or object containing properties.
 * @param string $numeric_prefix If numeric indices are used in the base array and this parameter is
 * provided, it will be prepended to the numeric index for elements in
 * the base array only.
 * @param string $arg_separator arg_separator.output
 * is used to separate arguments but may be overridden by specifying
 * this parameter.
 * @param int|null $enc_type By default, <code>PHP_QUERY_RFC1738</code>.
 *
 * @return string Returns a URL-encoded string.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.http-build-query.php
 */
function http_build_query($query_data, $numeric_prefix = null, $arg_separator = null, $enc_type = PHP_QUERY_RFC1738)
{
}

/**
 * Get or Set the HTTP response code
 *
 * @param int $response_code The optional <code>response_code</code> will set the response code.
 *
 * @return mixed If <code>response_code</code> is provided, then the previous
 * status code will be returned. If <code>response_code</code> is not
 * provided, then the current status code will be returned. Both of these
 * values will default to a <code>200</code> status code if used in a web
 * server environment.
 * <code>FALSE</code> will be returned if <code>response_code</code> is not
 * provided and it is not invoked in a web server environment (such as from a
 * CLI application). <code>TRUE</code> will be returned if
 * <code>response_code</code> is provided and it is not invoked in a
 * web server environment (but only when no previous response status has been
 * set).
 *
 * @since PHP 5 >= 5.4.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.http-response-code.php
 */
function http_response_code($response_code = null)
{
}

/**
 * Calculate the length of the hypotenuse of a right-angle triangle
 *
 * @param float $x Length of first side
 * @param float $y Length of second side
 *
 * @return float Calculated length of the hypotenuse
 *
 * @since PHP 4 >= 4.1.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.hypot.php
 */
function hypot($x, $y)
{
}

/**
 * Set whether a client disconnect should abort script execution
 *
 * @param bool $value If set, this function will set the ignore_user_abort ini setting
 * to the given <code>value</code>. If not, this function will
 * only return the previous setting without changing it.
 *
 * @return int Returns the previous setting, as an integer.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ignore-user-abort.php
 */
function ignore_user_abort($value = null)
{
}

/**
 * Get file extension for image type
 *
 * @param int $imagetype One of the <code>IMAGETYPE_XXX</code> constant.
 * @param bool|null $include_dot Whether to prepend a dot to the extension or not. Default to <code>TRUE</code>.
 *
 * @return string A string with the extension corresponding to the given image type.
 *
 * @since PHP 5 >= 5.2.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.image-type-to-extension.php
 */
function image_type_to_extension($imagetype, $include_dot = true)
{
}

/**
 * Get Mime-Type for image-type returned by getimagesize,
 * exif_read_data, exif_thumbnail, exif_imagetype
 *
 * @param int $imagetype One of the <code>IMAGETYPE_XXX</code> constants.
 *
 * @return string The returned values are as follows
 * <table>
 * <title>Returned values Constants</title>
 * <thead>
 * <tr>
 * <td><code>imagetype</code></td><td>Returned value</td>
 * </tr>
 * </thead>
 * <tbody>
 * <tr>
 * <td><code>IMAGETYPE_GIF</code></td><td><code>image/gif</code></td>
 * </tr>
 * <tr>
 * <td><code>IMAGETYPE_JPEG</code></td><td><code>image/jpeg</code></td>
 * </tr>
 * <tr>
 * <td><code>IMAGETYPE_PNG</code></td><td><code>image/png</code></td>
 * </tr>
 * <tr>
 * <td><code>IMAGETYPE_SWF</code></td><td><code>application/x-shockwave-flash</code></td>
 * </tr>
 * <tr>
 * <td><code>IMAGETYPE_PSD</code></td><td><code>image/psd</code></td>
 * </tr>
 * <tr>
 * <td><code>IMAGETYPE_BMP</code></td><td><code>image/bmp</code></td>
 * </tr>
 * <tr>
 * <td><code>IMAGETYPE_TIFF_II</code> (intel byte order)</td><td><code>image/tiff</code></td>
 * </tr>
 * <tr>
 * <td>
 * <code>IMAGETYPE_TIFF_MM</code> (motorola byte order)
 * </td><td><code>image/tiff</code></td>
 * </tr>
 * <tr>
 * <td><code>IMAGETYPE_JPC</code></td><td><code>application/octet-stream</code></td>
 * </tr>
 * <tr>
 * <td><code>IMAGETYPE_JP2</code></td><td><code>image/jp2</code></td>
 * </tr>
 * <tr>
 * <td><code>IMAGETYPE_JPX</code></td><td><code>application/octet-stream</code></td>
 * </tr>
 * <tr>
 * <td><code>IMAGETYPE_JB2</code></td><td><code>application/octet-stream</code></td>
 * </tr>
 * <tr>
 * <td><code>IMAGETYPE_SWC</code></td><td><code>application/x-shockwave-flash</code></td>
 * </tr>
 * <tr>
 * <td><code>IMAGETYPE_IFF</code></td><td><code>image/iff</code></td>
 * </tr>
 * <tr>
 * <td><code>IMAGETYPE_WBMP</code></td><td><code>image/vnd.wap.wbmp</code></td>
 * </tr>
 * <tr>
 * <td><code>IMAGETYPE_XBM</code></td><td><code>image/xbm</code></td>
 * </tr>
 * <tr>
 * <td><code>IMAGETYPE_ICO</code></td><td><code>image/vnd.microsoft.icon</code></td>
 * </tr>
 * <tr>
 * <td><code>IMAGETYPE_WEBP</code></td><td><code>image/webp</code></td>
 * </tr>
 * </tbody>
 * </table>
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.image-type-to-mime-type.php
 */
function image_type_to_mime_type($imagetype)
{
}

/**
 * Join array elements with a string
 *
 * @param string $glue Defaults to an empty string.
 * @param array $pieces The array of strings to implode.
 *
 * @return string Returns a string containing a string representation of all the array
 * elements in the same order, with the glue string between each element.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.implode.php
 */
function implode($glue, array $pieces)
{
}

/**
 * Checks if a value exists in an array
 *
 * @param mixed $needle The searched value.
 * @param array $haystack The array.
 * @param bool|null $strict If the third parameter <code>strict</code> is set to <code>TRUE</code>
 * then the <code>in_array</code> function will also check the
 * types of the
 * <code>needle</code> in the <code>haystack</code>.
 *
 * @return bool Returns <code>TRUE</code> if <code>needle</code> is found in the array,
 * <code>FALSE</code> otherwise.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.in-array.php
 */
function in_array($needle, array $haystack, $strict = false)
{
}

/**
 * Converts a packed internet address to a human readable representation
 *
 * @param string $in_addr A 32bit IPv4, or 128bit IPv6 address.
 *
 * @return string Returns a string representation of the address or <code>FALSE</code> on failure.
 *
 * @since PHP 5 >= 5.1.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.inet-ntop.php
 */
function inet_ntop($in_addr)
{
}

/**
 * Converts a human readable IP address to its packed in_addr representation
 *
 * @param string $address A human readable IPv4 or IPv6 address.
 *
 * @return string Returns the <code>in_addr</code> representation of the given
 * <code>address</code>, or <code>FALSE</code> if a syntactically invalid
 * <code>address</code> is given (for example, an IPv4 address
 * without dots or an IPv6 address without colons).
 *
 * @since PHP 5 >= 5.1.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.inet-pton.php
 */
function inet_pton($address)
{
}

/**
 * Alias of <code>ini_set</code>
 *
 * @param mixed $varname
 * @param mixed $newvalue
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ini-alter.php
 */
function ini_alter($varname, $newvalue)
{
}

/**
 * Gets the value of a configuration option
 *
 * @param string $varname The configuration option name.
 *
 * @return string Returns the value of the configuration option as a string on success, or an
 * empty string for <code>null</code> values. Returns <code>FALSE</code> if the
 * configuration option doesn't exist.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ini-get.php
 */
function ini_get($varname)
{
}

/**
 * Gets all configuration options
 *
 * @param string $extension An optional extension name. If set, the function return only options
 * specific for that extension.
 *
 * @return array Returns an associative array with directive name as the array key.
 * When <code>details</code> is <code>TRUE</code> (default) the array will
 * contain <code>global_value</code> (set in
 * <code>php.ini</code>), <code>local_value</code> (perhaps set with
 * <code>ini_set</code> or <code>.htaccess</code>), and
 * <code>access</code> (the access level).
 * When <code>details</code> is <code>FALSE</code> the value will be the
 * current value of the option.
 * See the manual section
 * for information on what access levels mean.
 *
 * @since PHP 4 >= 4.2.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ini-get-all.php
 */
function ini_get_all($extension = null)
{
}

/**
 * Restores the value of a configuration option
 *
 * @param string $varname The configuration option name.
 *
 * @return void No value is returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ini-restore.php
 */
function ini_restore($varname)
{
}

/**
 * Sets the value of a configuration option
 *
 * @param string $varname
 * @param string $newvalue The new value for the option.
 *
 * @return string Returns the old value on success, <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ini-set.php
 */
function ini_set($varname, $newvalue)
{
}

/**
 * Get the integer value of a variable
 *
 * @param mixed $var The scalar value being converted to an integer
 * @param int|null $base The base for the conversion
 *
 * @return int The integer value of <code>var</code> on success, or 0 on
 * failure. Empty arrays return 0, non-empty arrays return 1.
 * The maximum value depends on the system. 32 bit systems have a
 * maximum signed integer range of -2147483648 to 2147483647. So for example
 * on such a system, <code>intval('1000000000000')</code> will return
 * 2147483647. The maximum signed integer value for 64 bit systems is
 * 9223372036854775807.
 * Strings will most likely return 0 although this depends on the
 * leftmost characters of the string. The common rules of
 * integer casting
 * apply.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.intval.php
 */
function intval($var, $base = 10)
{
}

/**
 * Converts a string containing an (IPv4) Internet Protocol dotted address into a long integer
 *
 * @param string $ip_address A standard format address.
 *
 * @return int Returns the long integer or <code>FALSE</code> if <code>ip_address</code>
 * is invalid.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ip2long.php
 */
function ip2long($ip_address)
{
}

/**
 * Embeds binary IPTC data into a JPEG image
 *
 * @param string $iptcdata The data to be written.
 * @param string $jpeg_file_name Path to the JPEG image.
 * @param int|null $spool Spool flag. If the spool flag is less than 2 then the JPEG will be
 * returned as a string. Otherwise the JPEG will be printed to STDOUT.
 *
 * @return mixed If <code>spool</code> is less than 2, the JPEG will be returned,
 * or <code>FALSE</code> on failure. Otherwise returns <code>TRUE</code> on success
 * or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.iptcembed.php
 */
function iptcembed($iptcdata, $jpeg_file_name, $spool = 0)
{
}

/**
 * Parse a binary IPTC block into single tags
 *
 * @param string $iptcblock A binary IPTC block.
 *
 * @return array Returns an array using the tagmarker as an index and the value as the
 * value. It returns <code>FALSE</code> on error or if no IPTC data was found.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.iptcparse.php
 */
function iptcparse($iptcblock)
{
}

/**
 * Finds whether a variable is an array
 *
 * @param mixed $var The variable being evaluated.
 *
 * @return bool Returns <code>TRUE</code> if <code>var</code> is an <code>array</code>,
 * <code>FALSE</code> otherwise.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-array.php
 */
function is_array($var)
{
}

/**
 * Finds out whether a variable is a boolean
 *
 * @param mixed $var The variable being evaluated.
 *
 * @return bool Returns <code>TRUE</code> if <code>var</code> is a <code>boolean</code>,
 * <code>FALSE</code> otherwise.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-bool.php
 */
function is_bool($var)
{
}

/**
 * Verify that the contents of a variable can be called as a function
 *
 * @param mixed $var The value to check
 * @param bool|null $syntax_only If set to <code>TRUE</code> the function only verifies that
 * <code>name</code> might be a function or method. It will only
 * reject simple variables that are not strings, or an array that does
 * not have a valid structure to be used as a callback. The valid ones
 * are supposed to have only 2 entries, the first of which is an object
 * or a string, and the second a string.
 * @param string $callable_name Receives the "callable name". In the example below it is
 * "someClass::someMethod". Note, however, that despite the implication
 * that someClass::SomeMethod() is a callable static method, this is not
 * the case.
 *
 * @return bool Returns <code>TRUE</code> if <code>var</code> is callable, <code>FALSE</code>
 * otherwise.
 *
 * @since PHP 4 >= 4.0.6, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-callable.php
 */
function is_callable($var, $syntax_only = false, &$callable_name = null)
{
}

/**
 * Tells whether the filename is a directory
 *
 * @param string $filename Path to the file. If <code>filename</code> is a relative
 * filename, it will be checked relative to the current working
 * directory. If <code>filename</code> is a symbolic or hard link
 * then the link will be resolved and checked. If you have enabled safe mode,
 * or open_basedir further
 * restrictions may apply.
 *
 * @return bool Returns <code>TRUE</code> if the filename exists and is a directory, <code>FALSE</code>
 * otherwise.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-dir.php
 */
function is_dir($filename)
{
}

/**
 * Alias of <code>is_float</code>
 *
 * @param mixed $var
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-double.php
 */
function is_double($var)
{
}

/**
 * Tells whether the filename is executable
 *
 * @param string $filename Path to the file.
 *
 * @return bool Returns <code>TRUE</code> if the filename exists and is executable, or <code>FALSE</code> on
 * error.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-executable.php
 */
function is_executable($filename)
{
}

/**
 * Tells whether the filename is a regular file
 *
 * @param string $filename Path to the file.
 *
 * @return bool Returns <code>TRUE</code> if the filename exists and is a regular file, <code>FALSE</code>
 * otherwise.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-file.php
 */
function is_file($filename)
{
}

/**
 * Finds whether a value is a legal finite number
 *
 * @param float $val The value to check
 *
 * @return bool <code>TRUE</code> if <code>val</code> is a legal finite
 * number within the allowed range for a PHP float on this platform,
 * else <code>FALSE</code>.
 *
 * @since PHP 4 >= 4.2.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-finite.php
 */
function is_finite($val)
{
}

/**
 * Finds whether the type of a variable is float
 *
 * @param mixed $var The variable being evaluated.
 *
 * @return bool Returns <code>TRUE</code> if <code>var</code> is a <code>float</code>,
 * <code>FALSE</code> otherwise.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-float.php
 */
function is_float($var)
{
}

/**
 * Finds whether a value is infinite
 *
 * @param float $val The value to check
 *
 * @return bool <code>TRUE</code> if <code>val</code> is infinite, else <code>FALSE</code>.
 *
 * @since PHP 4 >= 4.2.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-infinite.php
 */
function is_infinite($val)
{
}

/**
 * Find whether the type of a variable is integer
 *
 * @param mixed $var The variable being evaluated.
 *
 * @return bool Returns <code>TRUE</code> if <code>var</code> is an <code>integer</code>,
 * <code>FALSE</code> otherwise.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-int.php
 */
function is_int($var)
{
}

/**
 * Alias of <code>is_int</code>
 *
 * @param mixed $var
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-integer.php
 */
function is_integer($var)
{
}

/**
 * Tells whether the filename is a symbolic link
 *
 * @param string $filename Path to the file.
 *
 * @return bool Returns <code>TRUE</code> if the filename exists and is a symbolic link, <code>FALSE</code>
 * otherwise.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-link.php
 */
function is_link($filename)
{
}

/**
 * Alias of <code>is_int</code>
 *
 * @param mixed $var
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-long.php
 */
function is_long($var)
{
}

/**
 * Finds whether a value is not a number
 *
 * @param float $val The value to check
 *
 * @return bool Returns <code>TRUE</code> if <code>val</code> is 'not a number',
 * else <code>FALSE</code>.
 *
 * @since PHP 4 >= 4.2.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-nan.php
 */
function is_nan($val)
{
}

/**
 * Finds whether a variable is <code>NULL</code>
 *
 * @param mixed $var The variable being evaluated.
 *
 * @return bool Returns <code>TRUE</code> if <code>var</code> is <code>null</code>, <code>FALSE</code>
 * otherwise.
 *
 * @since PHP 4 >= 4.0.4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-null.php
 */
function is_null($var)
{
}

/**
 * Finds whether a variable is a number or a numeric string
 *
 * @param mixed $var The variable being evaluated.
 *
 * @return bool Returns <code>TRUE</code> if <code>var</code> is a number or a numeric
 * string, <code>FALSE</code> otherwise.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-numeric.php
 */
function is_numeric($var)
{
}

/**
 * Finds whether a variable is an object
 *
 * @param mixed $var The variable being evaluated.
 *
 * @return bool Returns <code>TRUE</code> if <code>var</code> is an <code>object</code>,
 * <code>FALSE</code> otherwise.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-object.php
 */
function is_object($var)
{
}

/**
 * Tells whether a file exists and is readable
 *
 * @param string $filename Path to the file.
 *
 * @return bool Returns <code>TRUE</code> if the file or directory specified by
 * <code>filename</code> exists and is readable, <code>FALSE</code> otherwise.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-readable.php
 */
function is_readable($filename)
{
}

/**
 * Alias of <code>is_float</code>
 *
 * @param mixed $var
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-real.php
 */
function is_real($var)
{
}

/**
 * Finds whether a variable is a resource
 *
 * @param mixed $var The variable being evaluated.
 *
 * @return bool Returns <code>TRUE</code> if <code>var</code> is a <code>resource</code>,
 * <code>FALSE</code> otherwise.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-resource.php
 */
function is_resource($var)
{
}

/**
 * Finds whether a variable is a scalar
 *
 * @param mixed $var The variable being evaluated.
 *
 * @return bool Returns <code>TRUE</code> if <code>var</code> is a scalar, <code>FALSE</code>
 * otherwise.
 *
 * @since PHP 4 >= 4.0.5, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-scalar.php
 */
function is_scalar($var)
{
}

/**
 * Find whether the type of a variable is string
 *
 * @param mixed $var The variable being evaluated.
 *
 * @return bool Returns <code>TRUE</code> if <code>var</code> is of type <code>string</code>,
 * <code>FALSE</code> otherwise.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-string.php
 */
function is_string($var)
{
}

/**
 * Tells whether the file was uploaded via HTTP POST
 *
 * @param string $filename The filename being checked.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4 >= 4.0.3, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-uploaded-file.php
 */
function is_uploaded_file($filename)
{
}

/**
 * Tells whether the filename is writable
 *
 * @param string $filename The filename being checked.
 *
 * @return bool Returns <code>TRUE</code> if the <code>filename</code> exists and is
 * writable.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-writable.php
 */
function is_writable($filename)
{
}

/**
 * Alias of <code>is_writable</code>
 *
 * @param mixed $filename
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.is-writeable.php
 */
function is_writeable($filename)
{
}

/**
 * Alias of <code>implode</code>
 *
 * @param mixed $glue
 * @param mixed $pieces
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.join.php
 */
function join($glue, $pieces)
{
}

/**
 * Fetch a key from an array
 *
 * @param array $array The array.
 *
 * @return mixed The <code>key</code> function simply returns the
 * key of the array element that's currently being pointed to by the
 * internal pointer. It does not move the pointer in any way. If the
 * internal pointer points beyond the end of the elements list or the array is
 * empty, <code>key</code> returns <code>NULL</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.key.php
 */
function key(array &$array)
{
}

/**
 * Alias of <code>array_key_exists</code>
 *
 * @param mixed $key
 * @param mixed $search
 *
 * @since PHP 4 >= 4.0.6, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.key-exists.php
 */
function key_exists($key, $search)
{
}

/**
 * Sort an array by key in reverse order
 *
 * @param array $array The input array.
 * @param int|null $sort_flags You may modify the behavior of the sort using the optional parameter
 * <code>sort_flags</code>, for details see
 * <code>sort</code>.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.krsort.php
 */
function krsort(array &$array, $sort_flags = SORT_REGULAR)
{
}

/**
 * Sort an array by key
 *
 * @param array $array The input array.
 * @param int|null $sort_flags You may modify the behavior of the sort using the optional
 * parameter <code>sort_flags</code>, for details
 * see <code>sort</code>.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ksort.php
 */
function ksort(array &$array, $sort_flags = SORT_REGULAR)
{
}

/**
 * Make a string's first character lowercase
 *
 * @param string $str The input string.
 *
 * @return string Returns the resulting string.
 *
 * @since PHP 5 >= 5.3.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.lcfirst.php
 */
function lcfirst($str)
{
}

/**
 * Combined linear congruential generator
 *
 * @return float A pseudo random float value between 0.0 and 1.0, inclusive.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.lcg-value.php
 */
function lcg_value()
{
}

/**
 * Changes group ownership of symlink
 *
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @param string $filename Path to the symlink.
 * @param mixed $group The group specified by name or number.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 5 >= 5.1.3, PHP 7
 *
 * @link http://www.php.net/manual/en/function.lchgrp.php
 */
function lchgrp($filename, $group)
{
}

/**
 * Changes user ownership of symlink
 *
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @param string $filename Path to the file.
 * @param mixed $user User name or number.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 5 >= 5.1.3, PHP 7
 *
 * @link http://www.php.net/manual/en/function.lchown.php
 */
function lchown($filename, $user)
{
}

/**
 * Calculate Levenshtein distance between two strings
 *
 * @param string $str1 One of the strings being evaluated for Levenshtein distance.
 * @param string $str2 One of the strings being evaluated for Levenshtein distance.
 * @param mixed $cost_ins
 * @param mixed $cost_rep
 * @param mixed $cost_del
 *
 * @return int This function returns the Levenshtein-Distance between the
 * two argument strings or -1, if one of the argument strings
 * is longer than the limit of 255 characters.
 *
 * @since PHP 4 >= 4.0.1, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.levenshtein.php
 */
function levenshtein($str1, $str2, $cost_ins = null, $cost_rep = null, $cost_del = null)
{
}

/**
 * Create a hard link
 *
 * @param string $target Target of the link.
 * @param string $link The link name.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.link.php
 */
function link($target, $link)
{
}

/**
 * Gets information about a link
 *
 * @param string $path Path to the link.
 *
 * @return int <code>linkinfo</code> returns the <code>st_dev</code> field
 * of the Unix C stat structure returned by the <code>lstat</code>
 * system call. Returns 0 or <code>FALSE</code> in case of error.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.linkinfo.php
 */
function linkinfo($path)
{
}

/**
 * Get numeric formatting information
 *
 * @return array <code>localeconv</code> returns data based upon the current locale
 * as set by <code>setlocale</code>. The associative array that is
 * returned contains the following fields:
 * <table>
 * <thead>
 * <tr>
 * <td>Array element</td><td>Description</td>
 * </tr>
 * </thead>
 * <tbody>
 * <tr>
 * <td>decimal_point</td><td>Decimal point character</td>
 * </tr>
 * <tr>
 * <td>thousands_sep</td><td>Thousands separator</td>
 * </tr>
 * <tr>
 * <td>grouping</td><td>Array containing numeric groupings</td>
 * </tr>
 * <tr>
 * <td>int_curr_symbol</td><td>International currency symbol (i.e. USD)</td>
 * </tr>
 * <tr>
 * <td>currency_symbol</td><td>Local currency symbol (i.e. $)</td>
 * </tr>
 * <tr>
 * <td>mon_decimal_point</td><td>Monetary decimal point character</td>
 * </tr>
 * <tr>
 * <td>mon_thousands_sep</td><td>Monetary thousands separator</td>
 * </tr>
 * <tr>
 * <td>mon_grouping</td><td>Array containing monetary groupings</td>
 * </tr>
 * <tr>
 * <td>positive_sign</td><td>Sign for positive values</td>
 * </tr>
 * <tr>
 * <td>negative_sign</td><td>Sign for negative values</td>
 * </tr>
 * <tr>
 * <td>int_frac_digits</td><td>International fractional digits</td>
 * </tr>
 * <tr>
 * <td>frac_digits</td><td>Local fractional digits</td>
 * </tr>
 * <tr>
 * <td>p_cs_precedes</td><td>
 * <code>TRUE</code> if currency_symbol precedes a positive value, <code>FALSE</code>
 * if it succeeds one
 * </td>
 * </tr>
 * <tr>
 * <td>p_sep_by_space</td><td>
 * <code>TRUE</code> if a space separates currency_symbol from a positive
 * value, <code>FALSE</code> otherwise
 * </td>
 * </tr>
 * <tr>
 * <td>n_cs_precedes</td><td>
 * <code>TRUE</code> if currency_symbol precedes a negative value, <code>FALSE</code>
 * if it succeeds one
 * </td>
 * </tr>
 * <tr>
 * <td>n_sep_by_space</td><td>
 * <code>TRUE</code> if a space separates currency_symbol from a negative
 * value, <code>FALSE</code> otherwise
 * </td>
 * </tr>
 * <tr valign="top">
 * <td>p_sign_posn</td><td>
 * <ul>
 * <li>0 - Parentheses surround the quantity and currency_symbol</li>
 * <li>1 - The sign string precedes the quantity and currency_symbol</li>
 * <li>2 - The sign string succeeds the quantity and currency_symbol</li>
 * <li>3 - The sign string immediately precedes the currency_symbol</li>
 * <li>4 - The sign string immediately succeeds the currency_symbol</li>
 * </ul>
 * </td>
 * </tr>
 * <tr valign="top">
 * <td>n_sign_posn</td><td>
 * <ul>
 * <li>0 - Parentheses surround the quantity and currency_symbol</li>
 * <li>1 - The sign string precedes the quantity and currency_symbol</li>
 * <li>2 - The sign string succeeds the quantity and currency_symbol</li>
 * <li>3 - The sign string immediately precedes the currency_symbol</li>
 * <li>4 - The sign string immediately succeeds the currency_symbol</li>
 * </ul>
 * </td>
 * </tr>
 * </tbody>
 * </table>
 * The <code>p_sign_posn</code>, and <code>n_sign_posn</code> contain a string
 * of formatting options. Each number representing one of the above listed conditions.
 * The grouping fields contain arrays that define the way numbers should be
 * grouped. For example, the monetary grouping field for the nl_NL locale (in
 * UTF-8 mode with the euro sign), would contain a 2 item array with the
 * values 3 and 3. The higher the index in the array, the farther left the
 * grouping is. If an array element is equal to <code>CHAR_MAX</code>,
 * no further grouping is done. If an array element is equal to 0, the previous
 * element should be used.
 *
 * @since PHP 4 >= 4.0.5, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.localeconv.php
 */
function localeconv()
{
}

/**
 * Natural logarithm
 *
 * @param float $arg The value to calculate the logarithm for
 * @param float|null $base The optional logarithmic base to use
 * (defaults to 'e' and so to the natural logarithm).
 *
 * @return float The logarithm of <code>arg</code> to
 * <code>base</code>, if given, or the
 * natural logarithm.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.log.php
 */
function log($arg, $base = M_E)
{
}

/**
 * Base-10 logarithm
 *
 * @param float $arg The argument to process
 *
 * @return float The base-10 logarithm of <code>arg</code>
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.log10.php
 */
function log10($arg)
{
}

/**
 * Returns log(1 + number), computed in a way that is accurate even when
 * the value of number is close to zero
 *
 * @param float $number The argument to process
 *
 * @return float log(1 + <code>number</code>)
 *
 * @since PHP 4 >= 4.1.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.log1p.php
 */
function log1p($number)
{
}

/**
 * Converts an long integer address into a string in (IPv4) Internet standard dotted format
 *
 * @param int $proper_address A proper address representation in long integer.
 *
 * @return string Returns the Internet IP address as a string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.long2ip.php
 */
function long2ip($proper_address)
{
}

/**
 * Gives information about a file or symbolic link
 *
 * @param string $filename Path to a file or a symbolic link.
 *
 * @return array See the manual page for <code>stat</code> for information on
 * the structure of the array that <code>lstat</code> returns.
 * This function is identical to the <code>stat</code> function
 * except that if the <code>filename</code> parameter is a symbolic
 * link, the status of the symbolic link is returned, not the status of the
 * file pointed to by the symbolic link.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.lstat.php
 */
function lstat($filename)
{
}

/**
 * Strip whitespace (or other characters) from the beginning of a string
 *
 * @param string $str The input string.
 * @param string $character_mask You can also specify the characters you want to strip, by means of the
 * <code>character_mask</code> parameter.
 * Simply list all characters that you want to be stripped. With
 * <code>..</code> you can specify a range of characters.
 *
 * @return string This function returns a string with whitespace stripped from the
 * beginning of <code>str</code>.
 * Without the second parameter,
 * <code>ltrim</code> will strip these characters:
 * <ul>
 * " " (<code>ASCII</code> <code>32</code>
 * (<code>0x20</code>)), an ordinary space.
 * "\t" (<code>ASCII</code> <code>9</code>
 * (<code>0x09</code>)), a tab.
 * "\n" (<code>ASCII</code> <code>10</code>
 * (<code>0x0A</code>)), a new line (line feed).
 * "\r" (<code>ASCII</code> <code>13</code>
 * (<code>0x0D</code>)), a carriage return.
 * "\0" (<code>ASCII</code> <code>0</code>
 * (<code>0x00</code>)), the <code>NUL</code>-byte.
 * <!-- not \v, since not supported by PHP -->
 * "\x0B" (<code>ASCII</code> <code>11</code>
 * (<code>0x0B</code>)), a vertical tab.
 * </ul>
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ltrim.php
 */
function ltrim($str, $character_mask = null)
{
}

/**
 * Alias of <code>set_magic_quotes_runtime</code>
 *
 * @param mixed $new_setting
 *
 * @since PHP 4, PHP 5
 *
 * @deprecated as of PHP 5.3.0
 *
 * @link http://www.php.net/manual/en/function.magic-quotes-runtime.php
 */
function magic_quotes_runtime($new_setting)
{
}

/**
 * Send mail
 *
 * @param string $to Receiver, or receivers of the mail.
 * @param string $subject Subject of the email to be sent.
 * @param string $message Message to be sent.
 * @param mixed $additional_headers <code>String</code> or <code>array</code> to be inserted at the end of the email header.
 * @param string $additional_parameters The <code>additional_parameters</code> parameter
 * can be used to pass additional flags as command line options to the
 * program configured to be used when sending mail, as defined by the
 * <code>sendmail_path</code> configuration setting. For example,
 * this can be used to set the envelope sender address when using
 * sendmail with the <code>-f</code> sendmail option.
 *
 * @return bool Returns <code>TRUE</code> if the mail was successfully accepted for delivery, <code>FALSE</code> otherwise.
 * It is important to note that just because the mail was accepted for delivery,
 * it does NOT mean the mail will actually reach the intended destination.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.mail.php
 */
function mail($to, $subject, $message, $additional_headers = null, $additional_parameters = null)
{
}

/**
 * Find highest value
 *
 * @param array $values An array containing the values.
 *
 * @return mixed <code>max</code> returns the parameter value considered "highest" according to standard
 * comparisons. If multiple values of different types evaluate as equal (e.g. <code>0</code>
 * and <code>'abc'</code>) the first provided to the function will be returned.
 * If an empty array is passed, then <code>FALSE</code> will be returned and an
 * <code>E_WARNING</code> error will be emitted.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.max.php
 */
function max(array ...$values)
{
}

/**
 * Calculate the md5 hash of a string
 *
 * @param string $str The string.
 * @param bool|null $raw_output If the optional <code>raw_output</code> is set to <code>TRUE</code>,
 * then the md5 digest is instead returned in raw binary format with a
 * length of 16.
 *
 * @return string Returns the hash as a 32-character hexadecimal number.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.md5.php
 */
function md5($str, $raw_output = false)
{
}

/**
 * Calculates the md5 hash of a given file
 *
 * @param string $filename The filename
 * @param bool|null $raw_output When <code>TRUE</code>, returns the digest in raw binary format with a length of
 * 16.
 *
 * @return string Returns a string on success, <code>FALSE</code> otherwise.
 *
 * @since PHP 4 >= 4.2.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.md5-file.php
 */
function md5_file($filename, $raw_output = false)
{
}

/**
 * Returns the peak of memory allocated by PHP
 *
 * @param bool|null $real_usage Set this to <code>TRUE</code> to get the real size of memory allocated from
 * system. If not set or <code>FALSE</code> only the memory used by
 * <code>emalloc()</code> is reported.
 *
 * @return int Returns the memory peak in bytes.
 *
 * @since PHP 5 >= 5.2.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.memory-get-peak-usage.php
 */
function memory_get_peak_usage($real_usage = false)
{
}

/**
 * Returns the amount of memory allocated to PHP
 *
 * @param bool|null $real_usage Set this to <code>TRUE</code> to get total memory allocated from
 * system, including unused pages.
 * If not set or <code>FALSE</code> only the used memory is reported.
 *
 * @return int Returns the memory amount in bytes.
 *
 * @since PHP 4 >= 4.3.2, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.memory-get-usage.php
 */
function memory_get_usage($real_usage = false)
{
}

/**
 * Calculate the metaphone key of a string
 *
 * @param string $str The input string.
 * @param int|null $phonemes This parameter restricts the returned metaphone key to
 * <code>phonemes</code> characters in length.
 * The default value of <code>0</code> means no restriction.
 *
 * @return string Returns the metaphone key as a string, or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.metaphone.php
 */
function metaphone($str, $phonemes = 0)
{
}

/**
 * Return current Unix timestamp with microseconds
 *
 * @param bool|null $get_as_float If used and set to <code>TRUE</code>, <code>microtime</code> will return a
 * <code>float</code> instead of a <code>string</code>, as described in
 * the return values section below.
 *
 * @return mixed By default, <code>microtime</code> returns a <code>string</code> in
 * the form "msec sec", where <code>sec</code> is the number of seconds
 * since the Unix epoch (0:00:00 January 1,1970 GMT), and <code>msec</code>
 * measures microseconds that have elapsed since <code>sec</code>
 * and is also expressed in seconds.
 * If <code>get_as_float</code> is set to <code>TRUE</code>, then
 * <code>microtime</code> returns a <code>float</code>, which
 * represents the current time in seconds since the Unix epoch accurate to the
 * nearest microsecond.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.microtime.php
 */
function microtime($get_as_float = false)
{
}

/**
 * Find lowest value
 *
 * @param array $values An array containing the values.
 *
 * @return mixed <code>min</code> returns the parameter value considered "lowest" according to standard
 * comparisons. If multiple values of different types evaluate as equal (e.g. <code>0</code>
 * and <code>'abc'</code>) the first provided to the function will be returned.
 * If an empty array is passed, then <code>FALSE</code> will be returned and an
 * <code>E_WARNING</code> error will be emitted.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.min.php
 */
function min(array ...$values)
{
}

/**
 * Makes directory
 *
 * @param string $pathname The directory path.
 * @param int|null $mode The mode is 0777 by default, which means the widest possible
 * access. For more information on modes, read the details
 * on the <code>chmod</code> page.
 * @param bool|null $recursive Allows the creation of nested directories specified in the
 * <code>pathname</code>.
 * @param resource $context
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.mkdir.php
 */
function mkdir($pathname, $mode = 0777, $recursive = false, $context = null)
{
}

/**
 * Formats a number as a currency string
 *
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @param string $format The format specification consists of the following sequence:
 * <ul>
 * a <code>%</code> character
 * optional flags
 * optional field width
 * optional left precision
 * optional right precision
 * a required conversion character
 * </ul>
 * @param float $number The number to be formatted.
 *
 * @return string Returns the formatted string. Characters before and after the formatting
 * string will be returned unchanged.
 * Non-numeric <code>number</code> causes returning <code>NULL</code> and
 * emitting <code>E_WARNING</code>.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.money-format.php
 */
function money_format($format, $number)
{
}

/**
 * Moves an uploaded file to a new location
 *
 * @param string $filename The filename of the uploaded file.
 * @param string $destination The destination of the moved file.
 *
 * @return bool Returns <code>TRUE</code> on success.
 * If <code>filename</code> is not a valid upload file,
 * then no action will occur, and
 * <code>move_uploaded_file</code> will return
 * <code>FALSE</code>.
 * If <code>filename</code> is a valid upload file, but
 * cannot be moved for some reason, no action will occur, and
 * <code>move_uploaded_file</code> will return
 * <code>FALSE</code>. Additionally, a warning will be issued.
 *
 * @since PHP 4 >= 4.0.3, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.move-uploaded-file.php
 */
function move_uploaded_file($filename, $destination)
{
}

/**
 * Show largest possible random value
 *
 * @return int Returns the maximum random value returned by a call to
 * <code>mt_rand</code> without arguments, which is the maximum value
 * that can be used for its <code>max</code> parameter without the
 * result being scaled up (and therefore less random).
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.mt-getrandmax.php
 */
function mt_getrandmax()
{
}

/**
 * Generate a random value via the Mersenne Twister Random Number Generator
 *
 * @param mixed $min
 * @param mixed $max
 *
 * @return int A random integer value between <code>min</code> (or 0)
 * and <code>max</code> (or <code>mt_getrandmax</code>, inclusive),
 * or <code>FALSE</code> if <code>max</code> is less than <code>min</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.mt-rand.php
 */
function mt_rand($min = null, $max = null)
{
}

/**
 * Seeds the Mersenne Twister Random Number Generator
 *
 * @param int $seed An arbitrary <code>integer</code> seed value.
 *
 * @return void No value is returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.mt-srand.php
 */
function mt_srand($seed = null)
{
}

/**
 * Sort an array using a case insensitive "natural order" algorithm
 *
 * @param array $array The input array.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.natcasesort.php
 */
function natcasesort(array &$array)
{
}

/**
 * Sort an array using a "natural order" algorithm
 *
 * @param array $array The input array.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.natsort.php
 */
function natsort(array &$array)
{
}

/**
 * Advance the internal pointer of an array
 *
 * @param array $array The <code>array</code> being affected.
 *
 * @return mixed Returns the array value in the next place that's pointed to by the
 * internal array pointer, or <code>FALSE</code> if there are no more elements.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.next.php
 */
function next(array &$array)
{
}

/**
 * Inserts HTML line breaks before all newlines in a string
 *
 * @param string $string The input string.
 * @param bool|null $is_xhtml Whether to use XHTML compatible line breaks or not.
 *
 * @return string Returns the altered string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.nl2br.php
 */
function nl2br($string, $is_xhtml = true)
{
}

/**
 * Query language and locale information
 *
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @param int $item <code>item</code> may be an integer value of the element or the
 * constant name of the element. The following is a list of constant names
 * for <code>item</code> that may be used and their description.
 * Some of these constants may not be defined or hold no value for certain
 * locales.
 * <table>
 * <title>nl_langinfo Constants</title>
 * <colspec colname="c1"/>
 * <colspec colname="c2"/>
 * <thead>
 * <tr>
 * <td>Constant</td><td>Description</td>
 * </tr>
 * </thead>
 * <tbody>
 * <tr>
 * <td namest="c1" nameend="c2" align="center"><code>LC_TIME Category Constants</code></td>
 * </tr>
 * <tr>
 * <td><code>ABDAY_(1-7)</code></td><td>Abbreviated name of n-th day of the week.</td>
 * </tr>
 * <tr>
 * <td><code>DAY_(1-7)</code></td><td>Name of the n-th day of the week (DAY_1 = Sunday).</td>
 * </tr>
 * <tr>
 * <td><code>ABMON_(1-12)</code></td><td>Abbreviated name of the n-th month of the year.</td>
 * </tr>
 * <tr>
 * <td><code>MON_(1-12)</code></td><td>Name of the n-th month of the year.</td>
 * </tr>
 * <tr>
 * <td><code>AM_STR</code></td><td>String for Ante meridian.</td>
 * </tr>
 * <tr>
 * <td><code>PM_STR</code></td><td>String for Post meridian.</td>
 * </tr>
 * <tr>
 * <td><code>D_T_FMT</code></td><td>String that can be used as the format string for <code>strftime</code> to represent time and date.</td>
 * </tr>
 * <tr>
 * <td><code>D_FMT</code></td><td>String that can be used as the format string for <code>strftime</code> to represent date.</td>
 * </tr>
 * <tr>
 * <td><code>T_FMT</code></td><td>String that can be used as the format string for <code>strftime</code> to represent time.</td>
 * </tr>
 * <tr>
 * <td><code>T_FMT_AMPM</code></td><td>String that can be used as the format string for <code>strftime</code> to represent time in 12-hour format with ante/post meridian.</td>
 * </tr>
 * <tr>
 * <td><code>ERA</code></td><td>Alternate era.</td>
 * </tr>
 * <tr>
 * <td><code>ERA_YEAR</code></td><td>Year in alternate era format.</td>
 * </tr>
 * <tr>
 * <td><code>ERA_D_T_FMT</code></td><td>Date and time in alternate era format (string can be used in <code>strftime</code>).</td>
 * </tr>
 * <tr>
 * <td><code>ERA_D_FMT</code></td><td>Date in alternate era format (string can be used in <code>strftime</code>).</td>
 * </tr>
 * <tr>
 * <td><code>ERA_T_FMT</code></td><td>Time in alternate era format (string can be used in <code>strftime</code>).</td>
 * </tr>
 * <tr>
 * <td namest="c1" nameend="c2" align="center"><code>LC_MONETARY Category Constants</code></td>
 * </tr>
 * <tr>
 * <td><code>INT_CURR_SYMBOL</code></td><td>International currency symbol.</td>
 * </tr>
 * <tr>
 * <td><code>CURRENCY_SYMBOL</code></td><td>Local currency symbol.</td>
 * </tr>
 * <tr>
 * <td><code>CRNCYSTR</code></td><td>Same value as <code>CURRENCY_SYMBOL</code>.</td>
 * </tr>
 * <tr>
 * <td><code>MON_DECIMAL_POINT</code></td><td>Decimal point character.</td>
 * </tr>
 * <tr>
 * <td><code>MON_THOUSANDS_SEP</code></td><td>Thousands separator (groups of three digits).</td>
 * </tr>
 * <tr>
 * <td><code>MON_GROUPING</code></td><td>Like <code>"grouping"</code> element.</td>
 * </tr>
 * <tr>
 * <td><code>POSITIVE_SIGN</code></td><td>Sign for positive values.</td>
 * </tr>
 * <tr>
 * <td><code>NEGATIVE_SIGN</code></td><td>Sign for negative values.</td>
 * </tr>
 * <tr>
 * <td><code>INT_FRAC_DIGITS</code></td><td>International fractional digits.</td>
 * </tr>
 * <tr>
 * <td><code>FRAC_DIGITS</code></td><td>Local fractional digits.</td>
 * </tr>
 * <tr>
 * <td><code>P_CS_PRECEDES</code></td><td>Returns 1 if <code>CURRENCY_SYMBOL</code> precedes a positive value.</td>
 * </tr>
 * <tr>
 * <td><code>P_SEP_BY_SPACE</code></td><td>Returns 1 if a space separates <code>CURRENCY_SYMBOL</code> from a positive value.</td>
 * </tr>
 * <tr>
 * <td><code>N_CS_PRECEDES</code></td><td>Returns 1 if <code>CURRENCY_SYMBOL</code> precedes a negative value.</td>
 * </tr>
 * <tr>
 * <td><code>N_SEP_BY_SPACE</code></td><td>Returns 1 if a space separates <code>CURRENCY_SYMBOL</code> from a negative value.</td>
 * </tr>
 * <tr>
 * <td><code>P_SIGN_POSN</code></td><td morerows="1" valign="middle">
 * <ul>
 * Returns 0 if parentheses surround the quantity and <code>CURRENCY_SYMBOL</code>.
 * Returns 1 if the sign string precedes the quantity and <code>CURRENCY_SYMBOL</code>.
 * Returns 2 if the sign string follows the quantity and <code>CURRENCY_SYMBOL</code>.
 * Returns 3 if the sign string immediately precedes the <code>CURRENCY_SYMBOL</code>.
 * Returns 4 if the sign string immediately follows the <code>CURRENCY_SYMBOL</code>.
 * </ul>
 * </td>
 * </tr>
 * <tr>
 * <td><code>N_SIGN_POSN</code></td>
 * </tr>
 * <tr>
 * <td namest="c1" nameend="c2" align="center"><code>LC_NUMERIC Category Constants</code></td>
 * </tr>
 * <tr>
 * <td><code>DECIMAL_POINT</code></td><td>Decimal point character.</td>
 * </tr>
 * <tr>
 * <td><code>RADIXCHAR</code></td><td>Same value as <code>DECIMAL_POINT</code>.</td>
 * </tr>
 * <tr>
 * <td><code>THOUSANDS_SEP</code></td><td>Separator character for thousands (groups of three digits).</td>
 * </tr>
 * <tr>
 * <td><code>THOUSEP</code></td><td>Same value as <code>THOUSANDS_SEP</code>.</td>
 * </tr>
 * <tr>
 * <td><code>GROUPING</code></td><td/>
 * </tr>
 * <tr>
 * <td namest="c1" nameend="c2" align="center"><code>LC_MESSAGES Category Constants</code></td>
 * </tr>
 * <tr>
 * <td><code>YESEXPR</code></td><td>Regex string for matching <code>"yes"</code> input.</td>
 * </tr>
 * <tr>
 * <td><code>NOEXPR</code></td><td>Regex string for matching <code>"no"</code> input.</td>
 * </tr>
 * <tr>
 * <td><code>YESSTR</code></td><td>Output string for <code>"yes"</code>.</td>
 * </tr>
 * <tr>
 * <td><code>NOSTR</code></td><td>Output string for <code>"no"</code>.</td>
 * </tr>
 * <tr>
 * <td namest="c1" nameend="c2" align="center"><code>LC_CTYPE Category Constants</code></td>
 * </tr>
 * <tr>
 * <td><code>CODESET</code></td><td>Return a string with the name of the character encoding.</td>
 * </tr>
 * </tbody>
 * </table>
 *
 * @return string Returns the element as a string, or <code>FALSE</code> if <code>item</code>
 * is not valid.
 *
 * @since PHP 4 >= 4.1.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.nl-langinfo.php
 */
function nl_langinfo($item)
{
}

/**
 * Format a number with grouped thousands
 *
 * @param float $number The number being formatted.
 * @param int|null $decimals Sets the number of decimal points.
 * @param mixed $dec_separator
 * @param mixed $thousands_separator
 *
 * @return string A formatted version of <code>number</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.number-format.php
 */
function number_format($number, $decimals = 0, $dec_separator = null, $thousands_separator = null)
{
}

/**
 * Clean (erase) the output buffer
 *
 * @return void No value is returned.
 *
 * @since PHP 4 >= 4.2.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ob-clean.php
 */
function ob_clean()
{
}

/**
 * Clean (erase) the output buffer and turn off output buffering
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure. Reasons for failure are first that you called the
 * function without an active buffer or that for some reason a buffer could
 * not be deleted (possible for special buffer).
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ob-end-clean.php
 */
function ob_end_clean()
{
}

/**
 * Flush (send) the output buffer and turn off output buffering
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure. Reasons for failure are first that you called the
 * function without an active buffer or that for some reason a buffer could
 * not be deleted (possible for special buffer).
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ob-end-flush.php
 */
function ob_end_flush()
{
}

/**
 * Flush (send) the output buffer
 *
 * @return void No value is returned.
 *
 * @since PHP 4 >= 4.2.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ob-flush.php
 */
function ob_flush()
{
}

/**
 * Get current buffer contents and delete current output buffer
 *
 * @return string Returns the contents of the output buffer and end output buffering.
 * If output buffering isn't active then <code>FALSE</code> is returned.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ob-get-clean.php
 */
function ob_get_clean()
{
}

/**
 * Return the contents of the output buffer
 *
 * @return string This will return the contents of the output buffer or <code>FALSE</code>, if output
 * buffering isn't active.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ob-get-contents.php
 */
function ob_get_contents()
{
}

/**
 * Flush the output buffer, return it as a string and turn off output buffering
 *
 * @return string Returns the output buffer or <code>FALSE</code> if no buffering is active.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ob-get-flush.php
 */
function ob_get_flush()
{
}

/**
 * Return the length of the output buffer
 *
 * @return int Returns the length of the output buffer contents, in bytes, or <code>FALSE</code> if no
 * buffering is active.
 *
 * @since PHP 4 >= 4.0.2, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ob-get-length.php
 */
function ob_get_length()
{
}

/**
 * Return the nesting level of the output buffering mechanism
 *
 * @return int Returns the level of nested output buffering handlers or zero if output
 * buffering is not active.
 *
 * @since PHP 4 >= 4.2.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ob-get-level.php
 */
function ob_get_level()
{
}

/**
 * Get status of output buffers
 *
 * @param bool|null $full_status <code>TRUE</code> to return all active output buffer levels. If <code>FALSE</code> or not
 * set, only the top level output buffer is returned.
 *
 * @return array If called without the <code>full_status</code> parameter
 * or with <code>full_status</code> = <code>FALSE</code> a simple array
 * with the following elements is returned:
 * <informalexample>
 * <blockquote><pre>
 * Array
 * (
 * [level] =&gt; 2
 * [type] =&gt; 0
 * [status] =&gt; 0
 * [name] =&gt; URL-Rewriter
 * [del] =&gt; 1
 * )
 * </pre></blockquote>
 * </informalexample>
 * <segmentedlist>
 * <title>Simple <code>ob_get_status</code> results</title>
 * <segtitle>Key</segtitle><segtitle>Value</segtitle>
 * <seglistitem><seg>level</seg><seg>Output nesting level</seg></seglistitem>
 * <seglistitem><seg>type</seg><seg><code>PHP_OUTPUT_HANDLER_INTERNAL (0)</code> or <code>PHP_OUTPUT_HANDLER_USER (1)</code></seg></seglistitem>
 * <seglistitem><seg>status</seg><seg>One of <code>PHP_OUTPUT_HANDLER_START</code> (0), <code>PHP_OUTPUT_HANDLER_CONT</code> (1) or <code>PHP_OUTPUT_HANDLER_END</code> (2)</seg></seglistitem>
 * <seglistitem><seg>name</seg><seg>Name of active output handler or ' default output handler' if none is set</seg></seglistitem>
 * <seglistitem><seg>del</seg><seg>Erase-flag as set by <code>ob_start</code></seg></seglistitem>
 * </segmentedlist>
 * If called with <code>full_status</code> = <code>TRUE</code> an array
 * with one element for each active output buffer level is returned.
 * The output level is used as key of the top level array and each array
 * element itself is another array holding status information
 * on one active output level.
 * <informalexample>
 * <blockquote><pre>
 * Array
 * (
 * [0] =&gt; Array
 * (
 * [chunk_size] =&gt; 0
 * [size] =&gt; 40960
 * [block_size] =&gt; 10240
 * [type] =&gt; 1
 * [status] =&gt; 0
 * [name] =&gt; default output handler
 * [del] =&gt; 1
 * )
 * [1] =&gt; Array
 * (
 * [chunk_size] =&gt; 0
 * [size] =&gt; 40960
 * [block_size] =&gt; 10240
 * [type] =&gt; 0
 * [buffer_size] =&gt; 0
 * [status] =&gt; 0
 * [name] =&gt; URL-Rewriter
 * [del] =&gt; 1
 * )
 * )
 * </pre></blockquote>
 * </informalexample>
 * The full output contains these additional elements:
 * <segmentedlist>
 * <title>Full <code>ob_get_status</code> results</title>
 * <segtitle>Key</segtitle><segtitle>Value</segtitle>
 * <seglistitem><seg>chunk_size</seg><seg>Chunk size as set by <code>ob_start</code></seg></seglistitem>
 * <seglistitem><seg>size</seg><seg>...</seg></seglistitem>
 * <seglistitem><seg>blocksize</seg><seg>...</seg></seglistitem>
 * </segmentedlist>
 *
 * @since PHP 4 >= 4.2.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ob-get-status.php
 */
function ob_get_status($full_status = false)
{
}

/**
 * Turn implicit flush on/off
 *
 * @param int|null $flag <code>1</code> to turn implicit flushing on, <code>0</code> otherwise.
 *
 * @return void No value is returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ob-implicit-flush.php
 */
function ob_implicit_flush($flag = 1)
{
}

/**
 * List all output handlers in use
 *
 * @return array This will return an array with the output handlers in use (if any). If
 * output_buffering is enabled or
 * an anonymous function was used with <code>ob_start</code>,
 * <code>ob_list_handlers</code> will return "default output
 * handler".
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ob-list-handlers.php
 */
function ob_list_handlers()
{
}

/**
 * Turn on output buffering
 *
 * @param callable|null $output_callback An optional <code>output_callback</code> function may be
 * specified. This function takes a string as a parameter and should
 * return a string. The function will be called when
 * the output buffer is flushed (sent) or cleaned (with
 * <code>ob_flush</code>, <code>ob_clean</code> or similar
 * function) or when the output buffer
 * is flushed to the browser at the end of the request. When
 * <code>output_callback</code> is called, it will receive the
 * contents of the output buffer as its parameter and is expected to
 * return a new output buffer as a result, which will be sent to the
 * browser. If the <code>output_callback</code> is not a
 * callable function, this function will return <code>FALSE</code>.
 * This is the callback signature:
 * @param int|null $chunk_size If the optional parameter <code>chunk_size</code> is passed, the
 * buffer will be flushed after any output call which causes the buffer's
 * length to equal or exceed <code>chunk_size</code>. The default
 * value <code>0</code> means that the output function will only be
 * called when the output buffer is closed.
 * @param int|null $flags The <code>flags</code> parameter is a bitmask that controls
 * the operations that can be performed on the output buffer. The default
 * is to allow output buffers to be cleaned, flushed and removed, which
 * can be set explicitly via
 * <code>PHP_OUTPUT_HANDLER_CLEANABLE</code> |
 * <code>PHP_OUTPUT_HANDLER_FLUSHABLE</code> |
 * <code>PHP_OUTPUT_HANDLER_REMOVABLE</code>, or
 * <code>PHP_OUTPUT_HANDLER_STDFLAGS</code> as shorthand.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ob-start.php
 */
function ob_start(callable $output_callback = null, $chunk_size = 0, $flags = PHP_OUTPUT_HANDLER_STDFLAGS)
{
}

/**
 * Octal to decimal
 *
 * @param string $octal_string The octal string to convert
 *
 * @return float The decimal representation of <code>octal_string</code>
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.octdec.php
 */
function octdec($octal_string)
{
}

/**
 * Open directory handle
 *
 * @param string $path The directory path that is to be opened
 * @param resource $context For a description of the <code>context</code> parameter,
 * refer to the streams section of
 * the manual.
 *
 * @return resource Returns a directory handle <code>resource</code> on success, or
 * <code>FALSE</code> on failure.
 * If <code>path</code> is not a valid directory or the
 * directory can not be opened due to permission restrictions or
 * filesystem errors, <code>opendir</code> returns <code>FALSE</code> and
 * generates a PHP error of level
 * E_WARNING. You can suppress the error output of
 * <code>opendir</code> by prepending
 * '@' to the
 * front of the function name.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.opendir.php
 */
function opendir($path, $context = null)
{
}

/**
 * Open connection to system logger
 *
 * @param string $ident The string <code>ident</code> is added to each message.
 * @param int $option The <code>option</code> argument is used to indicate
 * what logging options will be used when generating a log message.
 * <table>
 * <title><code>openlog</code> Options</title>
 * <thead>
 * <tr>
 * <td>Constant</td><td>Description</td>
 * </tr>
 * </thead>
 * <tbody>
 * <tr>
 * <td><code>LOG_CONS</code></td><td>
 * if there is an error while sending data to the system logger,
 * write directly to the system console
 * </td>
 * </tr>
 * <tr>
 * <td><code>LOG_NDELAY</code></td><td>
 * open the connection to the logger immediately
 * </td>
 * </tr>
 * <tr>
 * <td><code>LOG_ODELAY</code></td><td>
 * (default) delay opening the connection until the first
 * message is logged
 * </td>
 * </tr>
 * <tr>
 * <td><code>LOG_PERROR</code></td><td>print log message also to standard error</td>
 * </tr>
 * <tr>
 * <td><code>LOG_PID</code></td><td>include PID with each message</td>
 * </tr>
 * </tbody>
 * </table>
 * You can use one or more of this options. When using multiple options
 * you need to <code>OR</code> them, i.e. to open the connection
 * immediately, write to the console and include the PID in each message,
 * you will use: <code>LOG_CONS | LOG_NDELAY | LOG_PID</code>
 * @param int $facility The <code>facility</code> argument is used to specify what
 * type of program is logging the message. This allows you to specify
 * (in your machine's syslog configuration) how messages coming from
 * different facilities will be handled.
 * <table>
 * <title><code>openlog</code> Facilities</title>
 * <thead>
 * <tr>
 * <td>Constant</td><td>Description</td>
 * </tr>
 * </thead>
 * <tbody>
 * <tr>
 * <td><code>LOG_AUTH</code></td><td>
 * security/authorization messages (use
 * <code>LOG_AUTHPRIV</code> instead
 * in systems where that constant is defined)
 * </td>
 * </tr>
 * <tr>
 * <td><code>LOG_AUTHPRIV</code></td><td>security/authorization messages (private)</td>
 * </tr>
 * <tr>
 * <td><code>LOG_CRON</code></td><td>clock daemon (cron and at)</td>
 * </tr>
 * <tr>
 * <td><code>LOG_DAEMON</code></td><td>other system daemons</td>
 * </tr>
 * <tr>
 * <td><code>LOG_KERN</code></td><td>kernel messages</td>
 * </tr>
 * <tr>
 * <td><code>LOG_LOCAL0</code> ... <code>LOG_LOCAL7</code></td><td>reserved for local use, these are not available in Windows</td>
 * </tr>
 * <tr>
 * <td><code>LOG_LPR</code></td><td>line printer subsystem</td>
 * </tr>
 * <tr>
 * <td><code>LOG_MAIL</code></td><td>mail subsystem</td>
 * </tr>
 * <tr>
 * <td><code>LOG_NEWS</code></td><td>USENET news subsystem</td>
 * </tr>
 * <tr>
 * <td><code>LOG_SYSLOG</code></td><td>messages generated internally by syslogd</td>
 * </tr>
 * <tr>
 * <td><code>LOG_USER</code></td><td>generic user-level messages</td>
 * </tr>
 * <tr>
 * <td><code>LOG_UUCP</code></td><td>UUCP subsystem</td>
 * </tr>
 * </tbody>
 * </table>
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.openlog.php
 */
function openlog($ident, $option, $facility)
{
}

/**
 * Return ASCII value of character
 *
 * @param string $string A character.
 *
 * @return int Returns the ASCII value as an integer.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ord.php
 */
function ord($string)
{
}

/**
 * Add URL rewriter values
 *
 * @param string $name The variable name.
 * @param string $value The variable value.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.output-add-rewrite-var.php
 */
function output_add_rewrite_var($name, $value)
{
}

/**
 * Reset URL rewriter values
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.output-reset-rewrite-vars.php
 */
function output_reset_rewrite_vars()
{
}

/**
 * Pack data into binary string
 *
 * @param string $format The <code>format</code> string consists of format codes
 * followed by an optional repeater argument. The repeater argument can
 * be either an integer value or <code>*</code> for repeating to
 * the end of the input data. For a, A, h, H the repeat count specifies
 * how many characters of one data argument are taken, for @ it is the
 * absolute position where to put the next data, for everything else the
 * repeat count specifies how many data arguments are consumed and packed
 * into the resulting binary string.
 * @param mixed $args
 *
 * @return string Returns a binary string containing data.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.pack.php
 */
function pack($format, ...$args)
{
}

/**
 * Parse a configuration file
 *
 * @param string $filename The filename of the ini file being parsed.
 * @param bool|null $process_sections By setting the <code>process_sections</code>
 * parameter to <code>TRUE</code>, you get a multidimensional array, with
 * the section names and settings included. The default
 * for <code>process_sections</code> is <code>FALSE</code>
 * @param int|null $scanner_mode Can either be <code>INI_SCANNER_NORMAL</code> (default) or
 * <code>INI_SCANNER_RAW</code>. If <code>INI_SCANNER_RAW</code>
 * is supplied, then option values will not be parsed.
 *
 * @return array The settings are returned as an associative <code>array</code> on success,
 * and <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.parse-ini-file.php
 */
function parse_ini_file($filename, $process_sections = false, $scanner_mode = INI_SCANNER_NORMAL)
{
}

/**
 * Parse a configuration string
 *
 * @param string $ini The contents of the ini file being parsed.
 * @param bool|null $process_sections By setting the <code>process_sections</code>
 * parameter to <code>TRUE</code>, you get a multidimensional array, with
 * the section names and settings included. The default
 * for <code>process_sections</code> is <code>FALSE</code>
 * @param int|null $scanner_mode Can either be <code>INI_SCANNER_NORMAL</code> (default) or
 * <code>INI_SCANNER_RAW</code>. If <code>INI_SCANNER_RAW</code>
 * is supplied, then option values will not be parsed.
 *
 * @return array The settings are returned as an associative <code>array</code> on success,
 * and <code>FALSE</code> on failure.
 *
 * @since PHP 5 >= 5.3.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.parse-ini-string.php
 */
function parse_ini_string($ini, $process_sections = false, $scanner_mode = INI_SCANNER_NORMAL)
{
}

/**
 * Parses the string into variables
 *
 * @param string $encoded_string The input string.
 * @param array $result If the second parameter <code>result</code> is present,
 * variables are stored in this variable as array elements instead.
 *
 * @return void No value is returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.parse-str.php
 */
function parse_str($encoded_string, array &$result = null)
{
}

/**
 * Parse a URL and return its components
 *
 * @param string $url The URL to parse. Invalid characters are replaced by
 * <code>_</code>.
 * @param int|null $component Specify one of <code>PHP_URL_SCHEME</code>,
 * <code>PHP_URL_HOST</code>, <code>PHP_URL_PORT</code>,
 * <code>PHP_URL_USER</code>, <code>PHP_URL_PASS</code>,
 * <code>PHP_URL_PATH</code>, <code>PHP_URL_QUERY</code>
 * or <code>PHP_URL_FRAGMENT</code> to retrieve just a specific
 * URL component as a <code>string</code> (except when
 * <code>PHP_URL_PORT</code> is given, in which case the return
 * value will be an <code>integer</code>).
 *
 * @return mixed On seriously malformed URLs, <code>parse_url</code> may return
 * <code>FALSE</code>.
 * If the <code>component</code> parameter is omitted, an
 * associative <code>array</code> is returned. At least one element will be
 * present within the array. Potential keys within this array are:
 * <ul>
 * <code remap="structfield">scheme</code> - e.g. http
 * <code remap="structfield">host</code>
 * <code remap="structfield">port</code>
 * <code remap="structfield">user</code>
 * <code remap="structfield">pass</code>
 * <code remap="structfield">path</code>
 * <code remap="structfield">query</code> - after the question mark <code>?</code>
 * <code remap="structfield">fragment</code> - after the hashmark <code>#</code>
 * </ul>
 * If the <code>component</code> parameter is specified,
 * <code>parse_url</code> returns a <code>string</code> (or an
 * <code>integer</code>, in the case of <code>PHP_URL_PORT</code>)
 * instead of an <code>array</code>. If the requested component doesn't exist
 * within the given URL, <code>NULL</code> will be returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.parse-url.php
 */
function parse_url($url, $component = -1)
{
}

/**
 * Execute an external program and display raw output
 *
 * @param string $command The command that will be executed.
 * @param int $return_var If the <code>return_var</code> argument is present, the
 * return status of the Unix command will be placed here.
 *
 * @return void No value is returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.passthru.php
 */
function passthru($command, &$return_var = null)
{
}

/**
 * Returns information about the given hash
 *
 * @param string $hash A hash created by <code>password_hash</code>.
 *
 * @return array Returns an associative array with three elements:
 * <ul>
 * <code>algo</code>, which will match a
 * password algorithm constant
 * <code>algoName</code>, which has the human readable name of the
 * algorithm
 * <code>options</code>, which includes the options
 * provided when calling <code>password_hash</code>
 * </ul>
 *
 * @since PHP 5 >= 5.5.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.password-get-info.php
 */
function password_get_info($hash)
{
}

/**
 * Creates a password hash
 *
 * @param string $password The user's password.
 * @param int $algo A password algorithm constant denoting the algorithm to use when hashing the password.
 * @param array $options An associative array containing options. See the password algorithm constants for documentation on the supported options for each algorithm.
 *
 * @return string Returns the hashed password, or <code>FALSE</code> on failure.
 * The used algorithm, cost and salt are returned as part of the hash. Therefore,
 * all information that's needed to verify the hash is included in it. This allows
 * the <code>password_verify</code> function to verify the hash without
 * needing separate storage for the salt or algorithm information.
 *
 * @since PHP 5 >= 5.5.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.password-hash.php
 */
function password_hash($password, $algo, array $options = null)
{
}

/**
 * Checks if the given hash matches the given options
 *
 * @param string $hash A hash created by <code>password_hash</code>.
 * @param int $algo A password algorithm constant denoting the algorithm to use when hashing the password.
 * @param array $options An associative array containing options. See the password algorithm constants for documentation on the supported options for each algorithm.
 *
 * @return bool Returns <code>TRUE</code> if the hash should be rehashed to match the given
 * <code>algo</code> and <code>options</code>, or <code>FALSE</code>
 * otherwise.
 *
 * @since PHP 5 >= 5.5.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.password-needs-rehash.php
 */
function password_needs_rehash($hash, $algo, array $options = null)
{
}

/**
 * Verifies that a password matches a hash
 *
 * @param string $password The user's password.
 * @param string $hash A hash created by <code>password_hash</code>.
 *
 * @return bool Returns <code>TRUE</code> if the password and hash match, or <code>FALSE</code> otherwise.
 *
 * @since PHP 5 >= 5.5.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.password-verify.php
 */
function password_verify($password, $hash)
{
}

/**
 * Returns information about a file path
 *
 * @param string $path The path to be parsed.
 * @param int|null $options If present, specifies a specific element to be returned; one of
 * <code>PATHINFO_DIRNAME</code>,
 * <code>PATHINFO_BASENAME</code>,
 * <code>PATHINFO_EXTENSION</code> or
 * <code>PATHINFO_FILENAME</code>.
 *
 * @return mixed If the <code>options</code> parameter is not passed, an
 * associative <code>array</code> containing the following elements is
 * returned:
 * <code>dirname</code>, <code>basename</code>,
 * <code>extension</code> (if any), and <code>filename</code>.
 * If <code>options</code> is present, returns a
 * <code>string</code> containing the requested element.
 *
 * @since PHP 4 >= 4.0.3, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.pathinfo.php
 */
function pathinfo($path, $options = PATHINFO_DIRNAME | PATHINFO_BASENAME | PATHINFO_EXTENSION | PATHINFO_FILENAME)
{
}

/**
 * Closes process file pointer
 *
 * @param resource $handle The file pointer must be valid, and must have been returned by a
 * successful call to <code>popen</code>.
 *
 * @return int Returns the termination status of the process that was run. In case of
 * an error then <code>-1</code> is returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.pclose.php
 */
function pclose($handle)
{
}

/**
 * Open persistent Internet or Unix domain socket connection
 *
 * @param string $hostname
 * @param int|null $port
 * @param int $errno
 * @param string $errstr
 * @param float|null $timeout
 *
 * @return resource
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.pfsockopen.php
 */
function pfsockopen($hostname, $port = -1, &$errno = null, &$errstr = null, $timeout = /* ini_get("default_socket_timeout") */ null)
{
}

/**
 * Retrieve a path to the loaded php.ini file
 *
 * @return string The loaded <code>php.ini</code> path, or <code>FALSE</code> if one is not loaded.
 *
 * @since PHP 5 >= 5.2.4, PHP 7
 *
 * @link http://www.php.net/manual/en/function.php-ini-loaded-file.php
 */
function php_ini_loaded_file()
{
}

/**
 * Return a list of .ini files parsed from the additional ini dir
 *
 * @return string Returns a comma-separated string of .ini files on success. Each comma is
 * followed by a newline. If the configure directive <code role="configure">--with-config-file-scan-dir</code> wasn't set and the
 * <code>PHP_INI_SCAN_DIR</code> environment variable isn't set, <code>FALSE</code>
 * is returned. If it was set and the directory was empty, an empty string is
 * returned. If a file is unrecognizable, the file will still make it into
 * the returned string but a PHP error will also result. This PHP error will
 * be seen both at compile time and while using
 * <code>php_ini_scanned_files</code>.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.php-ini-scanned-files.php
 */
function php_ini_scanned_files()
{
}

/**
 * Returns the type of interface between web server and PHP
 *
 * @return string Returns the interface type, as a lowercase string.
 * Although not exhaustive, the possible return values include
 * <code>aolserver</code>, <code>apache</code>,
 * <code>apache2filter</code>, <code>apache2handler</code>,
 * <code>caudium</code>, <code>cgi</code> (until PHP 5.3),
 * <code>cgi-fcgi</code>, <code>cli</code>, <code>cli-server</code>,
 * <code>continuity</code>, <code>embed</code>, <code>fpm-fcgi</code>,
 * <code>isapi</code>, <code>litespeed</code>,
 * <code>milter</code>, <code>nsapi</code>,
 * <code>phttpd</code>, <code>pi3web</code>, <code>roxen</code>,
 * <code>thttpd</code>, <code>tux</code>, and <code>webjames</code>.
 *
 * @since PHP 4 >= 4.0.1, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.php-sapi-name.php
 */
function php_sapi_name()
{
}

/**
 * Return source with stripped comments and whitespace
 *
 * @param string $filename Path to the PHP file.
 *
 * @return string The stripped source code will be returned on success, or an empty string
 * on failure.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.php-strip-whitespace.php
 */
function php_strip_whitespace($filename)
{
}

/**
 * Returns information about the operating system PHP is running on
 *
 * @return string Returns the description, as a string.
 *
 * @since PHP 4 >= 4.0.2, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.php-uname.php
 */
function php_uname()
{
}

/**
 * Prints out the credits for PHP
 *
 * @param int|null $flag To generate a custom credits page, you may want to use the
 * <code>flag</code> parameter.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.phpcredits.php
 */
function phpcredits($flag = CREDITS_ALL)
{
}

/**
 * Outputs information about PHP's configuration
 *
 * @param int|null $what The output may be customized by passing one or more of the
 * following <code>constants</code> bitwise values summed
 * together in the optional <code>what</code> parameter.
 * One can also combine the respective constants or bitwise values
 * together with the or operator.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.phpinfo.php
 */
function phpinfo($what = INFO_ALL)
{
}

/**
 * Gets the current PHP version
 *
 * @param string $extension An optional extension name.
 *
 * @return string If the optional <code>extension</code> parameter is
 * specified, <code>phpversion</code> returns the version of that
 * extension, or <code>FALSE</code> if there is no version information associated or
 * the extension isn't enabled.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.phpversion.php
 */
function phpversion($extension = null)
{
}

/**
 * Get value of pi
 *
 * @return float The value of pi as float.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.pi.php
 */
function pi()
{
}

/**
 * Opens process file pointer
 *
 * @param string $command The command
 * @param string $mode The mode
 *
 * @return resource Returns a file pointer identical to that returned by
 * <code>fopen</code>, except that it is unidirectional (may
 * only be used for reading or writing) and must be closed with
 * <code>pclose</code>. This pointer may be used with
 * <code>fgets</code>, <code>fgetss</code>, and
 * <code>fwrite</code>. When the mode is 'r', the returned
 * file pointer equals to the STDOUT of the command, when the mode
 * is 'w', the returned file pointer equals to the STDIN of the
 * command.
 * If an error occurs, returns <code>FALSE</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.popen.php
 */
function popen($command, $mode)
{
}

/**
 * Alias of <code>current</code>
 *
 * @param mixed $arg
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.pos.php
 */
function pos(&$arg)
{
}

/**
 * Exponential expression
 *
 * @param float $base The base to use
 * @param float $exp The exponent
 *
 * @return float <code>base</code> raised to the power of <code>exp</code>.
 * If both arguments are non-negative integers and the result can be represented
 * as an integer, the result will be returned with <code>integer</code> type,
 * otherwise it will be returned as a <code>float</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.pow.php
 */
function pow($base, $exp)
{
}

/**
 * Rewind the internal array pointer
 *
 * @param array $array The input array.
 *
 * @return mixed Returns the array value in the previous place that's pointed to by
 * the internal array pointer, or <code>FALSE</code> if there are no more
 * elements.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.prev.php
 */
function prev(array &$array)
{
}

/**
 * Prints human-readable information about a variable
 *
 * @param mixed $expression The expression to be printed.
 * @param bool|null $return If you would like to capture the output of <code>print_r</code>,
 * use the <code>return</code> parameter. When this parameter is set
 * to <code>TRUE</code>, <code>print_r</code> will return the information rather than print it.
 *
 * @return mixed If given a <code>string</code>, <code>integer</code> or <code>float</code>,
 * the value itself will be printed. If given an <code>array</code>, values
 * will be presented in a format that shows keys and elements. Similar
 * notation is used for <code>object</code>s.
 * When the <code>return</code> parameter is <code>TRUE</code>, this function
 * will return a <code>string</code>. Otherwise, the return value is <code>TRUE</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.print-r.php
 */
function print_r($expression, $return = false)
{
}

/**
 * Output a formatted string
 *
 * @param string $format See <code>sprintf</code> for a description of
 * <code>format</code>.
 * @param mixed $args
 *
 * @return int Returns the length of the outputted string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.printf.php
 */
function printf($format, ...$args)
{
}

/**
 * Close a process opened by <code>proc_open</code> and return the exit code of that process
 *
 * @param resource $process The <code>proc_open</code> <code>resource</code> that will
 * be closed.
 *
 * @return int Returns the termination status of the process that was run. In case of
 * an error then <code>-1</code> is returned.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.proc-close.php
 */
function proc_close($process)
{
}

/**
 * Get information about a process opened by <code>proc_open</code>
 *
 * @param resource $process The <code>proc_open</code> <code>resource</code> that will
 * be evaluated.
 *
 * @return array An <code>array</code> of collected information on success, and <code>FALSE</code>
 * on failure. The returned array contains the following elements:
 * <table>
 * <thead>
 * <tr><td>element</td><td>type</td><td>description</td></tr>
 * </thead>
 * <tbody>
 * <tr>
 * <td>command</td><td><code>string</code></td><td>
 * The command string that was passed to <code>proc_open</code>.
 * </td>
 * </tr>
 * <tr>
 * <td>pid</td><td><code>int</code></td><td>process id</td>
 * </tr>
 * <tr>
 * <td>running</td><td><code>bool</code></td><td>
 * <code>TRUE</code> if the process is still running, <code>FALSE</code> if it has
 * terminated.
 * </td>
 * </tr>
 * <tr>
 * <td>signaled</td><td><code>bool</code></td><td>
 * <code>TRUE</code> if the child process has been terminated by
 * an uncaught signal. Always set to <code>FALSE</code> on Windows.
 * </td>
 * </tr>
 * <tr>
 * <td>stopped</td><td><code>bool</code></td><td>
 * <code>TRUE</code> if the child process has been stopped by a
 * signal. Always set to <code>FALSE</code> on Windows.
 * </td>
 * </tr>
 * <tr>
 * <td>exitcode</td><td><code>int</code></td><td>
 * The exit code returned by the process (which is only
 * meaningful if <code>running</code> is <code>FALSE</code>).
 * Only first call of this function return real value, next calls return
 * <code>-1</code>.
 * </td>
 * </tr>
 * <tr>
 * <td>termsig</td><td><code>int</code></td><td>
 * The number of the signal that caused the child process to terminate
 * its execution (only meaningful if <code>signaled</code> is <code>TRUE</code>).
 * </td>
 * </tr>
 * <tr>
 * <td>stopsig</td><td><code>int</code></td><td>
 * The number of the signal that caused the child process to stop its
 * execution (only meaningful if <code>stopped</code> is <code>TRUE</code>).
 * </td>
 * </tr>
 * </tbody>
 * </table>
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.proc-get-status.php
 */
function proc_get_status($process)
{
}

/**
 * Change the priority of the current process
 *
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @param int $increment The new priority value, the value of this may differ on platforms.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 * If an error occurs, like the user lacks permission to change the priority,
 * an error of level <code>E_WARNING</code> is also generated.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.proc-nice.php
 */
function proc_nice($increment)
{
}

/**
 * Execute a command and open file pointers for input/output
 *
 * @param string $cmd The command to execute
 * @param array $descriptorspec An indexed array where the key represents the descriptor number and the
 * value represents how PHP will pass that descriptor to the child
 * process. 0 is stdin, 1 is stdout, while 2 is stderr.
 * @param array $pipes Will be set to an indexed array of file pointers that correspond to
 * PHP's end of any pipes that are created.
 * @param string $cwd The initial working dir for the command. This must be an
 * <code role="strong">absolute</code> directory path, or <code>NULL</code>
 * if you want to use the default value (the working dir of the current
 * PHP process)
 * @param array $env An array with the environment variables for the command that will be
 * run, or <code>NULL</code> to use the same environment as the current PHP process
 * @param array $other_options Allows you to specify additional options. Currently supported options
 * include:
 * <ul>
 * <li>
 * <code>suppress_errors</code> (windows only): suppresses errors
 * generated by this function when it's set to <code>TRUE</code>
 * </li>
 * <li>
 * <code>bypass_shell</code> (windows only): bypass
 * <code>cmd.exe</code> shell when set to <code>TRUE</code>
 * </li>
 * </ul>
 *
 * @return resource Returns a resource representing the process, which should be freed using
 * <code>proc_close</code> when you are finished with it. On failure
 * returns <code>FALSE</code>.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.proc-open.php
 */
function proc_open($cmd, array $descriptorspec, array &$pipes, $cwd = null, array $env = null, array $other_options = null)
{
}

/**
 * Kills a process opened by proc_open
 *
 * @param resource $process The <code>proc_open</code> <code>resource</code> that will
 * be closed.
 * @param int|null $signal This optional parameter is only useful on <code>POSIX</code>
 * operating systems; you may specify a signal to send to the process
 * using the <code>kill(2)</code> system call. The default is
 * <code>SIGTERM</code>.
 *
 * @return bool Returns the termination status of the process that was run.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.proc-terminate.php
 */
function proc_terminate($process, $signal = 15)
{
}

/**
 * Sets the value of an environment variable
 *
 * @param string $setting The setting, like <code>"FOO=BAR"</code>
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.putenv.php
 */
function putenv($setting)
{
}

/**
 * Convert a quoted-printable string to an 8 bit string
 *
 * @param string $str The input string.
 *
 * @return string Returns the 8-bit binary string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.quoted-printable-decode.php
 */
function quoted_printable_decode($str)
{
}

/**
 * Convert a 8 bit string to a quoted-printable string
 *
 * @param string $str The input string.
 *
 * @return string Returns the encoded string.
 *
 * @since PHP 5 >= 5.3.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.quoted-printable-encode.php
 */
function quoted_printable_encode($str)
{
}

/**
 * Quote meta characters
 *
 * @param string $str The input string.
 *
 * @return string Returns the string with meta characters quoted, or <code>FALSE</code> if an empty
 * string is given as <code>str</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.quotemeta.php
 */
function quotemeta($str)
{
}

/**
 * Converts the radian number to the equivalent number in degrees
 *
 * @param float $number A radian value
 *
 * @return float The equivalent of <code>number</code> in degrees
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.rad2deg.php
 */
function rad2deg($number)
{
}

/**
 * Generate a random integer
 *
 * @param mixed $min
 * @param mixed $max
 *
 * @return int A pseudo random value between <code>min</code>
 * (or 0) and <code>max</code> (or <code>getrandmax</code>, inclusive).
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.rand.php
 */
function rand($min = null, $max = null)
{
}

/**
 * Create an array containing a range of elements
 *
 * @param mixed $start First value of the sequence.
 * @param mixed $end The sequence is ended upon reaching the
 * <code>end</code> value.
 * @param float|null $step If a <code>step</code> value is given, it will be used as the
 * increment between elements in the sequence. <code>step</code>
 * should be given as a positive number. If not specified,
 * <code>step</code> will default to 1.
 *
 * @return array Returns an array of elements from <code>start</code> to
 * <code>end</code>, inclusive.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.range.php
 */
function range($start, $end, $step = 1)
{
}

/**
 * Decode URL-encoded strings
 *
 * @param string $str The URL to be decoded.
 *
 * @return string Returns the decoded URL, as a string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.rawurldecode.php
 */
function rawurldecode($str)
{
}

/**
 * URL-encode according to RFC 3986
 *
 * @param string $str The URL to be encoded.
 *
 * @return string Returns a string in which all non-alphanumeric characters except
 * <code>-_.~</code> have been replaced with a percent
 * (<code>%</code>) sign followed by two hex digits. This is the
 * encoding described in RFC 3986 for
 * protecting literal characters from being interpreted as special URL
 * delimiters, and for protecting URLs from being mangled by transmission
 * media with character conversions (like some email systems).
 * <blockquote>
 * Prior to PHP 5.3.0, rawurlencode encoded tildes (<code>~</code>) as per
 * RFC 1738.
 * </blockquote>
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.rawurlencode.php
 */
function rawurlencode($str)
{
}

/**
 * Read entry from directory handle
 *
 * @param resource $dir_handle The directory handle <code>resource</code> previously opened
 * with <code>opendir</code>. If the directory handle is
 * not specified, the last link opened by <code>opendir</code>
 * is assumed.
 *
 * @return string Returns the entry name on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.readdir.php
 */
function readdir($dir_handle = null)
{
}

/**
 * Outputs a file
 *
 * @param string $filename The filename being read.
 * @param bool|null $use_include_path You can use the optional second parameter and set it to <code>TRUE</code>, if
 * you want to search for the file in the include_path, too.
 * @param resource $context A context stream <code>resource</code>.
 *
 * @return int Returns the number of bytes read from the file. If an error
 * occurs, <code>FALSE</code> is returned and unless the function was called as
 * @<code>readfile</code>, an error message is printed.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.readfile.php
 */
function readfile($filename, $use_include_path = false, $context = null)
{
}

/**
 * Returns the target of a symbolic link
 *
 * @param string $path The symbolic link path.
 *
 * @return string Returns the contents of the symbolic link path or <code>FALSE</code> on error.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.readlink.php
 */
function readlink($path)
{
}

/**
 * Returns canonicalized absolute pathname
 *
 * @param string $path The path being checked.
 * <blockquote>
 * Whilst a path must be supplied, the value can be blank or <code>NULL</code>
 * In these cases, the value is interpreted as the current directory.
 * </blockquote>
 *
 * @return string Returns the canonicalized absolute pathname on success. The resulting path
 * will have no symbolic link, <code>/./</code> or <code>/../</code> components. Trailing delimiters,
 * such as <code>\</code> and <code>/</code>, are also removed.
 * <code>realpath</code> returns <code>FALSE</code> on failure, e.g. if
 * the file does not exist.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.realpath.php
 */
function realpath($path)
{
}

/**
 * Get realpath cache entries
 *
 * @return array Returns an array of realpath cache entries. The keys are original path
 * entries, and the values are arrays of data items, containing the resolved
 * path, expiration date, and other options kept in the cache.
 *
 * @since PHP 5 >= 5.3.2, PHP 7
 *
 * @link http://www.php.net/manual/en/function.realpath-cache-get.php
 */
function realpath_cache_get()
{
}

/**
 * Get realpath cache size
 *
 * @return int Returns how much memory realpath cache is using.
 *
 * @since PHP 5 >= 5.3.2, PHP 7
 *
 * @link http://www.php.net/manual/en/function.realpath-cache-size.php
 */
function realpath_cache_size()
{
}

/**
 * Register a function for execution on shutdown
 *
 * @param callable $callback The shutdown callback to register.
 * @param mixed $parameter It is possible to pass parameters to the shutdown function by passing
 * additional parameters.
 *
 * @return void No value is returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.register-shutdown-function.php
 */
function register_shutdown_function(callable $callback, ...$parameter)
{
}

/**
 * Register a function for execution on each tick
 *
 * @param callable $function The function name as a string, or an array consisting of an object and
 * a method.
 * @param mixed $arg
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4 >= 4.0.3, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.register-tick-function.php
 */
function register_tick_function(callable $function, ...$arg)
{
}

/**
 * Renames a file or directory
 *
 * @param string $oldname The old name.
 * @param string $newname The new name.
 * @param resource $context
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.rename.php
 */
function rename($oldname, $newname, $context = null)
{
}

/**
 * Set the internal pointer of an array to its first element
 *
 * @param array $array The input array.
 *
 * @return mixed Returns the value of the first array element, or <code>FALSE</code> if the array is
 * empty.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.reset.php
 */
function reset(array &$array)
{
}

/**
 * Restores the value of the include_path configuration option
 *
 * @return void No value is returned.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.restore-include-path.php
 */
function restore_include_path()
{
}

/**
 * Rewind the position of a file pointer
 *
 * @param resource $handle The file pointer must be valid, and must point to a file
 * successfully opened by <code>fopen</code>.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.rewind.php
 */
function rewind($handle)
{
}

/**
 * Rewind directory handle
 *
 * @param resource $dir_handle The directory handle <code>resource</code> previously opened
 * with <code>opendir</code>. If the directory handle is
 * not specified, the last link opened by <code>opendir</code>
 * is assumed.
 *
 * @return void Returns <code>NULL</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.rewinddir.php
 */
function rewinddir($dir_handle = null)
{
}

/**
 * Removes directory
 *
 * @param string $dirname Path to the directory.
 * @param resource $context
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.rmdir.php
 */
function rmdir($dirname, $context = null)
{
}

/**
 * Rounds a float
 *
 * @param float $val The value to round
 * @param int|null $precision The optional number of decimal digits to round to.
 * @param int|null $mode Use one of the following constants to specify the mode in which rounding occurs.
 * <table>
 * <thead>
 * <tr>
 * <td>Constant</td><td>Description</td>
 * </tr>
 * </thead>
 * <tbody>
 * <tr>
 * <td><code>PHP_ROUND_HALF_UP</code></td><td>
 * Round <code>val</code> up to <code>precision</code> decimal places
 * away from zero, when it is half way there. Making 1.5 into 2 and -1.5 into -2.
 * </td>
 * </tr>
 * <tr>
 * <td><code>PHP_ROUND_HALF_DOWN</code></td><td>
 * Round <code>val</code> down to <code>precision</code> decimal places
 * towards zero, when it is half way there. Making 1.5 into 1 and -1.5 into -1.
 * </td>
 * </tr>
 * <tr>
 * <td><code>PHP_ROUND_HALF_EVEN</code></td><td>
 * Round <code>val</code> to <code>precision</code> decimal places
 * towards the nearest even value.
 * </td>
 * </tr>
 * <tr>
 * <td><code>PHP_ROUND_HALF_ODD</code></td><td>
 * Round <code>val</code> to <code>precision</code> decimal places
 * towards the nearest odd value.
 * </td>
 * </tr>
 * </tbody>
 * </table>
 *
 * @return float The rounded value
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.round.php
 */
function round($val, $precision = 0, $mode = PHP_ROUND_HALF_UP)
{
}

/**
 * Sort an array in reverse order
 *
 * @param array $array The input array.
 * @param int|null $sort_flags You may modify the behavior of the sort using the optional
 * parameter <code>sort_flags</code>, for details see
 * <code>sort</code>.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.rsort.php
 */
function rsort(array &$array, $sort_flags = SORT_REGULAR)
{
}

/**
 * Strip whitespace (or other characters) from the end of a string
 *
 * @param string $str The input string.
 * @param string $character_mask You can also specify the characters you want to strip, by means
 * of the <code>character_mask</code> parameter.
 * Simply list all characters that you want to be stripped. With
 * <code>..</code> you can specify a range of characters.
 *
 * @return string Returns the modified string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.rtrim.php
 */
function rtrim($str, $character_mask = null)
{
}

/**
 * List files and directories inside the specified path
 *
 * @param string $directory The directory that will be scanned.
 * @param int|null $sorting_order By default, the sorted order is alphabetical in ascending order. If
 * the optional <code>sorting_order</code> is set to
 * <code>SCANDIR_SORT_DESCENDING</code>, then the sort order is
 * alphabetical in descending order. If it is set to
 * <code>SCANDIR_SORT_NONE</code> then the result is unsorted.
 * @param resource $context For a description of the <code>context</code> parameter,
 * refer to the streams section of
 * the manual.
 *
 * @return array Returns an <code>array</code> of filenames on success, or <code>FALSE</code> on
 * failure. If <code>directory</code> is not a directory, then
 * boolean <code>FALSE</code> is returned, and an error of level
 * <code>E_WARNING</code> is generated.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.scandir.php
 */
function scandir($directory, $sorting_order = SCANDIR_SORT_ASCENDING, $context = null)
{
}

/**
 * Generates a storable representation of a value
 *
 * @param mixed $value The value to be serialized. <code>serialize</code>
 * handles all types, except the <code>resource</code>-type.
 * You can even <code>serialize</code> arrays that contain
 * references to itself. Circular references inside the array/object you
 * are serializing will also be stored. Any other
 * reference will be lost.
 *
 * @return string Returns a string containing a byte-stream representation of
 * <code>value</code> that can be stored anywhere.
 * Note that this is a binary string which may include null bytes, and needs
 * to be stored and handled as such. For example,
 * <code>serialize</code> output should generally be stored in a BLOB
 * field in a database, rather than a CHAR or TEXT field.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.serialize.php
 */
function serialize($value)
{
}

/**
 * Alias of <code>stream_set_write_buffer</code>
 *
 * @param mixed $fp
 * @param mixed $buffer
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.set-file-buffer.php
 */
function set_file_buffer($fp, $buffer)
{
}

/**
 * Sets the include_path configuration option
 *
 * @param string $new_include_path The new value for the include_path
 *
 * @return string Returns the old include_path on
 * success or <code>FALSE</code> on failure.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.set-include-path.php
 */
function set_include_path($new_include_path)
{
}

/**
 * Sets the current active configuration setting of magic_quotes_runtime
 *
 * @param bool $new_setting <code>FALSE</code> for off, <code>TRUE</code> for on.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5
 *
 * @deprecated as of PHP 5.3.0
 *
 * @link http://www.php.net/manual/en/function.set-magic-quotes-runtime.php
 */
function set_magic_quotes_runtime($new_setting)
{
}

/**
 * Alias of <code>stream_set_blocking</code>
 *
 * @param mixed $socket
 * @param mixed $mode
 *
 * @since PHP 4, PHP 5
 *
 * @deprecated as of PHP 4.0.0
 *
 * @link http://www.php.net/manual/en/function.set-socket-blocking.php
 */
function set_socket_blocking($socket, $mode)
{
}

/**
 * Limits the maximum execution time
 *
 * @param int $seconds The maximum execution time, in seconds. If set to zero, no time limit
 * is imposed.
 *
 * @return bool Returns <code>TRUE</code> on success, or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.set-time-limit.php
 */
function set_time_limit($seconds)
{
}

/**
 * Send a cookie
 *
 * @param string $name The name of the cookie.
 * @param string|null $value The value of the cookie. This value is stored on the clients computer;
 * do not store sensitive information. Assuming the
 * <code>name</code> is <code>'cookiename'</code>, this
 * value is retrieved through <code>$_COOKIE['cookiename']</code>
 * @param int|null $expire The time the cookie expires. This is a Unix timestamp so is
 * in number of seconds since the epoch. In other words, you'll
 * most likely set this with the <code>time</code> function
 * plus the number of seconds before you want it to expire. Or
 * you might use <code>mktime</code>.
 * <code>time()+60*60*24*30</code> will set the cookie to
 * expire in 30 days. If set to 0, or omitted, the cookie will expire at
 * the end of the session (when the browser closes).
 * @param string|null $path The path on the server in which the cookie will be available on.
 * If set to <code>'/'</code>, the cookie will be available
 * within the entire <code>domain</code>. If set to
 * <code>'/foo/'</code>, the cookie will only be available
 * within the <code>/foo/</code> directory and all
 * sub-directories such as <code>/foo/bar/</code> of
 * <code>domain</code>. The default value is the
 * current directory that the cookie is being set in.
 * @param string|null $domain The (sub)domain that the cookie is available to. Setting this to a
 * subdomain (such as <code>'www.example.com'</code>) will make the
 * cookie available to that subdomain and all other sub-domains of it (i.e.
 * w2.www.example.com). To make the cookie available to the whole domain
 * (including all subdomains of it), simply set the value to the domain
 * name (<code>'example.com'</code>, in this case).
 * @param bool|null $secure Indicates that the cookie should only be transmitted over a
 * secure HTTPS connection from the client. When set to <code>TRUE</code>, the
 * cookie will only be set if a secure connection exists.
 * On the server-side, it's on the programmer to send this
 * kind of cookie only on secure connection (e.g. with respect to
 * <code>$_SERVER["HTTPS"]</code>).
 * @param bool|null $httponly When <code>TRUE</code> the cookie will be made accessible only through the HTTP
 * protocol. This means that the cookie won't be accessible by
 * scripting languages, such as JavaScript. It has been suggested that
 * this setting can effectively help to reduce identity theft through
 * XSS attacks (although it is not supported by all browsers), but that
 * claim is often disputed. Added in PHP 5.2.0.
 * <code>TRUE</code> or <code>FALSE</code>
 *
 * @return bool If output exists prior to calling this function,
 * <code>setcookie</code> will fail and return <code>FALSE</code>. If
 * <code>setcookie</code> successfully runs, it will return <code>TRUE</code>.
 * This does not indicate whether the user accepted the cookie.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.setcookie.php
 */
function setcookie($name, $value = "", $expire = 0, $path = "", $domain = "", $secure = false, $httponly = false)
{
}

/**
 * Set locale information
 *
 * @param int $category <code>category</code> is a named constant specifying the
 * category of the functions affected by the locale setting:
 * <ul>
 * <code>LC_ALL</code> for all of the below
 * <code>LC_COLLATE</code> for string comparison, see
 * <code>strcoll</code>
 * <code>LC_CTYPE</code> for character classification and conversion, for
 * example <code>strtoupper</code>
 * <code>LC_MONETARY</code> for <code>localeconv</code>
 * <code>LC_NUMERIC</code> for decimal separator (See also
 * <code>localeconv</code>)
 * <code>LC_TIME</code> for date and time formatting with
 * <code>strftime</code>
 * <code>LC_MESSAGES</code> for system responses (available if PHP was compiled with
 * <code>libintl</code>)
 * </ul>
 * @param string $locale If <code>locale</code> is <code>NULL</code> or the empty string
 * <code>""</code>, the locale names will be set from the
 * values of environment variables with the same names as the above
 * categories, or from "LANG".
 *
 * @return string Returns the new current locale, or <code>FALSE</code> if the locale functionality is
 * not implemented on your platform, the specified locale does not exist or
 * the category name is invalid.
 * An invalid category name also causes a warning message. Category/locale
 * names can be found in RFC 1766
 * and ISO 639.
 * Different systems have different naming schemes for locales.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.setlocale.php
 */
function setlocale($category, ...$locale)
{
}

/**
 * Send a cookie without urlencoding the cookie value
 *
 * @param string $name
 * @param string $value
 * @param int|null $expire
 * @param string $path
 * @param string $domain
 * @param bool|null $secure
 * @param bool|null $httponly
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.setrawcookie.php
 */
function setrawcookie($name, $value = null, $expire = 0, $path = null, $domain = null, $secure = false, $httponly = false)
{
}

/**
 * Set the type of a variable
 *
 * @param mixed $var The variable being converted.
 * @param string $type Possibles values of <code>type</code> are:
 * <ul>
 * "boolean" or "bool"
 * "integer" or "int"
 * "float" or "double"
 * "string"
 * "array"
 * "object"
 * "null"
 * </ul>
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.settype.php
 */
function settype(&$var, $type)
{
}

/**
 * Calculate the sha1 hash of a string
 *
 * @param string $str The input string.
 * @param bool|null $raw_output If the optional <code>raw_output</code> is set to <code>TRUE</code>,
 * then the sha1 digest is instead returned in raw binary format with a
 * length of 20, otherwise the returned value is a 40-character
 * hexadecimal number.
 *
 * @return string Returns the sha1 hash as a string.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.sha1.php
 */
function sha1($str, $raw_output = false)
{
}

/**
 * Calculate the sha1 hash of a file
 *
 * @param string $filename The filename of the file to hash.
 * @param bool|null $raw_output When <code>TRUE</code>, returns the digest in raw binary format with a length of
 * 20.
 *
 * @return string Returns a string on success, <code>FALSE</code> otherwise.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.sha1-file.php
 */
function sha1_file($filename, $raw_output = false)
{
}

/**
 * Execute command via shell and return the complete output as a string
 *
 * @param string $cmd The command that will be executed.
 *
 * @return string The output from the executed command or <code>NULL</code> if an error occurred or the
 * command produces no output.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.shell-exec.php
 */
function shell_exec($cmd)
{
}

/**
 * Alias of <code>highlight_file</code>
 *
 * @param mixed $file_name
 * @param mixed $return
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.show-source.php
 */
function show_source($file_name, $return = null)
{
}

/**
 * Shuffle an array
 *
 * @param array $array The array.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.shuffle.php
 */
function shuffle(array &$array)
{
}

/**
 * Calculate the similarity between two strings
 *
 * @param string $first The first string.
 * @param string $second The second string.
 * @param float $percent By passing a reference as third argument,
 * <code>similar_text</code> will calculate the similarity in
 * percent, by dividing the result of <code>similar_text</code> by
 * the average of the lengths of the given strings times
 * <code>100</code>.
 *
 * @return int Returns the number of matching chars in both strings.
 * The number of matching characters is calculated by finding the longest first
 * common substring, and then doing this for the prefixes and the suffixes,
 * recursively. The lengths of all found common substrings are added.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.similar-text.php
 */
function similar_text($first, $second, &$percent = null)
{
}

/**
 * Sine
 *
 * @param float $arg A value in radians
 *
 * @return float The sine of <code>arg</code>
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.sin.php
 */
function sin($arg)
{
}

/**
 * Hyperbolic sine
 *
 * @param float $arg The argument to process
 *
 * @return float The hyperbolic sine of <code>arg</code>
 *
 * @since PHP 4 >= 4.1.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.sinh.php
 */
function sinh($arg)
{
}

/**
 * Alias of <code>count</code>
 *
 * @param mixed $var
 * @param mixed $mode
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.sizeof.php
 */
function sizeof($var, $mode = null)
{
}

/**
 * Delay execution
 *
 * @param int $seconds Halt time in seconds.
 *
 * @return int Returns zero on success, or <code>FALSE</code> on error.
 * If the call was interrupted by a signal, <code>sleep</code> returns
 * a non-zero value. On Windows, this value will always be
 * <code>192</code> (the value of the
 * <code>WAIT_IO_COMPLETION</code> constant within the Windows API).
 * On other platforms, the return value will be the number of seconds left to
 * sleep.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.sleep.php
 */
function sleep($seconds)
{
}

/**
 * Alias of <code>stream_get_meta_data</code>
 *
 * @param mixed $fp
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.socket-get-status.php
 */
function socket_get_status($fp)
{
}

/**
 * Alias of <code>stream_set_blocking</code>
 *
 * @param mixed $socket
 * @param mixed $mode
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.socket-set-blocking.php
 */
function socket_set_blocking($socket, $mode)
{
}

/**
 * Alias of <code>stream_set_timeout</code>
 *
 * @param mixed $stream
 * @param mixed $seconds
 * @param mixed $microseconds
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.socket-set-timeout.php
 */
function socket_set_timeout($stream, $seconds, $microseconds)
{
}

/**
 * Sort an array
 *
 * @param array $array The input array.
 * @param int|null $sort_flags The optional second parameter <code>sort_flags</code>
 * may be used to modify the sorting behavior using these values:
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.sort.php
 */
function sort(array &$array, $sort_flags = SORT_REGULAR)
{
}

/**
 * Calculate the soundex key of a string
 *
 * @param string $str The input string.
 *
 * @return string Returns the soundex key as a string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.soundex.php
 */
function soundex($str)
{
}

/**
 * Return a formatted string
 *
 * @param string $format The format string is composed of zero or more directives:
 * ordinary characters (excluding <code>%</code>) that are
 * copied directly to the result and <code>conversion
 * specifications</code>, each of which results in fetching its
 * own parameter. This applies to both <code>sprintf</code>
 * and <code>printf</code>.
 * @param mixed $args
 *
 * @return string Returns a string produced according to the formatting string
 * <code>format</code>, or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.sprintf.php
 */
function sprintf($format, ...$args)
{
}

/**
 * Square root
 *
 * @param float $arg The argument to process
 *
 * @return float The square root of <code>arg</code>
 * or the special value <code>NAN</code> for negative numbers.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.sqrt.php
 */
function sqrt($arg)
{
}

/**
 * Seed the random number generator
 *
 * @param int $seed An arbitrary <code>integer</code> seed value.
 *
 * @return void No value is returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.srand.php
 */
function srand($seed = null)
{
}

/**
 * Parses input from a string according to a format
 *
 * @param string $str The input <code>string</code> being parsed.
 * @param string $format The interpreted format for <code>str</code>, which is
 * described in the documentation for <code>sprintf</code> with
 * following differences:
 * <ul>
 * <li>
 * Function is not locale-aware.
 * </li>
 * <li>
 * <code>F</code>, <code>g</code>, <code>G</code> and
 * <code>b</code> are not supported.
 * </li>
 * <li>
 * <code>D</code> stands for decimal number.
 * </li>
 * <li>
 * <code>i</code> stands for integer with base detection.
 * </li>
 * <li>
 * <code>n</code> stands for number of characters processed so far.
 * </li>
 * <li>
 * <code>s</code> stops reading at any whitespace character.
 * </li>
 * </ul>
 * @param mixed $vars Optionally pass in variables by reference that will contain the parsed values.
 *
 * @return mixed If only two parameters were passed to this function, the values parsed will
 * be returned as an array. Otherwise, if optional parameters are passed, the
 * function will return the number of assigned values. The optional parameters
 * must be passed by reference.
 * If there are more substrings expected in the <code>format</code>
 * than there are available within <code>str</code>,
 * <code>-1</code> will be returned.
 *
 * @since PHP 4 >= 4.0.1, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.sscanf.php
 */
function sscanf($str, $format, &...$vars)
{
}

/**
 * Gives information about a file
 *
 * @param string $filename Path to the file.
 *
 * @return array <table>
 * <title><code>stat</code> and <code>fstat</code> result
 * format</title>
 * <thead>
 * <tr>
 * <td>Numeric</td><td>Associative</td><td>Description</td>
 * </tr>
 * </thead>
 * <tbody>
 * <tr>
 * <td>0</td><td>dev</td><td>device number</td>
 * </tr>
 * <tr>
 * <td>1</td><td>ino</td><td>inode number *</td>
 * </tr>
 * <tr>
 * <td>2</td><td>mode</td><td>inode protection mode</td>
 * </tr>
 * <tr>
 * <td>3</td><td>nlink</td><td>number of links</td>
 * </tr>
 * <tr>
 * <td>4</td><td>uid</td><td>userid of owner *</td>
 * </tr>
 * <tr>
 * <td>5</td><td>gid</td><td>groupid of owner *</td>
 * </tr>
 * <tr>
 * <td>6</td><td>rdev</td><td>device type, if inode device</td>
 * </tr>
 * <tr>
 * <td>7</td><td>size</td><td>size in bytes</td>
 * </tr>
 * <tr>
 * <td>8</td><td>atime</td><td>time of last access (Unix timestamp)</td>
 * </tr>
 * <tr>
 * <td>9</td><td>mtime</td><td>time of last modification (Unix timestamp)</td>
 * </tr>
 * <tr>
 * <td>10</td><td>ctime</td><td>time of last inode change (Unix timestamp)</td>
 * </tr>
 * <tr>
 * <td>11</td><td>blksize</td><td>blocksize of filesystem IO **</td>
 * </tr>
 * <tr>
 * <td>12</td><td>blocks</td><td>number of 512-byte blocks allocated **</td>
 * </tr>
 * </tbody>
 * </table>
 * * On Windows this will always be 0.
 * ** Only valid on systems supporting the st_blksize type - other
 * systems (e.g. Windows) return -1.
 * In case of error, <code>stat</code> returns <code>FALSE</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stat.php
 */
function stat($filename)
{
}

/**
 * Parse a CSV string into an array
 *
 * @param string $input The string to parse.
 * @param string|null $delimiter Set the field delimiter (one character only).
 * @param string|null $enclosure Set the field enclosure character (one character only).
 * @param string|null $escape Set the escape character (one character only). Defaults as a backslash
 * (<code>\</code>)
 *
 * @return array Returns an indexed array containing the fields read.
 *
 * @since PHP 5 >= 5.3.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.str-getcsv.php
 */
function str_getcsv($input, $delimiter = ",", $enclosure = '"', $escape = "\\")
{
}

/**
 * Case-insensitive version of <code>str_replace</code>
 *
 * @param mixed $search The value being searched for, otherwise known as the
 * <code>needle</code>. An array may be used to designate
 * multiple needles.
 * @param mixed $replace The replacement value that replaces found <code>search</code>
 * values. An array may be used to designate multiple replacements.
 * @param mixed $subject The string or array being searched and replaced on,
 * otherwise known as the <code>haystack</code>.
 * @param int $count If passed, this will be set to the number of replacements performed.
 *
 * @return mixed Returns a string or an array of replacements.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.str-ireplace.php
 */
function str_ireplace($search, $replace, $subject, &$count = null)
{
}

/**
 * Pad a string to a certain length with another string
 *
 * @param string $input The input string.
 * @param int $pad_length If the value of <code>pad_length</code> is negative,
 * less than, or equal to the length of the input string, no padding
 * takes place, and <code>input</code> will be returned.
 * @param string|null $pad_string The <code>pad_string</code> may be truncated if the
 * required number of padding characters can't be evenly divided by the
 * <code>pad_string</code>'s length.
 * @param int|null $pad_type Optional argument <code>pad_type</code> can be
 * <code>STR_PAD_RIGHT</code>, <code>STR_PAD_LEFT</code>,
 * or <code>STR_PAD_BOTH</code>. If
 * <code>pad_type</code> is not specified it is assumed to be
 * <code>STR_PAD_RIGHT</code>.
 *
 * @return string Returns the padded string.
 *
 * @since PHP 4 >= 4.0.1, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.str-pad.php
 */
function str_pad($input, $pad_length, $pad_string = " ", $pad_type = STR_PAD_RIGHT)
{
}

/**
 * Repeat a string
 *
 * @param string $input The string to be repeated.
 * @param int $multiplier Number of time the <code>input</code> string should be
 * repeated.
 *
 * @return string Returns the repeated string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.str-repeat.php
 */
function str_repeat($input, $multiplier)
{
}

/**
 * Replace all occurrences of the search string with the replacement string
 *
 * @param mixed $search The value being searched for, otherwise known as the <code>needle</code>.
 * An array may be used to designate multiple needles.
 * @param mixed $replace The replacement value that replaces found <code>search</code>
 * values. An array may be used to designate multiple replacements.
 * @param mixed $subject The string or array being searched and replaced on,
 * otherwise known as the <code>haystack</code>.
 * @param int $count If passed, this will be set to the number of replacements performed.
 *
 * @return mixed This function returns a string or an array with the replaced values.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.str-replace.php
 */
function str_replace($search, $replace, $subject, &$count = null)
{
}

/**
 * Perform the rot13 transform on a string
 *
 * @param string $str The input string.
 *
 * @return string Returns the ROT13 version of the given string.
 *
 * @since PHP 4 >= 4.2.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.str-rot13.php
 */
function str_rot13($str)
{
}

/**
 * Randomly shuffles a string
 *
 * @param string $str The input string.
 *
 * @return string Returns the shuffled string.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.str-shuffle.php
 */
function str_shuffle($str)
{
}

/**
 * Convert a string to an array
 *
 * @param string $string The input string.
 * @param int|null $split_length Maximum length of the chunk.
 *
 * @return array If the optional <code>split_length</code> parameter is
 * specified, the returned array will be broken down into chunks with each
 * being <code>split_length</code> in length, otherwise each chunk
 * will be one character in length.
 * <code>FALSE</code> is returned if <code>split_length</code> is less than 1.
 * If the <code>split_length</code> length exceeds the length of
 * <code>string</code>, the entire string is returned as the first
 * (and only) array element.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.str-split.php
 */
function str_split($string, $split_length = 1)
{
}

/**
 * Return information about words used in a string
 *
 * @param string $string The string
 * @param int|null $format Specify the return value of this function. The current supported values
 * are:
 * <ul>
 * 0 - returns the number of words found
 * 1 - returns an array containing all the words found inside the
 * <code>string</code>
 * 2 - returns an associative array, where the key is the numeric
 * position of the word inside the <code>string</code> and
 * the value is the actual word itself
 * </ul>
 * @param string $charlist A list of additional characters which will be considered as 'word'
 *
 * @return mixed Returns an array or an integer, depending on the
 * <code>format</code> chosen.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.str-word-count.php
 */
function str_word_count($string, $format = 0, $charlist = null)
{
}

/**
 * Alias of <code>strstr</code>
 *
 * @param mixed $haystack
 * @param mixed $needle
 * @param mixed $part
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.strchr.php
 */
function strchr($haystack, $needle, $part = null)
{
}

/**
 * Locale based string comparison
 *
 * @param string $str1 The first string.
 * @param string $str2 The second string.
 *
 * @return int Returns < 0 if <code>str1</code> is less than
 * <code>str2</code>; > 0 if
 * <code>str1</code> is greater than
 * <code>str2</code>, and 0 if they are equal.
 *
 * @since PHP 4 >= 4.0.5, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.strcoll.php
 */
function strcoll($str1, $str2)
{
}

/**
 * Find length of initial segment not matching mask
 *
 * @param string $subject The string to examine.
 * @param string $mask The string containing every disallowed character.
 * @param int $start The position in <code>subject</code> to
 * start searching.
 * @param int $length The length of the segment from <code>subject</code>
 * to examine.
 *
 * @return int Returns the length of the initial segment of <code>subject</code>
 * which consists entirely of characters <code>not</code> in <code>mask</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.strcspn.php
 */
function strcspn($subject, $mask, $start = null, $length = null)
{
}

/**
 * Append bucket to brigade
 *
 * @param resource $brigade
 * @param mixed $bucket
 *
 * @return void
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-bucket-append.php
 */
function stream_bucket_append($brigade, $bucket)
{
}

/**
 * Return a bucket object from the brigade for operating on
 *
 * @param resource $brigade
 *
 * @return mixed
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-bucket-make-writeable.php
 */
function stream_bucket_make_writeable($brigade)
{
}

/**
 * Create a new bucket for use on the current stream
 *
 * @param resource $stream
 * @param string $buffer
 *
 * @return mixed
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-bucket-new.php
 */
function stream_bucket_new($stream, $buffer)
{
}

/**
 * Prepend bucket to brigade
 *
 * @param resource $brigade <code>brigade</code> is a resource pointing to a <code>bucket brigade</code>
 * which contains one or more <code>bucket</code> objects.
 * @param mixed $bucket A bucket object.
 *
 * @return void No value is returned.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-bucket-prepend.php
 */
function stream_bucket_prepend($brigade, $bucket)
{
}

/**
 * Creates a stream context
 *
 * @param array $options Must be an associative array of associative arrays in the format
 * <code>$arr['wrapper']['option'] = $value</code>. Refer to context options for a list of available wrappers and options.
 * @param array $params Must be an associative array in the format
 * <code>$arr['parameter'] = $value</code>.
 * Refer to context parameters for
 * a listing of standard stream parameters.
 *
 * @return resource A stream context <code>resource</code>.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-context-create.php
 */
function stream_context_create(array $options = null, array $params = null)
{
}

/**
 * Retrieve the default stream context
 *
 * @param array $options As of PHP 5.3.0, the <code>stream_context_set_default</code> function
 * can be used to set the default context.
 *
 * @return resource A stream context <code>resource</code>.
 *
 * @since PHP 5 >= 5.1.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-context-get-default.php
 */
function stream_context_get_default(array $options = null)
{
}

/**
 * Retrieve options for a stream/wrapper/context
 *
 * @param resource $stream_or_context The <code>stream</code> or <code>context</code> to get options from
 *
 * @return array Returns an associative array with the options.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-context-get-options.php
 */
function stream_context_get_options($stream_or_context)
{
}

/**
 * Retrieves parameters from a context
 *
 * @param resource $stream_or_context A stream <code>resource</code> or a
 * context <code>resource</code>
 *
 * @return array Returns an associate array containing all context options and parameters.
 *
 * @since PHP 5 >= 5.3.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-context-get-params.php
 */
function stream_context_get_params($stream_or_context)
{
}

/**
 * Set the default stream context
 *
 * @param array $options The options to set for the default context.
 *
 * @return resource Returns the default stream context.
 *
 * @since PHP 5 >= 5.3.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-context-set-default.php
 */
function stream_context_set_default(array $options)
{
}

/**
 * Sets an option for a stream/wrapper/context
 *
 * @param resource $stream_or_context The stream or context resource to apply the options to.
 * @param string $wrapper
 * @param string $option
 * @param mixed $value
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-context-set-option.php
 */
function stream_context_set_option($stream_or_context, $wrapper, $option, $value)
{
}

/**
 * Set parameters for a stream/wrapper/context
 *
 * @param resource $stream_or_context The stream or context to apply the parameters too.
 * @param array $params An array of parameters to set.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-context-set-params.php
 */
function stream_context_set_params($stream_or_context, array $params)
{
}

/**
 * Copies data from one stream to another
 *
 * @param resource $source The source stream
 * @param resource $dest The destination stream
 * @param int|null $maxlength Maximum bytes to copy
 * @param int|null $offset The offset where to start to copy data
 *
 * @return int Returns the total count of bytes copied, or <code>FALSE</code> on failure.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-copy-to-stream.php
 */
function stream_copy_to_stream($source, $dest, $maxlength = -1, $offset = 0)
{
}

/**
 * Attach a filter to a stream
 *
 * @param resource $stream The target stream.
 * @param string $filtername The filter name.
 * @param int $read_write By default, <code>stream_filter_append</code> will
 * attach the filter to the <code>read filter chain</code>
 * if the file was opened for reading (i.e. File Mode:
 * <code>r</code>, and/or <code>+</code>). The filter
 * will also be attached to the <code>write filter chain</code>
 * if the file was opened for writing (i.e. File Mode:
 * <code>w</code>, <code>a</code>, and/or <code>+</code>).
 * <code>STREAM_FILTER_READ</code>,
 * <code>STREAM_FILTER_WRITE</code>, and/or
 * <code>STREAM_FILTER_ALL</code> can also be passed to the
 * <code>read_write</code> parameter to override this behavior.
 * @param mixed $params This filter will be added with the specified
 * <code>params</code> to the <code>end</code> of
 * the list and will therefore be called last during stream operations.
 * To add a filter to the beginning of the list, use
 * <code>stream_filter_prepend</code>.
 *
 * @return resource Returns a resource on success or <code>FALSE</code> on failure. The resource can be
 * used to refer to this filter instance during a call to
 * <code>stream_filter_remove</code>.
 * <code>FALSE</code> is returned if <code>stream</code> is not a resource or
 * if <code>filtername</code> cannot be located.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-filter-append.php
 */
function stream_filter_append($stream, $filtername, $read_write = null, $params = null)
{
}

/**
 * Attach a filter to a stream
 *
 * @param resource $stream The target stream.
 * @param string $filtername The filter name.
 * @param int $read_write By default, <code>stream_filter_prepend</code> will
 * attach the filter to the <code>read filter chain</code>
 * if the file was opened for reading (i.e. File Mode:
 * <code>r</code>, and/or <code>+</code>). The filter
 * will also be attached to the <code>write filter chain</code>
 * if the file was opened for writing (i.e. File Mode:
 * <code>w</code>, <code>a</code>, and/or <code>+</code>).
 * <code>STREAM_FILTER_READ</code>,
 * <code>STREAM_FILTER_WRITE</code>, and/or
 * <code>STREAM_FILTER_ALL</code> can also be passed to the
 * <code>read_write</code> parameter to override this behavior.
 * See <code>stream_filter_append</code> for an example of
 * using this parameter.
 * @param mixed $params This filter will be added with the specified <code>params</code>
 * to the <code>beginning</code> of the list and will therefore be
 * called first during stream operations. To add a filter to the end of the
 * list, use <code>stream_filter_append</code>.
 *
 * @return resource Returns a resource on success or <code>FALSE</code> on failure. The resource can be
 * used to refer to this filter instance during a call to
 * <code>stream_filter_remove</code>.
 * <code>FALSE</code> is returned if <code>stream</code> is not a resource or
 * if <code>filtername</code> cannot be located.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-filter-prepend.php
 */
function stream_filter_prepend($stream, $filtername, $read_write = null, $params = null)
{
}

/**
 * Register a user defined stream filter
 *
 * @param string $filtername The filter name to be registered.
 * @param string $classname To implement a filter, you need to define a class as an extension of
 * <code>php_user_filter</code> with a number of member
 * functions. When performing read/write operations on the stream
 * to which your filter is attached, PHP will pass the data through your
 * filter (and any other filters attached to that stream) so that the
 * data may be modified as desired. You must implement the methods
 * exactly as described in <code>php_user_filter</code> - doing
 * otherwise will lead to undefined behaviour.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 * <code>stream_filter_register</code> will return <code>FALSE</code> if the
 * <code>filtername</code> is already defined.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-filter-register.php
 */
function stream_filter_register($filtername, $classname)
{
}

/**
 * Remove a filter from a stream
 *
 * @param resource $stream_filter The stream filter to be removed.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 5 >= 5.1.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-filter-remove.php
 */
function stream_filter_remove($stream_filter)
{
}

/**
 * Reads remainder of a stream into a string
 *
 * @param resource $handle A stream resource (e.g. returned from <code>fopen</code>)
 * @param int|null $maxlength The maximum bytes to read. Defaults to -1 (read all the remaining
 * buffer).
 * @param int|null $offset Seek to the specified offset before reading. If this number is negative,
 * no seeking will occur and reading will start from the current position.
 *
 * @return string Returns a string or <code>FALSE</code> on failure.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-get-contents.php
 */
function stream_get_contents($handle, $maxlength = -1, $offset = -1)
{
}

/**
 * Retrieve list of registered filters
 *
 * @return array Returns an indexed array containing the name of all stream filters
 * available.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-get-filters.php
 */
function stream_get_filters()
{
}

/**
 * Gets line from stream resource up to a given delimiter
 *
 * @param resource $handle A valid file handle.
 * @param int $length The number of bytes to read from the handle.
 * @param string $ending An optional string delimiter.
 *
 * @return string Returns a string of up to <code>length</code> bytes read from the file
 * pointed to by <code>handle</code>.
 * If an error occurs, returns <code>FALSE</code>.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-get-line.php
 */
function stream_get_line($handle, $length, $ending = null)
{
}

/**
 * Retrieves header/meta data from streams/file pointers
 *
 * @param resource $stream The stream can be any stream created by <code>fopen</code>,
 * <code>fsockopen</code> and <code>pfsockopen</code>.
 *
 * @return array The result array contains the following items:
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-get-meta-data.php
 */
function stream_get_meta_data($stream)
{
}

/**
 * Retrieve list of registered socket transports
 *
 * @return array Returns an indexed array of socket transports names.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-get-transports.php
 */
function stream_get_transports()
{
}

/**
 * Retrieve list of registered streams
 *
 * @return array Returns an indexed array containing the name of all stream wrappers
 * available on the running system.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-get-wrappers.php
 */
function stream_get_wrappers()
{
}

/**
 * Checks if a stream is a local stream
 *
 * @param mixed $stream_or_url The stream <code>resource</code> or URL to check.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 5 >= 5.2.4, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-is-local.php
 */
function stream_is_local($stream_or_url)
{
}

/**
 * Alias of <code>stream_wrapper_register</code>
 *
 * @param mixed $protocol
 * @param mixed $classname
 * @param mixed $flags
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-register-wrapper.php
 */
function stream_register_wrapper($protocol, $classname, $flags = null)
{
}

/**
 * Resolve filename against the include path
 *
 * @param string $filename The filename to resolve.
 *
 * @return string Returns a <code>string</code> containing the resolved absolute filename, or <code>FALSE</code> on failure.
 *
 * @since PHP 5 >= 5.3.2, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-resolve-include-path.php
 */
function stream_resolve_include_path($filename)
{
}

/**
 * Runs the equivalent of the select() system call on the given
 * arrays of streams with a timeout specified by tv_sec and tv_usec
 *
 * @param array $read The streams listed in the <code>read</code> array will be watched to
 * see if characters become available for reading (more precisely, to see if
 * a read will not block - in particular, a stream resource is also ready on
 * end-of-file, in which case an <code>fread</code> will return
 * a zero length string).
 * @param array $write The streams listed in the <code>write</code> array will be
 * watched to see if a write will not block.
 * @param array $except The streams listed in the <code>except</code> array will be
 * watched for high priority exceptional ("out-of-band") data arriving.
 * @param int $tv_sec The <code>tv_sec</code> and <code>tv_usec</code>
 * together form the <code>timeout</code> parameter,
 * <code>tv_sec</code> specifies the number of seconds while
 * <code>tv_usec</code> the number of microseconds.
 * The <code>timeout</code> is an upper bound on the amount of time
 * that <code>stream_select</code> will wait before it returns.
 * If <code>tv_sec</code> and <code>tv_usec</code> are
 * both set to <code>0</code>, <code>stream_select</code> will
 * not wait for data - instead it will return immediately, indicating the
 * current status of the streams.
 * @param int|null $tv_usec See <code>tv_sec</code> description.
 *
 * @return int On success <code>stream_select</code> returns the number of
 * stream resources contained in the modified arrays, which may be zero if
 * the timeout expires before anything interesting happens. On error <code>FALSE</code>
 * is returned and a warning raised (this can happen if the system call is
 * interrupted by an incoming signal).
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-select.php
 */
function stream_select(array &$read, array &$write, array &$except, $tv_sec, $tv_usec = 0)
{
}

/**
 * Set blocking/non-blocking mode on a stream
 *
 * @param resource $stream The stream.
 * @param bool $mode If <code>mode</code> is <code>FALSE</code>, the given stream
 * will be switched to non-blocking mode, and if <code>TRUE</code>, it
 * will be switched to blocking mode. This affects calls like
 * <code>fgets</code> and <code>fread</code>
 * that read from the stream. In non-blocking mode an
 * <code>fgets</code> call will always return right away
 * while in blocking mode it will wait for data to become available
 * on the stream.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-set-blocking.php
 */
function stream_set_blocking($stream, $mode)
{
}

/**
 * Set the stream chunk size
 *
 * @param resource $fp The target stream.
 * @param int $chunk_size The desired new chunk size.
 *
 * @return int Returns the previous chunk size on success.
 * Will return <code>FALSE</code> if <code>chunk_size</code> is less than 1 or
 * greater than <code>PHP_INT_MAX</code>.
 *
 * @since PHP 5 >= 5.4.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-set-chunk-size.php
 */
function stream_set_chunk_size($fp, $chunk_size)
{
}

/**
 * Set read file buffering on the given stream
 *
 * @param resource $stream The file pointer.
 * @param int $buffer The number of bytes to buffer. If <code>buffer</code>
 * is 0 then read operations are unbuffered. This ensures that all reads
 * with <code>fread</code> are completed before other processes are
 * allowed to read from that input stream.
 *
 * @return int Returns 0 on success, or another value if the request
 * cannot be honored.
 *
 * @since PHP 5 >= 5.3.3, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-set-read-buffer.php
 */
function stream_set_read_buffer($stream, $buffer)
{
}

/**
 * Set timeout period on a stream
 *
 * @param resource $stream The target stream.
 * @param int $seconds The seconds part of the timeout to be set.
 * @param int|null $microseconds The microseconds part of the timeout to be set.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-set-timeout.php
 */
function stream_set_timeout($stream, $seconds, $microseconds = 0)
{
}

/**
 * Sets write file buffering on the given stream
 *
 * @param resource $stream The file pointer.
 * @param int $buffer The number of bytes to buffer. If <code>buffer</code>
 * is 0 then write operations are unbuffered. This ensures that all writes
 * with <code>fwrite</code> are completed before other processes are
 * allowed to write to that output stream.
 *
 * @return int Returns 0 on success, or another value if the request cannot be honored.
 *
 * @since PHP 4 >= 4.3.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-set-write-buffer.php
 */
function stream_set_write_buffer($stream, $buffer)
{
}

/**
 * Accept a connection on a socket created by <code>stream_socket_server</code>
 *
 * @param resource $server_socket The server socket to accept a connection from.
 * @param float|null $timeout Override the default socket accept timeout. Time should be given in
 * seconds.
 * @param string $peername Will be set to the name (address) of the client which connected, if
 * included and available from the selected transport.
 *
 * @return resource Returns a stream to the accepted socket connection or <code>FALSE</code> on failure.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-socket-accept.php
 */
function stream_socket_accept($server_socket, $timeout = /* ini_get("default_socket_timeout") */ null, &$peername = null)
{
}

/**
 * Open Internet or Unix domain socket connection
 *
 * @param string $remote_socket Address to the socket to connect to.
 * @param int $errno Will be set to the system level error number if connection fails.
 * @param string $errstr Will be set to the system level error message if the connection fails.
 * @param float|null $timeout Number of seconds until the <code>connect()</code> system call
 * should timeout.
 * <blockquote>
 * This parameter only applies when not making asynchronous
 * connection attempts.
 * </blockquote>
 * <blockquote>
 * To set a timeout for reading/writing data over the socket, use the
 * <code>stream_set_timeout</code>, as the
 * <code>timeout</code> only applies while making connecting
 * the socket.
 * </blockquote>
 * @param int|null $flags Bitmask field which may be set to any combination of connection flags.
 * Currently the select of connection flags is limited to
 * <code>STREAM_CLIENT_CONNECT</code> (default),
 * <code>STREAM_CLIENT_ASYNC_CONNECT</code> and
 * <code>STREAM_CLIENT_PERSISTENT</code>.
 * @param resource $context A valid context resource created with <code>stream_context_create</code>.
 *
 * @return resource On success a stream resource is returned which may
 * be used together with the other file functions (such as
 * <code>fgets</code>, <code>fgetss</code>,
 * <code>fwrite</code>, <code>fclose</code>, and
 * <code>feof</code>), <code>FALSE</code> on failure.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-socket-client.php
 */
function stream_socket_client($remote_socket, &$errno = null, &$errstr = null, $timeout = /* ini_get("default_socket_timeout") */ null, $flags = STREAM_CLIENT_CONNECT, $context = null)
{
}

/**
 * Turns encryption on/off on an already connected socket
 *
 * @param resource $stream The stream resource.
 * @param bool $enable Enable/disable cryptography on the stream.
 * @param int $crypto_type Setup encryption on the stream.
 * Valid methods are
 * <ul>
 * <code>STREAM_CRYPTO_METHOD_SSLv2_CLIENT</code>
 * <code>STREAM_CRYPTO_METHOD_SSLv3_CLIENT</code>
 * <code>STREAM_CRYPTO_METHOD_SSLv23_CLIENT</code>
 * <code>STREAM_CRYPTO_METHOD_ANY_CLIENT</code>
 * <code>STREAM_CRYPTO_METHOD_TLS_CLIENT</code>
 * <code>STREAM_CRYPTO_METHOD_TLSv1_0_CLIENT</code>
 * <code>STREAM_CRYPTO_METHOD_TLSv1_1_CLIENT</code>
 * <code>STREAM_CRYPTO_METHOD_TLSv1_2_CLIENT</code>
 * <code>STREAM_CRYPTO_METHOD_SSLv2_SERVER</code>
 * <code>STREAM_CRYPTO_METHOD_SSLv3_SERVER</code>
 * <code>STREAM_CRYPTO_METHOD_SSLv23_SERVER</code>
 * <code>STREAM_CRYPTO_METHOD_ANY_SERVER</code>
 * <code>STREAM_CRYPTO_METHOD_TLS_SERVER</code>
 * <code>STREAM_CRYPTO_METHOD_TLSv1_0_SERVER</code>
 * <code>STREAM_CRYPTO_METHOD_TLSv1_1_SERVER</code>
 * <code>STREAM_CRYPTO_METHOD_TLSv1_2_SERVER</code>
 * </ul>
 * @param resource $session_stream Seed the stream with settings from <code>session_stream</code>.
 *
 * @return mixed Returns <code>TRUE</code> on success, <code>FALSE</code> if negotiation has failed or
 * <code>0</code> if there isn't enough data and you should try again
 * (only for non-blocking sockets).
 *
 * @since PHP 5 >= 5.1.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-socket-enable-crypto.php
 */
function stream_socket_enable_crypto($stream, $enable, $crypto_type = null, $session_stream = null)
{
}

/**
 * Retrieve the name of the local or remote sockets
 *
 * @param resource $handle The socket to get the name of.
 * @param bool $want_peer If set to <code>TRUE</code> the <code>remote</code> socket name will be returned, if set
 * to <code>FALSE</code> the <code>local</code> socket name will be returned.
 *
 * @return string The name of the socket.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-socket-get-name.php
 */
function stream_socket_get_name($handle, $want_peer)
{
}

/**
 * Creates a pair of connected, indistinguishable socket streams
 *
 * @param int $domain The protocol family to be used: <code>STREAM_PF_INET</code>,
 * <code>STREAM_PF_INET6</code> or
 * <code>STREAM_PF_UNIX</code>
 * @param int $type The type of communication to be used:
 * <code>STREAM_SOCK_DGRAM</code>,
 * <code>STREAM_SOCK_RAW</code>,
 * <code>STREAM_SOCK_RDM</code>,
 * <code>STREAM_SOCK_SEQPACKET</code> or
 * <code>STREAM_SOCK_STREAM</code>
 * @param int $protocol The protocol to be used: <code>STREAM_IPPROTO_ICMP</code>,
 * <code>STREAM_IPPROTO_IP</code>,
 * <code>STREAM_IPPROTO_RAW</code>,
 * <code>STREAM_IPPROTO_TCP</code> or
 * <code>STREAM_IPPROTO_UDP</code>
 *
 * @return array Returns an <code>array</code> with the two socket resources on success, or
 * <code>FALSE</code> on failure.
 *
 * @since PHP 5 >= 5.1.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-socket-pair.php
 */
function stream_socket_pair($domain, $type, $protocol)
{
}

/**
 * Receives data from a socket, connected or not
 *
 * @param resource $socket The remote socket.
 * @param int $length The number of bytes to receive from the <code>socket</code>.
 * @param int|null $flags The value of <code>flags</code> can be any combination
 * of the following:
 * <table>
 * <title>Possible values for <code>flags</code></title>
 * <tbody>
 * <tr>
 * <td><code>STREAM_OOB</code></td><td>
 * Process OOB (<code>out-of-band</code>) data.
 * </td>
 * </tr>
 * <tr>
 * <td><code>STREAM_PEEK</code></td><td>
 * Retrieve data from the socket, but do not consume the buffer.
 * Subsequent calls to <code>fread</code> or
 * <code>stream_socket_recvfrom</code> will see
 * the same data.
 * </td>
 * </tr>
 * </tbody>
 * </table>
 * @param string $address If <code>address</code> is provided it will be populated with
 * the address of the remote socket.
 *
 * @return string Returns the read data, as a string
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-socket-recvfrom.php
 */
function stream_socket_recvfrom($socket, $length, $flags = 0, &$address = null)
{
}

/**
 * Sends a message to a socket, whether it is connected or not
 *
 * @param resource $socket The socket to send <code>data</code> to.
 * @param string $data The data to be sent.
 * @param int|null $flags The value of <code>flags</code> can be any combination
 * of the following:
 * <table>
 * <title>possible values for <code>flags</code></title>
 * <tbody>
 * <tr>
 * <td><code>STREAM_OOB</code></td><td>
 * Process OOB (out-of-band) data.
 * </td>
 * </tr>
 * </tbody>
 * </table>
 * @param string $address The address specified when the socket stream was created will be used
 * unless an alternate address is specified in <code>address</code>.
 *
 * @return int Returns a result code, as an integer.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-socket-sendto.php
 */
function stream_socket_sendto($socket, $data, $flags = 0, $address = null)
{
}

/**
 * Create an Internet or Unix domain server socket
 *
 * @param string $local_socket The type of socket created is determined by the transport specified
 * using standard URL formatting: <code>transport://target</code>.
 * @param int $errno If the optional <code>errno</code> and <code>errstr</code>
 * arguments are present they will be set to indicate the actual system
 * level error that occurred in the system-level <code>socket()</code>,
 * <code>bind()</code>, and <code>listen()</code> calls. If
 * the value returned in <code>errno</code> is
 * <code>0</code> and the function returned <code>FALSE</code>, it is an
 * indication that the error occurred before the <code>bind()</code>
 * call. This is most likely due to a problem initializing the socket.
 * Note that the <code>errno</code> and
 * <code>errstr</code> arguments will always be passed by reference.
 * @param string $errstr See <code>errno</code> description.
 * @param int|null $flags A bitmask field which may be set to any combination of socket creation
 * flags.
 * @param resource $context
 *
 * @return resource Returns the created stream, or <code>FALSE</code> on error.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-socket-server.php
 */
function stream_socket_server($local_socket, &$errno = null, &$errstr = null, $flags = STREAM_SERVER_BIND | STREAM_SERVER_LISTEN, $context = null)
{
}

/**
 * Shutdown a full-duplex connection
 *
 * @param resource $stream An open stream (opened with <code>stream_socket_client</code>,
 * for example)
 * @param int $how One of the following constants: <code>STREAM_SHUT_RD</code>
 * (disable further receptions), <code>STREAM_SHUT_WR</code>
 * (disable further transmissions) or
 * <code>STREAM_SHUT_RDWR</code> (disable further receptions and
 * transmissions).
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 5 >= 5.2.1, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-socket-shutdown.php
 */
function stream_socket_shutdown($stream, $how)
{
}

/**
 * Tells whether the stream supports locking
 *
 * @param resource $stream The stream to check.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 5 >= 5.3.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-supports-lock.php
 */
function stream_supports_lock($stream)
{
}

/**
 * Register a URL wrapper implemented as a PHP class
 *
 * @param string $protocol The wrapper name to be registered.
 * @param string $classname The classname which implements the <code>protocol</code>.
 * @param int|null $flags Should be set to <code>STREAM_IS_URL</code> if
 * <code>protocol</code> is a URL protocol. Default is 0, local
 * stream.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 * <code>stream_wrapper_register</code> will return <code>FALSE</code> if the
 * <code>protocol</code> already has a handler.
 *
 * @since PHP 4 >= 4.3.2, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-wrapper-register.php
 */
function stream_wrapper_register($protocol, $classname, $flags = 0)
{
}

/**
 * Restores a previously unregistered built-in wrapper
 *
 * @param string $protocol
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 5 >= 5.1.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-wrapper-restore.php
 */
function stream_wrapper_restore($protocol)
{
}

/**
 * Unregister a URL wrapper
 *
 * @param string $protocol
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 5 >= 5.1.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stream-wrapper-unregister.php
 */
function stream_wrapper_unregister($protocol)
{
}

/**
 * Strip HTML and PHP tags from a string
 *
 * @param string $str The input string.
 * @param string $allowable_tags You can use the optional second parameter to specify tags which should
 * not be stripped.
 *
 * @return string Returns the stripped string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.strip-tags.php
 */
function strip_tags($str, $allowable_tags = null)
{
}

/**
 * Un-quote string quoted with <code>addcslashes</code>
 *
 * @param string $str The string to be unescaped.
 *
 * @return string Returns the unescaped string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stripcslashes.php
 */
function stripcslashes($str)
{
}

/**
 * Find the position of the first occurrence of a case-insensitive substring in a string
 *
 * @param string $haystack The string to search in.
 * @param string $needle Note that the <code>needle</code> may be a string of one or
 * more characters.
 * @param int|null $offset If specified, search will start this number of characters counted from
 * the beginning of the string. If the offset is negative, the search will start
 * this number of characters counted from the end of the string.
 *
 * @return int Returns the position of where the needle exists relative to the beginnning of
 * the <code>haystack</code> string (independent of offset).
 * Also note that string positions start at 0, and not 1.
 * Returns <code>FALSE</code> if the needle was not found.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stripos.php
 */
function stripos($haystack, $needle, $offset = 0)
{
}

/**
 * Un-quotes a quoted string
 *
 * @param string $str The input string.
 *
 * @return string Returns a string with backslashes stripped off.
 * (<code>\'</code> becomes <code>'</code> and so on.)
 * Double backslashes (<code>\\</code>) are made into a single
 * backslash (<code>\</code>).
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stripslashes.php
 */
function stripslashes($str)
{
}

/**
 * Case-insensitive <code>strstr</code>
 *
 * @param string $haystack The string to search in
 * @param mixed $needle If <code>needle</code> is not a string, it is converted to
 * an integer and applied as the ordinal value of a character.
 * @param bool|null $before_needle If <code>TRUE</code>, <code>stristr</code>
 * returns the part of the <code>haystack</code> before the
 * first occurrence of the <code>needle</code> (excluding needle).
 *
 * @return string Returns the matched substring. If <code>needle</code> is not
 * found, returns <code>FALSE</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.stristr.php
 */
function stristr($haystack, $needle, $before_needle = false)
{
}

/**
 * Case insensitive string comparisons using a "natural order" algorithm
 *
 * @param string $str1 The first string.
 * @param string $str2 The second string.
 *
 * @return int Similar to other string comparison functions, this one returns < 0 if
 * <code>str1</code> is less than <code>str2</code> >
 * 0 if <code>str1</code> is greater than
 * <code>str2</code>, and 0 if they are equal.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.strnatcasecmp.php
 */
function strnatcasecmp($str1, $str2)
{
}

/**
 * String comparisons using a "natural order" algorithm
 *
 * @param string $str1 The first string.
 * @param string $str2 The second string.
 *
 * @return int Similar to other string comparison functions, this one returns < 0 if
 * <code>str1</code> is less than <code>str2</code>; >
 * 0 if <code>str1</code> is greater than
 * <code>str2</code>, and 0 if they are equal.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.strnatcmp.php
 */
function strnatcmp($str1, $str2)
{
}

/**
 * Search a string for any of a set of characters
 *
 * @param string $haystack The string where <code>char_list</code> is looked for.
 * @param string $char_list This parameter is case sensitive.
 *
 * @return string Returns a string starting from the character found, or <code>FALSE</code> if it is
 * not found.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.strpbrk.php
 */
function strpbrk($haystack, $char_list = null)
{
}

/**
 * Find the position of the first occurrence of a substring in a string
 *
 * @param string $haystack The string to search in.
 * @param mixed $needle If <code>needle</code> is not a string, it is converted
 * to an integer and applied as the ordinal value of a character.
 * @param int|null $offset If specified, search will start this number of characters counted from
 * the beginning of the string. If the offset is negative, the search will start
 * this number of characters counted from the end of the string.
 *
 * @return int Returns the position of where the needle exists relative to the beginning of
 * the <code>haystack</code> string (independent of offset).
 * Also note that string positions start at 0, and not 1.
 * Returns <code>FALSE</code> if the needle was not found.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.strpos.php
 */
function strpos($haystack, $needle, $offset = 0)
{
}

/**
 * Parse a time/date generated with <code>strftime</code>
 *
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @param string $date The string to parse (e.g. returned from <code>strftime</code>).
 * @param string $format The format used in <code>date</code> (e.g. the same as
 * used in <code>strftime</code>). Note that some of the format
 * options available to <code>strftime</code> may not have any
 * effect within <code>strptime</code>; the exact subset that are
 * supported will vary based on the operating system and C library in
 * use.
 *
 * @return array Returns an array or <code>FALSE</code> on failure.
 * <table>
 * <title>The following parameters are returned in the array</title>
 * <thead>
 * <tr>
 * <td>parameters</td><td>Description</td>
 * </tr>
 * </thead>
 * <tbody>
 * <tr>
 * <td><code>"tm_sec"</code></td><td>Seconds after the minute (0-61)</td>
 * </tr>
 * <tr>
 * <td><code>"tm_min"</code></td><td>Minutes after the hour (0-59)</td>
 * </tr>
 * <tr>
 * <td><code>"tm_hour"</code></td><td>Hour since midnight (0-23)</td>
 * </tr>
 * <tr>
 * <td><code>"tm_mday"</code></td><td>Day of the month (1-31)</td>
 * </tr>
 * <tr>
 * <td><code>"tm_mon"</code></td><td>Months since January (0-11)</td>
 * </tr>
 * <tr>
 * <td><code>"tm_year"</code></td><td>Years since 1900</td>
 * </tr>
 * <tr>
 * <td><code>"tm_wday"</code></td><td>Days since Sunday (0-6)</td>
 * </tr>
 * <tr>
 * <td><code>"tm_yday"</code></td><td>Days since January 1 (0-365)</td>
 * </tr>
 * <tr>
 * <td><code>"unparsed"</code></td><td>the <code>date</code> part which was not
 * recognized using the specified <code>format</code></td>
 * </tr>
 * </tbody>
 * </table>
 *
 * @since PHP 5 >= 5.1.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.strptime.php
 */
function strptime($date, $format)
{
}

/**
 * Find the last occurrence of a character in a string
 *
 * @param string $haystack The string to search in
 * @param mixed $needle If <code>needle</code> contains more than one character,
 * only the first is used. This behavior is different from that of
 * <code>strstr</code>.
 *
 * @return string This function returns the portion of string, or <code>FALSE</code> if
 * <code>needle</code> is not found.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.strrchr.php
 */
function strrchr($haystack, $needle)
{
}

/**
 * Reverse a string
 *
 * @param string $string The string to be reversed.
 *
 * @return string Returns the reversed string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.strrev.php
 */
function strrev($string)
{
}

/**
 * Find the position of the last occurrence of a case-insensitive substring in a string
 *
 * @param string $haystack The string to search in.
 * @param string $needle If <code>needle</code> is not a string, it is converted
 * to an integer and applied as the ordinal value of a character.
 * @param int|null $offset If specified, search will start this number of characters counted from the
 * beginning of the string. If the value is negative, search will instead start
 * from that many characters from the end of the string, searching backwards.
 *
 * @return int Returns the position where the needle exists relative to the beginnning of
 * the <code>haystack</code> string (independent of search direction
 * or offset).
 * Also note that string positions start at 0, and not 1.
 * Returns <code>FALSE</code> if the needle was not found.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.strripos.php
 */
function strripos($haystack, $needle, $offset = 0)
{
}

/**
 * Find the position of the last occurrence of a substring in a string
 *
 * @param string $haystack The string to search in.
 * @param string $needle If <code>needle</code> is not a string, it is converted
 * to an integer and applied as the ordinal value of a character.
 * @param int|null $offset If specified, search will start this number of characters counted from the
 * beginning of the string. If the value is negative, search will instead start
 * from that many characters from the end of the string, searching backwards.
 *
 * @return int Returns the position where the needle exists relative to the beginning of
 * the <code>haystack</code> string (independent of search direction
 * or offset).
 * Also note that string positions start at 0, and not 1.
 * Returns <code>FALSE</code> if the needle was not found.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.strrpos.php
 */
function strrpos($haystack, $needle, $offset = 0)
{
}

/**
 * Finds the length of the initial segment of a string consisting
 * entirely of characters contained within a given mask
 *
 * @param string $subject The string to examine.
 * @param string $mask The list of allowable characters.
 * @param int $start The position in <code>subject</code> to
 * start searching.
 * @param int $length The length of the segment from <code>subject</code>
 * to examine.
 *
 * @return int Returns the length of the initial segment of <code>subject</code>
 * which consists entirely of characters in <code>mask</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.strspn.php
 */
function strspn($subject, $mask, $start = null, $length = null)
{
}

/**
 * Find the first occurrence of a string
 *
 * @param string $haystack The input string.
 * @param mixed $needle If <code>needle</code> is not a string, it is converted to
 * an integer and applied as the ordinal value of a character.
 * @param bool|null $before_needle If <code>TRUE</code>, <code>strstr</code> returns
 * the part of the <code>haystack</code> before the first
 * occurrence of the <code>needle</code> (excluding the needle).
 *
 * @return string Returns the portion of string, or <code>FALSE</code> if <code>needle</code>
 * is not found.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.strstr.php
 */
function strstr($haystack, $needle, $before_needle = false)
{
}

/**
 * Tokenize string
 *
 * @param string $str The <code>string</code> being split up into smaller strings (tokens).
 * @param string $token The delimiter used when splitting up <code>str</code>.
 *
 * @return string A <code>string</code> token.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.strtok.php
 */
function strtok($str, $token)
{
}

/**
 * Make a string lowercase
 *
 * @param string $string The input string.
 *
 * @return string Returns the lowercased string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.strtolower.php
 */
function strtolower($string)
{
}

/**
 * Make a string uppercase
 *
 * @param string $string The input string.
 *
 * @return string Returns the uppercased string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.strtoupper.php
 */
function strtoupper($string)
{
}

/**
 * Translate characters or replace substrings
 *
 * @param string $str The <code>string</code> being translated.
 * @param string $from The <code>string</code> being translated to <code>to</code>.
 * @param string $to The <code>string</code> replacing <code>from</code>.
 *
 * @return string Returns the translated <code>string</code>.
 * If <code>replace_pairs</code> contains a key which
 * is an empty <code>string</code> (<code>""</code>),
 * <code>FALSE</code> will be returned. If the <code>str</code> is not a scalar
 * then it is not typecasted into a string, instead a warning is raised and
 * <code>NULL</code> is returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.strtr.php
 */
function strtr($str, $from, $to = null)
{
}

/**
 * Get string value of a variable
 *
 * @param mixed $var The variable that is being converted to a <code>string</code>.
 *
 * @return string The <code>string</code> value of <code>var</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.strval.php
 */
function strval($var)
{
}

/**
 * Return part of a string
 *
 * @param string $string The input string. Must be one character or longer.
 * @param int $start If <code>start</code> is non-negative, the returned string
 * will start at the <code>start</code>'th position in
 * <code>string</code>, counting from zero. For instance,
 * in the string '<code>abcdef</code>', the character at
 * position <code>0</code> is '<code>a</code>', the
 * character at position <code>2</code> is
 * '<code>c</code>', and so forth.
 * @param int $length If <code>length</code> is given and is positive, the string
 * returned will contain at most <code>length</code> characters
 * beginning from <code>start</code> (depending on the length of
 * <code>string</code>).
 *
 * @return string Returns the extracted part of <code>string</code>; or <code>FALSE</code> on failure, or
 * an empty string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.substr.php
 */
function substr($string, $start, $length = null)
{
}

/**
 * Binary safe comparison of two strings from an offset, up to length characters
 *
 * @param string $main_str The main string being compared.
 * @param string $str The secondary string being compared.
 * @param int $offset The start position for the comparison. If negative, it starts counting
 * from the end of the string.
 * @param int $length The length of the comparison. The default value is the largest of the
 * length of the <code>str</code> compared to the length of
 * <code>main_str</code> less the
 * <code>offset</code>.
 * @param bool|null $case_insensitivity If <code>case_insensitivity</code> is <code>TRUE</code>, comparison is
 * case insensitive.
 *
 * @return int Returns < 0 if <code>main_str</code> from position
 * <code>offset</code> is less than <code>str</code>, >
 * 0 if it is greater than <code>str</code>, and 0 if they are equal.
 * If <code>offset</code> is equal to or greater than the length of
 * <code>main_str</code>, or the <code>length</code> is
 * set and is less than 1 (prior to PHP 5.5.11),
 * <code>substr_compare</code> prints a warning and returns
 * <code>FALSE</code>.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.substr-compare.php
 */
function substr_compare($main_str, $str, $offset, $length = null, $case_insensitivity = false)
{
}

/**
 * Count the number of substring occurrences
 *
 * @param string $haystack The string to search in
 * @param string $needle The substring to search for
 * @param int|null $offset The offset where to start counting. If the offset is negative, counting
 * starts from the end of the string.
 * @param int $length The maximum length after the specified offset to search for the
 * substring. It outputs a warning if the offset plus the length is
 * greater than the <code>haystack</code> length.
 * A negative length counts from the end of <code>haystack</code>.
 *
 * @return int This function returns an <code>integer</code>.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.substr-count.php
 */
function substr_count($haystack, $needle, $offset = 0, $length = null)
{
}

/**
 * Replace text within a portion of a string
 *
 * @param mixed $string The input string.
 * @param mixed $replacement The replacement string.
 * @param mixed $start If <code>start</code> is non-negative, the replacing will
 * begin at the <code>start</code>'th offset into
 * <code>string</code>.
 * @param mixed $length If given and is positive, it represents the length of the portion of
 * <code>string</code> which is to be replaced. If it is
 * negative, it represents the number of characters from the end of
 * <code>string</code> at which to stop replacing. If it
 * is not given, then it will default to strlen(
 * <code>string</code> ); i.e. end the replacing at the
 * end of <code>string</code>. Of course, if
 * <code>length</code> is zero then this function will have the
 * effect of inserting <code>replacement</code> into
 * <code>string</code> at the given
 * <code>start</code> offset.
 *
 * @return mixed The result string is returned. If <code>string</code> is an
 * array then array is returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.substr-replace.php
 */
function substr_replace($string, $replacement, $start, $length = null)
{
}

/**
 * Creates a symbolic link
 *
 * @param string $target Target of the link.
 * @param string $link The link name.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.symlink.php
 */
function symlink($target, $link)
{
}

/**
 * Returns directory path used for temporary files
 *
 * @return string Returns the path of the temporary directory.
 *
 * @since PHP 5 >= 5.2.1, PHP 7
 *
 * @link http://www.php.net/manual/en/function.sys-get-temp-dir.php
 */
function sys_get_temp_dir()
{
}

/**
 * Gets system load average
 *
 * WARNING: this may be available on POSIX but not on Windows
 *
 * @return array Returns an <code>array</code> with three samples (last 1, 5 and 15
 * minutes).
 *
 * @since PHP 5 >= 5.1.3, PHP 7
 *
 * @link http://www.php.net/manual/en/function.sys-getloadavg.php
 */
function sys_getloadavg()
{
}

/**
 * Generate a system log message
 *
 * @param int $priority <code>priority</code> is a combination of the facility and
 * the level. Possible values are:
 * <table>
 * <title><code>syslog</code> Priorities (in descending order)</title>
 * <thead>
 * <tr>
 * <td>Constant</td><td>Description</td>
 * </tr>
 * </thead>
 * <tbody>
 * <tr>
 * <td><code>LOG_EMERG</code></td><td>system is unusable</td>
 * </tr>
 * <tr>
 * <td><code>LOG_ALERT</code></td><td>action must be taken immediately</td>
 * </tr>
 * <tr>
 * <td><code>LOG_CRIT</code></td><td>critical conditions</td>
 * </tr>
 * <tr>
 * <td><code>LOG_ERR</code></td><td>error conditions</td>
 * </tr>
 * <tr>
 * <td><code>LOG_WARNING</code></td><td>warning conditions</td>
 * </tr>
 * <tr>
 * <td><code>LOG_NOTICE</code></td><td>normal, but significant, condition</td>
 * </tr>
 * <tr>
 * <td><code>LOG_INFO</code></td><td>informational message</td>
 * </tr>
 * <tr>
 * <td><code>LOG_DEBUG</code></td><td>debug-level message</td>
 * </tr>
 * </tbody>
 * </table>
 * @param string $message The message to send, except that the two characters
 * <code>%m</code> will be replaced by the error message string
 * (strerror) corresponding to the present value of
 * <errortype>errno</errortype>.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.syslog.php
 */
function syslog($priority, $message)
{
}

/**
 * Execute an external program and display the output
 *
 * @param string $command The command that will be executed.
 * @param int $return_var If the <code>return_var</code> argument is present, then the
 * return status of the executed command will be written to this
 * variable.
 *
 * @return string Returns the last line of the command output on success, and <code>FALSE</code>
 * on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.system.php
 */
function system($command, &$return_var = null)
{
}

/**
 * Tangent
 *
 * @param float $arg The argument to process in radians
 *
 * @return float The tangent of <code>arg</code>
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.tan.php
 */
function tan($arg)
{
}

/**
 * Hyperbolic tangent
 *
 * @param float $arg The argument to process
 *
 * @return float The hyperbolic tangent of <code>arg</code>
 *
 * @since PHP 4 >= 4.1.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.tanh.php
 */
function tanh($arg)
{
}

/**
 * Create file with unique file name
 *
 * @param string $dir The directory where the temporary filename will be created.
 * @param string $prefix The prefix of the generated temporary filename.
 *
 * @return string Returns the new temporary filename (with path), or <code>FALSE</code> on
 * failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.tempnam.php
 */
function tempnam($dir, $prefix)
{
}

/**
 * Delay for a number of seconds and nanoseconds
 *
 * @param int $seconds Must be a non-negative integer.
 * @param int $nanoseconds Must be a non-negative integer less than 1 billion.
 *
 * @return mixed Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 * If the delay was interrupted by a signal, an associative array will be
 * returned with the components:
 * <ul>
 * <code>seconds</code> - number of seconds remaining in
 * the delay
 * <code>nanoseconds</code> - number of nanoseconds
 * remaining in the delay
 * </ul>
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.time-nanosleep.php
 */
function time_nanosleep($seconds, $nanoseconds)
{
}

/**
 * Make the script sleep until the specified time
 *
 * @param float $timestamp The timestamp when the script should wake.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 5 >= 5.1.0, PHP 7
 *
 * @link http://www.php.net/manual/en/function.time-sleep-until.php
 */
function time_sleep_until($timestamp)
{
}

/**
 * Creates a temporary file
 *
 * @return resource Returns a file handle, similar to the one returned by
 * <code>fopen</code>, for the new file or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.tmpfile.php
 */
function tmpfile()
{
}

/**
 * Sets access and modification time of file
 *
 * @param string $filename The name of the file being touched.
 * @param int|null $time The touch time. If <code>time</code> is not supplied,
 * the current system time is used.
 * @param int $atime If present, the access time of the given filename is set to
 * the value of <code>atime</code>. Otherwise, it is set to
 * the value passed to the <code>time</code> parameter.
 * If neither are present, the current system time is used.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.touch.php
 */
function touch($filename, $time = /* time() */ null, $atime = null)
{
}

/**
 * Strip whitespace (or other characters) from the beginning and end of a string
 *
 * @param string $str The <code>string</code> that will be trimmed.
 * @param string|null $character_mask Optionally, the stripped characters can also be specified using
 * the <code>character_mask</code> parameter.
 * Simply list all characters that you want to be stripped. With
 * <code>..</code> you can specify a range of characters.
 *
 * @return string The trimmed string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.trim.php
 */
function trim($str, $character_mask = " \t\n\r\0\x0B")
{
}

/**
 * Sort an array with a user-defined comparison function and maintain index association
 *
 * @param array $array The input array.
 * @param callable $value_compare_func See <code>usort</code> and <code>uksort</code> for
 * examples of user-defined comparison functions.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.uasort.php
 */
function uasort(array &$array, callable $value_compare_func)
{
}

/**
 * Make a string's first character uppercase
 *
 * @param string $str The input string.
 *
 * @return string Returns the resulting string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ucfirst.php
 */
function ucfirst($str)
{
}

/**
 * Uppercase the first character of each word in a string
 *
 * @param string $str The input string.
 * @param string|null $delimiters The optional <code>delimiters</code> contains the word separator characters.
 *
 * @return string Returns the modified string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.ucwords.php
 */
function ucwords($str, $delimiters = " \t\r\n\f\v")
{
}

/**
 * Sort an array by keys using a user-defined comparison function
 *
 * @param array $array The input array.
 * @param callable $key_compare_func The comparison function must return an integer less than, equal to, or greater than zero if the first argument is considered to be respectively less than, equal to, or greater than the second.
 * Note that before PHP 7.0.0 this integer had to be in the range from -2147483648 to 2147483647.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.uksort.php
 */
function uksort(array &$array, callable $key_compare_func)
{
}

/**
 * Changes the current umask
 *
 * @param int $mask The new umask.
 *
 * @return int <code>umask</code> without arguments simply returns the
 * current umask otherwise the old umask is returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.umask.php
 */
function umask($mask = null)
{
}

/**
 * Generate a unique ID
 *
 * @param string|null $prefix Can be useful, for instance, if you generate identifiers
 * simultaneously on several hosts that might happen to generate the
 * identifier at the same microsecond.
 * @param bool|null $more_entropy If set to <code>TRUE</code>, <code>uniqid</code> will add additional
 * entropy (using the combined linear congruential generator) at the end
 * of the return value, which increases the likelihood that the result
 * will be unique.
 *
 * @return string Returns timestamp based unique identifier as a string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.uniqid.php
 */
function uniqid($prefix = "", $more_entropy = false)
{
}

/**
 * Deletes a file
 *
 * @param string $filename Path to the file.
 * @param resource $context
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.unlink.php
 */
function unlink($filename, $context = null)
{
}

/**
 * Unpack data from binary string
 *
 * @param string $format See <code>pack</code> for an explanation of the format codes.
 * @param string $data The packed data.
 *
 * @return array Returns an associative array containing unpacked elements of binary
 * string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.unpack.php
 */
function unpack($format, $data)
{
}

/**
 * De-register a function for execution on each tick
 *
 * @param string $function_name The function name, as a string.
 *
 * @return void No value is returned.
 *
 * @since PHP 4 >= 4.0.3, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.unregister-tick-function.php
 */
function unregister_tick_function($function_name)
{
}

/**
 * Creates a PHP value from a stored representation
 *
 * @param string $str The serialized string.
 *
 * @return mixed The converted value is returned, and can be a <code>boolean</code>,
 * <code>integer</code>, <code>float</code>, <code>string</code>,
 * <code>array</code> or <code>object</code>.
 * In case the passed string is not unserializeable, <code>FALSE</code> is returned and
 * <code>E_NOTICE</code> is issued.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.unserialize.php
 */
function unserialize($str)
{
}

/**
 * Decodes URL-encoded string
 *
 * @param string $str The string to be decoded.
 *
 * @return string Returns the decoded string.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.urldecode.php
 */
function urldecode($str)
{
}

/**
 * URL-encodes string
 *
 * @param string $str The string to be encoded.
 *
 * @return string Returns a string in which all non-alphanumeric characters except
 * <code>-_.</code> have been replaced with a percent
 * (<code>%</code>) sign followed by two hex digits and spaces encoded
 * as plus (<code>+</code>) signs. It is encoded the same way that the
 * posted data from a WWW form is encoded, that is the same way as in
 * <code>application/x-www-form-urlencoded</code> media type. This
 * differs from the RFC 3986 encoding (see
 * <code>rawurlencode</code>) in that for historical reasons, spaces
 * are encoded as plus (+) signs.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.urlencode.php
 */
function urlencode($str)
{
}

/**
 * Delay execution in microseconds
 *
 * @param int $micro_seconds Halt time in microseconds. A microsecond is one millionth of a
 * second.
 *
 * @return void No value is returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.usleep.php
 */
function usleep($micro_seconds)
{
}

/**
 * Sort an array by values using a user-defined comparison function
 *
 * @param array $array The input array.
 * @param callable $value_compare_func The comparison function must return an integer less than, equal to, or greater than zero if the first argument is considered to be respectively less than, equal to, or greater than the second.
 * Note that before PHP 7.0.0 this integer had to be in the range from -2147483648 to 2147483647.
 *
 * @return bool Returns <code>TRUE</code> on success or <code>FALSE</code> on failure.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.usort.php
 */
function usort(array &$array, callable $value_compare_func)
{
}

/**
 * Dumps information about a variable
 *
 * @param mixed $expression The variable you want to dump.
 *
 * @return void No value is returned.
 *
 * @since PHP 4, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.var-dump.php
 */
function var_dump(...$expression)
{
}

/**
 * Outputs or returns a parsable string representation of a variable
 *
 * @param mixed $expression The variable you want to export.
 * @param bool|null $return If used and set to <code>TRUE</code>, <code>var_export</code> will return
 * the variable representation instead of outputting it.
 *
 * @return mixed Returns the variable representation when the <code>return</code>
 * parameter is used and evaluates to <code>TRUE</code>. Otherwise, this function will
 * return <code>NULL</code>.
 *
 * @since PHP 4 >= 4.2.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.var-export.php
 */
function var_export($expression, $return = false)
{
}

/**
 * Compares two "PHP-standardized" version number strings
 *
 * @param string $version1 First version number.
 * @param string $version2 Second version number.
 * @param mixed $oper
 *
 * @return int By default, <code>version_compare</code> returns
 * <code>-1</code> if the first version is lower than the second,
 * <code>0</code> if they are equal, and
 * <code>1</code> if the second is lower.
 * When using the optional <code>operator</code> argument, the
 * function will return <code>TRUE</code> if the relationship is the one specified
 * by the operator, <code>FALSE</code> otherwise.
 *
 * @since PHP 4 >= 4.1.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.version-compare.php
 */
function version_compare($version1, $version2, $oper = null)
{
}

/**
 * Write a formatted string to a stream
 *
 * @param resource $handle
 * @param string $format See <code>sprintf</code> for a description of
 * <code>format</code>.
 * @param array $args
 *
 * @return int Returns the length of the outputted string.
 *
 * @since PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.vfprintf.php
 */
function vfprintf($handle, $format, array $args)
{
}

/**
 * Output a formatted string
 *
 * @param string $format See <code>sprintf</code> for a description of
 * <code>format</code>.
 * @param array $args
 *
 * @return int Returns the length of the outputted string.
 *
 * @since PHP 4 >= 4.1.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.vprintf.php
 */
function vprintf($format, array $args)
{
}

/**
 * Return a formatted string
 *
 * @param string $format See <code>sprintf</code> for a description of
 * <code>format</code>.
 * @param array $args
 *
 * @return string Return array values as a formatted string according to
 * <code>format</code> (which is described in the documentation
 * for <code>sprintf</code>).
 *
 * @since PHP 4 >= 4.1.0, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.vsprintf.php
 */
function vsprintf($format, array $args)
{
}

/**
 * Wraps a string to a given number of characters
 *
 * @param string $str The input string.
 * @param int|null $width The number of characters at which the string will be wrapped.
 * @param string|null $break The line is broken using the optional
 * <code>break</code> parameter.
 * @param bool|null $cut If the <code>cut</code> is set to <code>TRUE</code>, the string is
 * always wrapped at or before the specified <code>width</code>. So if you have
 * a word that is larger than the given width, it is broken apart.
 * (See second example). When <code>FALSE</code> the function does not split the word
 * even if the <code>width</code> is smaller than the word width.
 *
 * @return string Returns the given string wrapped at the specified length.
 *
 * @since PHP 4 >= 4.0.2, PHP 5, PHP 7
 *
 * @link http://www.php.net/manual/en/function.wordwrap.php
 */
function wordwrap($str, $width = 75, $break = "\n", $cut = false)
{
}
